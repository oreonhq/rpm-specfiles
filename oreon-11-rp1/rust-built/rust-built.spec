%global source0_hash 5c0e531d93d39c34eef561e929e8a7f86d77a5af08aac4f6d6e39976c51858e9

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate built

Name:           rust-built
Version:        0.8.1
Release:        %autorelease
Summary:        Provides a crate with information from the time it was built.

License:        MIT
URL:            https://crates.io/crates/built
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Provides a crate with information from the time it was built.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+cargo-lock-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+cargo-lock-devel %{_description}

This package contains library source intended for building other packages which
use the "cargo-lock" feature of the "%{crate}" crate.

%files       -n %{name}+cargo-lock-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+chrono-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+chrono-devel %{_description}

This package contains library source intended for building other packages which
use the "chrono" feature of the "%{crate}" crate.

%files       -n %{name}+chrono-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+dependency-tree-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+dependency-tree-devel %{_description}

This package contains library source intended for building other packages which
use the "dependency-tree" feature of the "%{crate}" crate.

%files       -n %{name}+dependency-tree-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+git2-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+git2-devel %{_description}

This package contains library source intended for building other packages which
use the "git2" feature of the "%{crate}" crate.

%files       -n %{name}+git2-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gix-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gix-devel %{_description}

This package contains library source intended for building other packages which
use the "gix" feature of the "%{crate}" crate.

%files       -n %{name}+gix-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+semver-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+semver-devel %{_description}

This package contains library source intended for building other packages which
use the "semver" feature of the "%{crate}" crate.

%files       -n %{name}+semver-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
