%global source0_hash f84ac04429c13a7ff43785d75ad27569f2951ce0ffd30a3321230db2fc727216

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate phf_macros

Name:           rust-phf_macros
Version:        0.11.3
Release:        %autorelease
Summary:        Macros to generate types in the phf crate

License:        MIT
URL:            https://crates.io/crates/phf_macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Macros to generate types in the phf crate}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+unicase-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unicase-devel %{_description}

This package contains library source intended for building other packages which
use the "unicase" feature of the "%{crate}" crate.

%files       -n %{name}+unicase-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unicase_-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unicase_-devel %{_description}

This package contains library source intended for building other packages which
use the "unicase_" feature of the "%{crate}" crate.

%files       -n %{name}+unicase_-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
