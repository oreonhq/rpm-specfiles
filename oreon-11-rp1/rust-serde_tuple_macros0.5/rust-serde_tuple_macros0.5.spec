%global source0_hash 4076151d1a2b688e25aaf236997933c66e18b870d0369f8b248b8ab2be630d7e

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate serde_tuple_macros

Name:           rust-serde_tuple_macros0.5
Version:        0.5.0
Release:        %autorelease
Summary:        De/serialize structs with named fields as array of values

License:        MIT
URL:            https://crates.io/crates/serde_tuple_macros
Source:         %{crates_source}
# * https://github.com/kardeiz/serde_tuple/pull/12
Source2:        https://github.com/kardeiz/serde_tuple/raw/96b8643/LICENSE
# Manually created patch for downstream crate metadata changes
# * remove reference to readme file that is not included in published crates
Patch:          serde_tuple_macros-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
De/serialize structs with named fields as array of values.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE2} .
# remove stray executable bit from Rust source code
chmod -x src/lib.rs

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
