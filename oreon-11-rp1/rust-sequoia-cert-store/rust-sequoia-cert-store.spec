%global source0_hash c4e07e420427eb5e0dc3bb61172d181fa3c87afb2a77e8c629e920f54a291a6d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate sequoia-cert-store

Name:           rust-sequoia-cert-store
Version:        0.7.3
Release:        %autorelease
Summary:        A certificate database interface.

License:        LGPL-2.0-or-later
URL:            https://crates.io/crates/sequoia-cert-store
Source:         %{crates_source}
Patch:          sequoia-cert-store-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A certificate database interface.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+keyserver-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+keyserver-devel %{_description}

This package contains library source intended for building other packages which
use the "keyserver" feature of the "%{crate}" crate.

%files       -n %{name}+keyserver-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+sequoia-net-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+sequoia-net-devel %{_description}

This package contains library source intended for building other packages which
use the "sequoia-net" feature of the "%{crate}" crate.

%files       -n %{name}+sequoia-net-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio" feature of the "%{crate}" crate.

%files       -n %{name}+tokio-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
