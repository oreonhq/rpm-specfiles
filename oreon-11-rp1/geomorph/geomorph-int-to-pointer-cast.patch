diff -rup a/src/utils/utils.c b/src/utils/utils.c
--- a/src/utils/utils.c	2016-05-13 10:09:08.363325818 +0200
+++ b/src/utils/utils.c	2016-05-13 10:10:45.692674604 +0200
@@ -415,7 +415,7 @@ void my_msg_subcall(gchar * message, int
 	gtk_signal_connect_object(GTK_OBJECT(ok), "clicked",
 		GTK_SIGNAL_FUNC(gtk_widget_destroy), GTK_OBJECT(dialog));
 	gtk_signal_connect(GTK_OBJECT(dialog), "destroy",
-		GTK_SIGNAL_FUNC(msg_callb1), (void *)flag_exit);
+		GTK_SIGNAL_FUNC(msg_callb1), GINT_TO_POINTER(flag_exit));
 	gtk_signal_connect_object(GTK_OBJECT(dialog), "destroy",
 		GTK_SIGNAL_FUNC(g_main_loop_quit), (void *) loop);
 	gtk_container_add(GTK_CONTAINER(GTK_DIALOG(dialog)->action_area),ok);
diff -rup a/src/utils/menus_n_tools.c b/src/utils/menus_n_tools.c
--- a/src/utils/menus_n_tools.c	2016-05-13 10:12:00.706172701 +0200
+++ b/src/utils/menus_n_tools.c	2016-05-13 10:14:32.529156879 +0200
@@ -41,7 +41,7 @@ submenu_struct *submenu_new (gint ndx, g
 	if (NULL == (subm_buf = (submenu_struct *) x_malloc(sizeof(submenu_struct), "submenu_struct"))) 
 		my_msg("Allocation: submenu_struct ???\n",ABORT);
 	subm_buf->itemndx = (GList *)NULL;
-	subm_buf->itemndx = g_list_append(subm_buf->itemndx, (gpointer) ndx);
+	subm_buf->itemndx = g_list_append(subm_buf->itemndx, GINT_TO_POINTER(ndx));
 	subm_buf->sm_item = gtk_menu_item_new_with_label(label);
 	gtk_menu_bar_append (GTK_MENU_BAR (bar), subm_buf->sm_item);
 	gtk_widget_show(subm_buf->sm_item);
@@ -107,7 +107,7 @@ menu_struct *menu_new(gint nbitems, comm
 			item_buf);
 		// Keep the indexes for future use (?)
 		((submenu_struct *) g_list_last(menu->submenus)->data)->itemndx = 
-			g_list_append( ((submenu_struct *) g_list_last(menu->submenus)->data)->itemndx  , (gpointer) i);
+			g_list_append( ((submenu_struct *) g_list_last(menu->submenus)->data)->itemndx  , GINT_TO_POINTER(i));
 		(items+i)->menu_item = item_buf;  // For future use (ex. freeing the widget)
 		if ((items+i)->accel)
 			gtk_widget_add_accelerator (item_buf, "activate", accel_grp, 
