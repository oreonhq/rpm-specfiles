%global source0_hash 62cd9cf24f6c96a491f55ad9fbf7f8aa41cdcae154a262687fd90e9d17c99f0f

# Generated by go2rpm
%bcond check 0
%global debug_package %{nil}

# https://github.com/go-playground/validator
%global goipath         github.com/go-playground/validator/v10
%global forgeurl        https://github.com/go-playground/validator
Version:                10.10.0

%gometa

%global goaltipaths     gopkg.in/go-playground/validator.v10

%global common_description %{expand:
Package Validator implements value validations for structs and individual fields
based on tags.

It can also handle Cross-Field and Cross-Struct validation for nested structs
and has the ability to dive into arrays and maps of any type.}

%global golicenses      LICENSE
%global godocs          examples README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Go struct and field validation

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
