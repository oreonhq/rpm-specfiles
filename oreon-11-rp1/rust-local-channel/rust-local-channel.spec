%global source0_hash b6cbc85e69b8df4b8bb8b89ec634e7189099cea8927a276b7384ce5488e53ec8

# Generated by rust2rpm 25
%bcond check 0
%global debug_package %{nil}

%global crate local-channel

Name:           rust-local-channel
Version:        0.1.5
Release:        %autorelease
Summary:        Non-threadsafe multi-producer, single-consumer, futures-aware, FIFO queue

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/local-channel
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * add missing futures-util and tokio dev-dependencies:
#   https://github.com/actix/actix-net/blob/local-channel-v0.1.5/local-channel/Cargo.toml
Patch:          local-channel-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A non-threadsafe multi-producer, single-consumer, futures-aware, FIFO
queue.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGES.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
