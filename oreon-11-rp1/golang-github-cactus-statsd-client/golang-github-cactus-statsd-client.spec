%global source0_hash f553deaec3bf129a63a346dab457b192dbdd4d36646ff5fa6c5952f8c9f03f7f

# Generated by go2rpm
%bcond check 0

# https://github.com/cactus/go-statsd-client
%global goipath         github.com/cactus/go-statsd-client
Version:                5.0.0

%gometa

%global goaltipaths     github.com/cactus/go-statsd-client/v5

%global common_description %{expand:
A StatsD client for Go.}

%global golicenses      LICENSE.md
%global godocs          README.md CHANGELOG.md

Name:           %{goname}
Release:        19%{?dist}
Summary:        Statsd client for Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/jessevdk/go-flags)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in test-client; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license  LICENSE.md
%doc README.md CHANGELOG.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
