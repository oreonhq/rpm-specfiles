%global source0_hash d6d60529725b8fe0a75d30361961257f2dc0c99ae567256fb6d9e45bf5243036

# Generated by go2rpm 1.3
%bcond check 0

%global debug_package %{nil}

# https://github.com/gosuri/uilive
%global goipath         github.com/gosuri/uilive
Version:                0.0.4

%gometa

%global common_description %{expand:
Uilive is a go library for updating terminal output in realtime.}

%global golicenses      LICENSE
%global godocs          doc example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Go library for updating terminal output in realtime

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
