%global source0_hash 848b83d809e7bb987cbb253e9c3834c1a40d5e6d7da898c00c2d709378d8d6bb

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/mndrix/tap-go
%global goipath         github.com/mndrix/tap-go
%global commit          629fa407e90bbf3781e448e8e9b8931523ec823c

%gometa

%global common_description %{expand:
The Test Anything Protocol ("TAP") is a text-based interface between tests and a
test harness. This package helps Go to generate TAP output.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        %autorelease
Summary:        Test Anything Protocol for Go

License:        Unlicense
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
