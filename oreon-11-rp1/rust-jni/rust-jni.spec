%global source0_hash 5efd9a482cf3a427f00d6b35f14332adc7902ce91efb778580e180ff90fa3498

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate jni

Name:           rust-jni
Version:        0.22.4
Release:        %autorelease
Summary:        Rust bindings to the JNI

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/jni
Source:         %{crates_source}
Patch:          jni-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Rust bindings to the JNI}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+_cfg_test-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+_cfg_test-devel %{_description}

This package contains library source intended for building other packages which
use the "_cfg_test" feature of the "%{crate}" crate.

%files       -n %{name}+_cfg_test-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+invocation-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+invocation-devel %{_description}

This package contains library source intended for building other packages which
use the "invocation" feature of the "%{crate}" crate.

%files       -n %{name}+invocation-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
