%global source0_hash 6f25a01f65079fdaf562930db1293d7a4abd4d98346a450cb7f37073af5c2b10

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate superluminal-perf

Name:           rust-superluminal-perf
Version:        0.4.0
Release:        %autorelease
Summary:        Superluminal Performance API for adding user events to profiler captures

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/superluminal-perf
Source:         %{crates_source}
Patch:          superluminal-perf-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Superluminal Performance API for adding user events to profiler captures}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+enable-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+enable-devel %{_description}

This package contains library source intended for building other packages which
use the "enable" feature of the "%{crate}" crate.

%files       -n %{name}+enable-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
