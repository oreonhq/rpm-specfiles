%global source0_hash 439ef04fcd896b278b99de793c5b7bab02e13b628bc50df01cb22990c8587b40

# Generated by go2rpm
%bcond check 0

# https://github.com/akavel/rsrc
%global goipath         github.com/akavel/rsrc
Version:                0.10.2

%gometa

%global common_description %{expand:
Tool for embedding binary resources in Go programs.}

%global golicenses      LICENSE.txt
%global godocs          AUTHORS README.txt

Name:           %{goname}
Release:        18%{?dist}
Summary:        Tool for embedding .ico & manifest resources in Go programs

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
%gobuild -o %{gobuilddir}/bin/rsrc %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE.txt
%doc AUTHORS README.txt
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
