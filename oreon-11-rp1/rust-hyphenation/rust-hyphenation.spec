%global source0_hash bcf4dd4c44ae85155502a52c48739c8a48185d1449fff1963cffee63c28a50f0

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate hyphenation

Name:           rust-hyphenation
Version:        0.8.4
Release:        %autorelease
Summary:        Knuth-Liang hyphenation for a variety of languages

License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/hyphenation
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Knuth-Liang hyphenation for a variety of languages}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+build_dictionaries-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+build_dictionaries-devel %{_description}

This package contains library source intended for building other packages which
use the "build_dictionaries" feature of the "%{crate}" crate.

%files       -n %{name}+build_dictionaries-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+embed_all-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+embed_all-devel %{_description}

This package contains library source intended for building other packages which
use the "embed_all" feature of the "%{crate}" crate.

%files       -n %{name}+embed_all-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+embed_en-us-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+embed_en-us-devel %{_description}

This package contains library source intended for building other packages which
use the "embed_en-us" feature of the "%{crate}" crate.

%files       -n %{name}+embed_en-us-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nfc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nfc-devel %{_description}

This package contains library source intended for building other packages which
use the "nfc" feature of the "%{crate}" crate.

%files       -n %{name}+nfc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nfd-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nfd-devel %{_description}

This package contains library source intended for building other packages which
use the "nfd" feature of the "%{crate}" crate.

%files       -n %{name}+nfd-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nfkc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nfkc-devel %{_description}

This package contains library source intended for building other packages which
use the "nfkc" feature of the "%{crate}" crate.

%files       -n %{name}+nfkc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nfkd-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nfkd-devel %{_description}

This package contains library source intended for building other packages which
use the "nfkd" feature of the "%{crate}" crate.

%files       -n %{name}+nfkd-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
