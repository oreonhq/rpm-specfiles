%global source0_hash 2eed960aa9531a87c82d2379677723bc0129bc9e387dc0dbb3bb70b78c928dd0

# Generated by go2rpm 1.6.0
%bcond check 0

# https://github.com/GeertJohan/go.rice
%global goipath         github.com/GeertJohan/go.rice
Version:                1.0.2

%gometa

%global common_description %{expand:
Go.rice is a Go package that makes working with resources such as
html,js,css,images,templates, etc very easy.}

%global golicenses      LICENSE
%global godocs          example AUTHORS README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Go package that makes working with resources such as html, js, css very easy

License:        BSD-2-Clause
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in rice; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc example AUTHORS README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
