%global source0_hash b2b03ff07a220d0d0ec9a1f0f238951b7967a5a2e96aefcd21a117b1083415e9

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate opener

Name:           rust-opener
Version:        0.8.5
Release:        %autorelease
Summary:        Open a file or link using the system default program.

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/opener
Source:         %{crates_source}
Patch:          opener-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Open a file or link using the system default program.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%license %{crate_instdir}/LICENSE-THIRD-PARTY
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+reveal-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+reveal-devel %{_description}

This package contains library source intended for building other packages which
use the "reveal" feature of the "%{crate}" crate.

%files       -n %{name}+reveal-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
