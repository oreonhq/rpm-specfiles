%global source0_hash 7f64fe59e11af447d12fd60a403c74106eb104309f34b4c6dbce6e927d97da9d

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate uefi-raw

# compile and run tests only on supported architectures
%global unsupported_arches ppc64le s390x

Name:           rust-uefi-raw
Version:        0.13.0
Release:        %autorelease
Summary:        Raw UEFI types and bindings for protocols, boot, and runtime services

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/uefi-raw
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Raw UEFI types and bindings for protocols, boot, and runtime services.
This can serve as base for an UEFI firmware implementation or a high-
level wrapper to access UEFI functionality from an UEFI image.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/api_guidelines.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%ifnarch %{unsupported_arches}
%cargo_build
%endif

%install
%cargo_install

%if %{with check}
%ifnarch %{unsupported_arches}
%check
%cargo_test
%endif
%endif

%changelog
%autochangelog
