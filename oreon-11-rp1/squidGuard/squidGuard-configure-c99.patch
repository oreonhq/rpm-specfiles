Avoid calling the undeclared exit function, so that the tests do not change
results with strict(er) C99 compilers.  (Implicit function declarations were
removed with C99.)

diff -ur squidGuard-1.4.orig/configure squidGuard-1.4/configure
--- squidGuard-1.4.orig/configure	2009-01-03 21:05:25.000000000 +0100
+++ squidGuard-1.4/configure	2022-11-26 17:02:39.162492436 +0100
@@ -4295,7 +4295,7 @@
        {
                LDAP *p;
                p = ldap_init("localhost", LDAP_PORT);
-               exit(0);
+               return 0;
        }
 
 _ACEOF
@@ -4620,8 +4620,8 @@
 #endif
 		ver = major + ((float) minor / 1000);
                 if (ver >= 2.006)
-			exit (0);
-		exit (1);
+			return 0;
+		return 1;
 	}
 
 _ACEOF
@@ -4689,8 +4689,8 @@
 #endif
 		ver = major + ((float) minor / 1000);
                 if (ver > 2.007 && ver < 3.002)
-			exit (1);
-		exit (0);
+			return 1;
+		return 0;
 	}
 
 _ACEOF
@@ -4759,8 +4759,8 @@
 #endif
 		ver = major + ((float) minor / 1000);
                 if (ver >= 3.002)
-			exit (0);
-		exit (1);
+			return 0;
+		return 1;
 	}
 
 _ACEOF
@@ -4824,8 +4824,8 @@
 #endif
                 ver = major + ((float) minor / 1000);
                 if (ver >= 4.002)
-                        exit (0);
-                exit (1);
+                        return 0;
+                return 1;
         }
 
 _ACEOF
diff -ur squidGuard-1.4.orig/configure.in squidGuard-1.4/configure.in
--- squidGuard-1.4.orig/configure.in	2009-01-03 21:04:30.000000000 +0100
+++ squidGuard-1.4/configure.in	2022-11-26 17:02:54.937327301 +0100
@@ -194,7 +194,7 @@
        {
                LDAP *p;
                p = ldap_init("localhost", LDAP_PORT);
-               exit(0);
+               return 0;
        }
        ],,[
                echo
@@ -311,8 +311,8 @@
 #endif
 		ver = major + ((float) minor / 1000);
                 if (ver >= 2.006)
-			exit (0);
-		exit (1);
+			return 0;
+		return 1;
 	}	
 	], db_ok_version=yes, db_ok_version=no, db_ok_version=no)
 
@@ -342,8 +342,8 @@
 #endif
 		ver = major + ((float) minor / 1000);
                 if (ver > 2.007 && ver < 3.002)
-			exit (1);
-		exit (0);
+			return 1;
+		return 0;
 	}	
 	], db_ok_version=yes, db_ok_version=no, db_ok_version=no)
 
@@ -374,8 +374,8 @@
 #endif
 		ver = major + ((float) minor / 1000);
                 if (ver >= 3.002)
-			exit (0);
-		exit (1);
+			return 0;
+		return 1;
 	}	
 	], dbg2_ok_version=yes, dbg2_ok_version=no, dbg2_ok_version=no)
 
@@ -398,8 +398,8 @@
 #endif
                 ver = major + ((float) minor / 1000);
                 if (ver >= 4.002)
-                        exit (0);
-                exit (1);
+                        return 0;
+                return 1;
         }
         ], dbg3_ok_version=yes, dbg3_ok_version=no, dbg3_ok_version=no)
 
