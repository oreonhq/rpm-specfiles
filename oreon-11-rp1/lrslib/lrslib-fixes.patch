--- 2nash.c.orig	2024-05-29 21:07:39.000000000 -0600
+++ 2nash.c	2025-10-20 15:29:24.315752528 -0600
@@ -9,6 +9,7 @@
 #include <unistd.h>
 #include <stdio.h>
 #include <signal.h>
+#include <fcntl.h>
 
        int main(int argc, char *argv[])
 	       {
@@ -28,14 +29,15 @@
 		      if(cpid[l-1] == 0) {
 			 //forked threads
 			// n= sprintf(buffer, "lrs %s > out%i", argv[l], l);
+                         sprintf(buffer, "out%i", l);
+                         int fd = creat(buffer, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
                          if(l==1) {
-                               sprintf(buffer, "lrsnash %s %s > out%i", argv[1], argv[2], l);
+                               execlp("lrsnash", "lrsnash", argv[1], argv[2], NULL);
                          }
                          else     {
-                               sprintf(buffer, "lrsnash %s %s > out%i", argv[2], argv[1], l);
+                               execlp("lrsnash", "lrsnash", argv[2], argv[1], NULL);
                          }
 
-			  system(buffer);
                           _exit(0);
 		      }
 		  }
@@ -50,18 +52,18 @@
 			      printf("lrsnash %s %s   finished first\n", argv[2], argv[1]);
 			      printf("player numbers will be reversed in output\n");
                                }
+                           sprintf(buffer, "out%i", j);
                            if(argc == 4) {
 			       printf("output file: %s\n", argv[3]);
-			       sprintf(buffer, "/bin/mv -f out%i %s", j, argv[3]);
+			       rename(buffer, argv[3]);
                            }
                            else  {
 			        printf("output file: out\n");
-			        sprintf(buffer, "/bin/mv -f out%i out", j);
+			        rename(buffer, "out");
                            }
-			 system(buffer);
 		      } else {
-			  sprintf(buffer, "/bin/rm -f out%i", j);
-			  system(buffer);
+			  sprintf(buffer, "out%i", j);
+			  unlink(buffer);
 		      }
 		  }
 		  printf("the other process will be ");   /*...will be killed */
--- hvref.c.orig	2024-05-29 21:07:39.000000000 -0600
+++ hvref.c	2025-10-20 15:39:03.949635242 -0600
@@ -34,8 +34,8 @@ int main(int argc, char *argv[]) {
   char s[10000];
   int x;
 
-  int *nrow;
-  int **table;
+  int *nrow = NULL;
+  int **table = NULL;
 
   FILE *lrs_ifp;
 
