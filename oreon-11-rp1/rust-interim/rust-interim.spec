%global source0_hash a9ce9099a85f468663d3225bf87e85d0548968441e1db12248b996b24f0f5b5a

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate interim

Name:           rust-interim
Version:        0.2.1
Release:        %autorelease
Summary:        Parses simple English dates, inspired by Linux date command

License:        MIT
URL:            https://crates.io/crates/interim
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Remove unused features: jiff_0.1
# * Relax logos dependency
Patch:          interim-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Parses simple English dates, inspired by Linux date command, and forked
from chrono-english.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+chrono_0_4-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+chrono_0_4-devel %{_description}

This package contains library source intended for building other packages which
use the "chrono_0_4" feature of the "%{crate}" crate.

%files       -n %{name}+chrono_0_4-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+jiff_0_2-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+jiff_0_2-devel %{_description}

This package contains library source intended for building other packages which
use the "jiff_0_2" feature of the "%{crate}" crate.

%files       -n %{name}+jiff_0_2-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+time_0_3-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+time_0_3-devel %{_description}

This package contains library source intended for building other packages which
use the "time_0_3" feature of the "%{crate}" crate.

%files       -n %{name}+time_0_3-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -a

%build
%cargo_build -a

%install
%cargo_install -a

%if %{with check}
%check
%cargo_test -a
%endif

%changelog
%autochangelog
