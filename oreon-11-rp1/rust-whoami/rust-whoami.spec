%global source0_hash 998767ef88740d1f5b0682a9c53c24431453923962269c2db68ee43788c5a40d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate whoami

Name:           rust-whoami
Version:        2.1.2
Release:        %autorelease
Summary:        Rust library for getting information about the current user and envir...

License:        Apache-2.0 OR BSL-1.0 OR MIT
URL:            https://crates.io/crates/whoami
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Rust library for getting information about the current user and environment}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE_APACHE
%license %{crate_instdir}/LICENSE_BOOST
%license %{crate_instdir}/LICENSE_MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+force-stub-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+force-stub-devel %{_description}

This package contains library source intended for building other packages which
use the "force-stub" feature of the "%{crate}" crate.

%files       -n %{name}+force-stub-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+wasi-wasite-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+wasi-wasite-devel %{_description}

This package contains library source intended for building other packages which
use the "wasi-wasite" feature of the "%{crate}" crate.

%files       -n %{name}+wasi-wasite-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+wasm-web-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+wasm-web-devel %{_description}

This package contains library source intended for building other packages which
use the "wasm-web" feature of the "%{crate}" crate.

%files       -n %{name}+wasm-web-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
