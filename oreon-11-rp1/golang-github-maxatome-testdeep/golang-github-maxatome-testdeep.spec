%global source0_hash 5b9690aac1dd7c93f7467ce721a365805e46cb60dd8a5c24ee30fbbfb216f9f5

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/maxatome/go-testdeep
%global goipath         github.com/maxatome/go-testdeep
Version:                1.13.0

%gometa -f

%global common_description %{expand:
Extremely flexible golang deep comparison, extends the go testing package,
tests HTTP APIs and provides tests suite.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Tests HTTP APIs and provides tests suite

License:        BSD-2-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
