%global source0_hash c04141a07bb0c0bc461cb657808764de571702a59bc5c726c400ac9a7625e3ab

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate libssh2-sys

Name:           rust-libssh2-sys
Version:        0.3.2
Release:        %autorelease
Summary:        Native bindings to the libssh2 library

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/libssh2-sys
Source:         %{crates_source}
Patch:          libssh2-sys-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Native bindings to the libssh2 library}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT

%{crate_instdir}/

%package     -n %{name}+openssl-on-win32-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+openssl-on-win32-devel %{_description}

This package contains library source intended for building other packages which
use the "openssl-on-win32" feature of the "%{crate}" crate.

%files       -n %{name}+openssl-on-win32-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+vendored-openssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vendored-openssl-devel %{_description}

This package contains library source intended for building other packages which
use the "vendored-openssl" feature of the "%{crate}" crate.

%files       -n %{name}+vendored-openssl-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zlib-ng-compat-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zlib-ng-compat-devel %{_description}

This package contains library source intended for building other packages which
use the "zlib-ng-compat" feature of the "%{crate}" crate.

%files       -n %{name}+zlib-ng-compat-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
