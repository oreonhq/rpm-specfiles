%global source0_hash 2a1867f8f005bd7fb73c367e2e45dd628417906a2ca27597fe59cbf04279a222

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate sealed_test

Name:           rust-sealed_test
Version:        1.1.0
Release:        %autorelease
Summary:        Procmacro attribute to run your test in an isolated environment

License:        MIT
URL:            https://crates.io/crates/sealed_test
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Update cmd_lib dev-dependency to 2.0,
#   https://github.com/oknozor/sealed-test/pull/9
Patch:          sealed_test-fix-metadata.diff
# * Fix test code with cmd_lib 1.9.6 (PathBuf does not implement Display)
# * An alternative approach was submitted upstream as
#   https://github.com/oknozor/sealed-test/pull/8.
Patch10:        0001-Fix-test-code-with-cmd_lib-1.9.6-PathBuf-does-not-im.patch

BuildRequires:  cargo-rpm-macros >= 24
%if %{with check}
BuildRequires:  git-core
%endif

%global _description %{expand:
A procmacro attribute to run your test in an isolated environment.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
git config --global user.name fedora
git config --global user.email fedora@example.com
%cargo_test
%endif

%changelog
%autochangelog
