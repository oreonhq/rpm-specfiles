# weldr-client

The weldr-client package

This now uses the go vendor tools and macros, before making a new release run:

  make update-vendor

in order to update the vendor archive, and then upload the new one alongside
the new source tar and signature.

