%global source0_hash c3b62c76d365906bba9aa9d9ec048fd33a2a5742299d6482a08e7466d4c52d77

# Generated by go2rpm 1.8.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/mattn/go-colorable
%global goipath         github.com/mattn/go-colorable
Version:                0.1.13

%gometa

%global common_description %{expand:
Colorable writer for windows.

For example, most of logger packages doesn't show colors on windows. This
package is possible to handle escape sequence for ansi color on windows.}

%global golicenses      LICENSE
%global godocs          _example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Colorable writer for windows

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

BuildRequires:  golang(github.com/mattn/go-isatty)

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
