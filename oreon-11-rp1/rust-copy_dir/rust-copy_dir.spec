%global source0_hash 543d1dd138ef086e2ff05e3a48cf9da045da2033d16f8538fd76b86cd49b2ca3

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate copy_dir

Name:           rust-copy_dir
Version:        0.1.3
Release:        %autorelease
Summary:        Copy directories recursively in a straightforward and predictable way

License:        MIT
URL:            https://crates.io/crates/copy_dir
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * port tests from tempdir to tempfile:
#   https://github.com/mdunsmuir/copy_dir/pull/8
Patch:          copy_dir-fix-metadata.diff
Patch:          0001-Port-from-tempdir-to-tempfile.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Copy directories recursively in a straightforward and predictable way.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
