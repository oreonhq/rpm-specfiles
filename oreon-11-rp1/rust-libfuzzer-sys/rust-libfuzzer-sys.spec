%global source0_hash a9fd2f41a1cba099f79a0b6b6c35656cf7c03351a7bae8ff0f28f25270f929d2

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate libfuzzer-sys

Name:           rust-libfuzzer-sys
Version:        0.4.13
Release:        %autorelease
Summary:        A wrapper around LLVM's libFuzzer runtime.

License:        (MIT OR Apache-2.0) AND NCSA
URL:            https://crates.io/crates/libfuzzer-sys
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A wrapper around LLVM's libFuzzer runtime.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+arbitrary-derive-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+arbitrary-derive-devel %{_description}

This package contains library source intended for building other packages which
use the "arbitrary-derive" feature of the "%{crate}" crate.

%files       -n %{name}+arbitrary-derive-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+link_libfuzzer-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+link_libfuzzer-devel %{_description}

This package contains library source intended for building other packages which
use the "link_libfuzzer" feature of the "%{crate}" crate.

%files       -n %{name}+link_libfuzzer-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
