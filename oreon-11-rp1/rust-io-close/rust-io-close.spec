%global source0_hash 9cadcf447f06744f8ce713d2d6239bb5bde2c357a452397a9ed90c625da390bc

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate io-close

Name:           rust-io-close
Version:        0.3.7
Release:        %autorelease
Summary:        An extension trait for safely dropping I/O writers such as File and B...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/io-close
Source:         %{crates_source}
Patch:          io-close-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An extension trait for safely dropping I/O writers such as File and BufWriter.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+os_pipe-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+os_pipe-devel %{_description}

This package contains library source intended for building other packages which
use the "os_pipe" feature of the "%{crate}" crate.

%files       -n %{name}+os_pipe-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
