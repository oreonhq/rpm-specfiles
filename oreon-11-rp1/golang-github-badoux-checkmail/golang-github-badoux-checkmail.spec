%global source0_hash 41e1b4afb111bcbec7b0650a4a02ea9f68dd4178f80dc3e33727c45481dfe6eb

# Generated by go2rpm 1.3
%bcond check 0
%bcond_with network
%global debug_package %{nil}

# https://github.com/badoux/checkmail
%global goipath         github.com/badoux/checkmail
Version:                1.2.1

%gometa

%global common_description %{expand:
Golang package for email validation.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Golang package for email validation

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%if %{without network}
%global gotestflags %{?gotestflags} -run TestValidateFormat
%endif
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
