%global source0_hash e3f28bb3fa0803c889c5affb32b6b4665e606f1579e1402d0a5a458e71046fdd

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/u-root/uio
%global goipath         github.com/u-root/uio
%global commit          a3c409a6018e0dacb898ea857dc899a3aca5c51f

%gometa -f

%global common_description %{expand:
Experimental, for breaking up circular dependencies in u-root, so we can remove
version tags.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        %autorelease -p
Summary:        Golang package for breaking up circular dependencies in u-root

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
