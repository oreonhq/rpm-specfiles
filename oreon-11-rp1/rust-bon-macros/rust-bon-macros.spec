%global source0_hash 519bd3116aeeb42d5372c29d982d16d0170d3d4a5ed85fc7dd91642ffff3c67c

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate bon-macros

Name:           rust-bon-macros
Version:        3.9.1
Release:        %autorelease
Summary:        Compile-time-checked builder generator

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/bon-macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
This is a proc-macro crate that is supposed to be a private
implementation detail of the `bon` crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+experimental-generics-setters-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+experimental-generics-setters-devel %{_description}

This package contains library source intended for building other packages which
use the "experimental-generics-setters" feature of the "%{crate}" crate.

%files       -n %{name}+experimental-generics-setters-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+experimental-overwritable-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+experimental-overwritable-devel %{_description}

This package contains library source intended for building other packages which
use the "experimental-overwritable" feature of the "%{crate}" crate.

%files       -n %{name}+experimental-overwritable-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+implied-bounds-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+implied-bounds-devel %{_description}

This package contains library source intended for building other packages which
use the "implied-bounds" feature of the "%{crate}" crate.

%files       -n %{name}+implied-bounds-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
