%global source0_hash cd05f409e63674f29cff0e496bd33eee70229985243cce486107085fab747082

# Generated by go2rpm
%bcond check 0

# https://github.com/rakyll/statik
%global goipath         github.com/rakyll/statik
Version:                0.1.7

%gometa

%global common_description %{expand:
Statik allows you to embed a directory of static files into your Go binary to be
later served from an http.FileSystem.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Embed files into a Go executable

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}
# Use Modified.UTC() instead of ModTime() for Go 1.13
Patch0:         0001-Use-Modified.UTC-instead-of-ModTime.patch
Patch1:         0001-Fix-Println-arg-list-ends-with-redundant-newline.patch

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%patch -P0 -p1
%patch -P1 -p1

%build
%gobuild -o %{gobuilddir}/bin/statik %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck -d fs
%endif

%files
%license LICENSE
%doc example README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
