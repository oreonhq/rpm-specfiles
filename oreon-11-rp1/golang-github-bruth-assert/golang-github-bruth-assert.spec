%global source0_hash 8acc63aa6c735b9ede817bc72e6afe5bb4e9b28b7c53b81fefadce7fe5b9e8ef

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/bruth/assert
%global goipath         github.com/bruth/assert
%global commit          de420fa3b72e87255136e10ade1e267581cc3947

%gometa

%global common_description %{expand:
Simple test assertions for Go. This is a fork of a fork of a bmizerany/assert
with improved support for things like nil pointers, etc.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        %autorelease
Summary:        Simple test assertions for Golang tests

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Patch0:         add-license.patch

BuildRequires:  golang(github.com/kr/pretty)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%patch -P0 -p1

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
