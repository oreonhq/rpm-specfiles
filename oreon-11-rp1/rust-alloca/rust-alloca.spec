%global source0_hash e5a7d05ea6aea7e9e64d25b9156ba2fee3fdd659e34e41063cd2fc7cd020d7f4

# Generated by rust2rpm 28
# * skip checks in mock / awkward deps
%bcond check 0
%global debug_package %{nil}

%global crate alloca

Name:           rust-alloca
Version:        0.4.0
Release:        %autorelease
Summary:        Mostly safe wrapper for alloca

License:        MIT
URL:            https://crates.io/crates/alloca
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Mostly safe wrapper for alloca.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+stack-clash-protection-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+stack-clash-protection-devel %{_description}

This package contains library source intended for building other packages which
use the "stack-clash-protection" feature of the "%{crate}" crate.

%files       -n %{name}+stack-clash-protection-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install
rm -f %{buildroot}%{_bindir}/alloca

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
