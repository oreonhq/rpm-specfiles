%global source0_hash 95a70d956c93ddbe5332c2cf0a8150e54a1e11f9d4d61c21b833b710dfd63d9a

# Generated by go2rpm 1.9.0
# Requires fuse module
%bcond check 1
%global debug_package %{nil}

# https://github.com/hanwen/go-fuse
%global goipath         github.com/hanwen/go-fuse/v2
Version:                2.4.0

%gometa -f

%global common_description %{expand:
FUSE bindings for Go.}

%global golicenses      LICENSE
%global godocs          example AUTHORS README.md

Name:           %{goname}
Release:        %autorelease
Summary:        FUSE bindings for Go

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
