%global source0_hash 0df3183760954d877b894d42f5c6954837d8a7d7bd5f042409c4175afeaf24ea

# Generated by rust2rpm 28
# * Tests required to have a running instance of Monado
%bcond check 0
%global debug_package %{nil}

%global crate libmonado

Name:           rust-libmonado
Version:        1.6.0
Release:        %autorelease
Summary:        Rust bindings to the libmonado library that controls Monado from other processes

License:        MIT
URL:            https://crates.io/crates/libmonado
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Rust bindings to the libmonado library that controls Monado from other
processes.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+arc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+arc-devel %{_description}

This package contains library source intended for building other packages which
use the "arc" feature of the "%{crate}" crate.

%files       -n %{name}+arc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rc-devel %{_description}

This package contains library source intended for building other packages which
use the "rc" feature of the "%{crate}" crate.

%files       -n %{name}+rc-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
