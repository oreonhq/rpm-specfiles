%global source0_hash 9c845311f0ff7951c5506121a9ad75aec44d083c31583b2ea5a30bcb0b0abba0

# Generated by rust2rpm 27
# * Tests require the previous version of rstest.
%bcond check 0
%global debug_package %{nil}

%global crate rstest_macros

Name:           rust-rstest_macros
Version:        0.26.1
Release:        %autorelease
Summary:        Rust fixture based test framework

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/rstest_macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Rust fixture based test framework. It use procedural macro to implement
fixtures and table based tests.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+async-timeout-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+async-timeout-devel %{_description}

This package contains library source intended for building other packages which
use the "async-timeout" feature of the "%{crate}" crate.

%files       -n %{name}+async-timeout-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+crate-name-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+crate-name-devel %{_description}

This package contains library source intended for building other packages which
use the "crate-name" feature of the "%{crate}" crate.

%files       -n %{name}+crate-name-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
