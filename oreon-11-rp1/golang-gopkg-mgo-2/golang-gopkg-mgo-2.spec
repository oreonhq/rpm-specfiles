%global source0_hash c2cffcba50754ad15ea9459d641f477ce815365868f4c6df4120ce756f26c32c

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/go-mgo/mgo
%global goipath         gopkg.in/mgo.v2
%global forgeurl        https://github.com/go-mgo/mgo
Version:                r2016.08.01
%global commit          a6b53ec6cb22a3699387a57a161566f9402ee85b

%gometa -f

%global common_description %{expand:
The MongoDB driver for Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        MongoDB driver for Go

# Upstream license specification: BSD-2-Clause
License:        BSD
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(gopkg.in/tomb.v2)
BuildRequires:  cyrus-sasl-devel

%if %{with check}
# Tests
BuildRequires:  golang(gopkg.in/check.v1)
BuildRequires:  golang(gopkg.in/yaml.v2)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
# .: needs network
# txn: needs mongodb
%gocheck -d . -d dbtest -d txn
%endif

%gopkgfiles

%changelog
%autochangelog
