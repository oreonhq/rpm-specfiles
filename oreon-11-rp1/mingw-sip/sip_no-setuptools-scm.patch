diff -rupN --no-dereference sip-6.15.1/pyproject.toml sip-6.15.1-new/pyproject.toml
--- sip-6.15.1/pyproject.toml	2025-12-17 10:48:31.000000000 +0100
+++ sip-6.15.1-new/pyproject.toml	2026-01-30 23:17:41.415028050 +0100
@@ -1,11 +1,12 @@
 # The project configuration for sip.
 
 [build-system]
-requires = ["setuptools>=77", "setuptools_scm>=8"]
+requires = ["setuptools>=77"]
 build-backend = "setuptools.build_meta"
 
-[tool.setuptools_scm]
-version_file = "sipbuild/_version.py"
+[tool.setuptools]
+packages = ["sipbuild"]
+py-modules = ["__init__"]
 
 [project]
 name = "sip"
@@ -16,7 +17,7 @@ dependencies = ["packaging>=24.2", "setu
 requires-python = ">=3.10"
 license = "BSD-2-Clause"
 license-files = ["LICENSE"]
-dynamic = ["version"]
+version = "@version@"
 
 [[project.authors]]
 name = "Phil Thompson"
