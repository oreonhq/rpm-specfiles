%global source0_hash 2a8d17885f0219a4c4a0665d8121805e84c79d110764813096fdfd9f0a07a9a5

# Generated by go2rpm 1.3
%bcond check 0

# https://github.com/sophaskins/efs2tar
%global goipath         github.com/sophaskins/efs2tar
%global commit          4db1b0fddc0353a041f48301fd12e4d058a3a240

%gometa

%global common_description %{expand:
efs2tar is a tool that converts SGI EFS-formatted filesystem images (ie, the
result of dd-ing a whole device in to a file) in to tarballs. It was based
entirely on NetBSD's sys/fs/efs.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        0.19%{?dist}
Summary:        A tool to convert SGI efs filesystems to tarballs

# Upstream license specification: BSD-2-Clause
# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/davecgh/go-spew/spew)

%description
%{common_description}

%package -n     efs2tar
Summary:        %{summary}
%description -n efs2tar
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
# These are one-off debugging utilities that aren't useful to the end user
# for cmd in cmd/* ; do
#  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
# done
%gobuild -o %{gobuilddir}/bin/efs2tar %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files -n efs2tar
%license LICENSE
%doc README.md
%{_bindir}/efs2tar

%gopkgfiles

%changelog
%autochangelog
