diff -up ntp-4.2.8p18/ntpd/ntp_control.c.orig ntp-4.2.8p18/ntpd/ntp_control.c
--- ntp-4.2.8p18/ntpd/ntp_control.c.orig	2024-05-07 13:21:15.000000000 +0200
+++ ntp-4.2.8p18/ntpd/ntp_control.c	2025-02-18 16:27:45.385967437 +0100
@@ -3664,16 +3664,16 @@ static u_int32 derive_nonce(
 	}
 
 	MD5Init(&ctx);
-	MD5Update(&ctx, salt, sizeof(salt));
-	MD5Update(&ctx, &ts_i, sizeof(ts_i));
-	MD5Update(&ctx, &ts_f, sizeof(ts_f));
+	MD5Update(&ctx, (const void *)salt, sizeof(salt));
+	MD5Update(&ctx, (const void *)&ts_i, sizeof(ts_i));
+	MD5Update(&ctx, (const void *)&ts_f, sizeof(ts_f));
 	if (IS_IPV4(addr)) {
-		MD5Update(&ctx, &SOCK_ADDR4(addr), sizeof(SOCK_ADDR4(addr)));
+		MD5Update(&ctx, (const void *)&SOCK_ADDR4(addr), sizeof(SOCK_ADDR4(addr)));
 	} else {
-		MD5Update(&ctx, &SOCK_ADDR6(addr), sizeof(SOCK_ADDR6(addr)));
+		MD5Update(&ctx, (const void *)&SOCK_ADDR6(addr), sizeof(SOCK_ADDR6(addr)));
 	}
-	MD5Update(&ctx, &NSRCPORT(addr), sizeof(NSRCPORT(addr)));
-	MD5Update(&ctx, salt, sizeof(salt));
+	MD5Update(&ctx, (const void *)&NSRCPORT(addr), sizeof(NSRCPORT(addr)));
+	MD5Update(&ctx, (const void *)salt, sizeof(salt));
 	MD5Final(d.digest, &ctx);
 
 	return d.extract;
