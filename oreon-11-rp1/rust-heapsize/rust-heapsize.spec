%global source0_hash 1679e6ea370dee694f91f1dc469bf94cf8f52051d147aec3e1f9497c6fc22461

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate heapsize

Name:           rust-heapsize
Version:        0.4.2
Release:        %autorelease
Summary:        Infrastructure for measuring the total runtime size of an object on t...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/heapsize
Source:         %{crates_source}
Patch:          heapsize-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Infrastructure for measuring the total runtime size of an object on the heap}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+flexible-tests-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+flexible-tests-devel %{_description}

This package contains library source intended for building other packages which
use the "flexible-tests" feature of the "%{crate}" crate.

%files       -n %{name}+flexible-tests-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unstable-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-devel %{_description}

This package contains library source intended for building other packages which
use the "unstable" feature of the "%{crate}" crate.

%files       -n %{name}+unstable-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
