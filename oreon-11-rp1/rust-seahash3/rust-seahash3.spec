%global source0_hash 58f57ca1d128a43733fd71d583e837b1f22239a37ebea09cde11d8d9a9080f47

# Generated by rust2rpm 24
%bcond check 0
%global debug_package %{nil}

%global crate seahash

Name:           rust-seahash3
Version:        3.0.7
Release:        %autorelease
Summary:        Blazingly fast, portable hash function with proven statistical guarantees

License:        MIT
URL:            https://crates.io/crates/seahash
Source0:        %{crates_source}
# https://github.com/redox-os/tfs/pull/76
Source1:        https://github.com/redox-os/tfs/raw/facbfb6/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A blazingly fast, portable hash function with proven statistical
guarantees.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/
%exclude %{crate_instdir}/logo.png

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
