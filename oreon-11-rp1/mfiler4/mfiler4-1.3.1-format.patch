--- mfiler4-1.3.1/src/filer.c.format	2013-12-29 22:36:45.000000000 +0900
+++ mfiler4-1.3.1/src/filer.c	2022-07-22 10:20:24.129100562 +0900
@@ -2456,7 +2456,7 @@ void cmdline_view_filer()
 
     char buf[1024];
     str_cut(gKanjiCode, SFD(nextout).mBuf, maxx, buf, 1024);
-    mvprintw(maxy-2, 0, buf);
+    mvprintw(maxy-2, 0, "%s", buf);
     //mvprintw(maxy-2, 0, SFD(nextout).mBuf);
 
     (void)vector_pop_back(gStackFrames);
--- mfiler4-1.3.1/src/gui.c.format	2013-12-29 22:36:45.000000000 +0900
+++ mfiler4-1.3.1/src/gui.c	2022-07-22 10:25:43.752630052 +0900
@@ -694,7 +694,7 @@ void mbox(int y, int x, int width, int h
    hbar[i] = '+';
    hbar[i+1] = 0;
 
-   mvprintw(y, x, hbar);
+   mvprintw(y, x, "%s", hbar);
    for(i=0; i<height-2; i++) {
        char hbar2[256];
 
@@ -706,10 +706,10 @@ void mbox(int y, int x, int width, int h
        hbar2[j] = '|';
        hbar2[j+1] = 0;
 
-       mvprintw(y + 1 +i, x, hbar2);
+       mvprintw(y + 1 +i, x, "%s", hbar2);
    }
 
-   mvprintw(y + height-1, x, hbar);
+   mvprintw(y + height-1, x, "%s", hbar);
 }
 
 void mclear_lastline()
@@ -726,5 +726,5 @@ void mclear_lastline()
     space[x] = 0;
 
     attron(0);
-    mvprintw(maxy-1, 0, space);
+    mvprintw(maxy-1, 0, "%s", space);
 }
--- mfiler4-1.3.1/src/isearch.c.format	2013-12-29 22:36:45.000000000 +0900
+++ mfiler4-1.3.1/src/isearch.c	2022-07-22 10:22:18.791931781 +0900
@@ -677,7 +677,7 @@ void isearch_view()
         }
     }
     buf[i] = 0;
-    printw(buf);
+    printw("%s", buf);
 }
 #endif
 
--- mfiler4-1.3.1/src/menu.c.format	2013-12-29 22:36:45.000000000 +0900
+++ mfiler4-1.3.1/src/menu.c	2022-07-22 10:24:12.264764746 +0900
@@ -375,7 +375,7 @@ void menu_view()
         mbox(0, 2, item_maxx, size < maxy ? size : maxy);
 
         attron(A_BOLD);
-        mvprintw(0, 4, self->mTitle);
+        mvprintw(0, 4, "%s", self->mTitle);
         attroff(A_BOLD);
 
         float f;
@@ -402,11 +402,11 @@ void menu_view()
             
             if(i == self->mCursor) {
                 attron(A_REVERSE);
-                mvprintw(i-self->mScrollTop+1, 3, buf);
+                mvprintw(i-self->mScrollTop+1, 3, "%s", buf);
                 attroff(A_REVERSE);
             }
             else  {
-                mvprintw(i-self->mScrollTop+1, 3, buf);
+                mvprintw(i-self->mScrollTop+1, 3, "%s", buf);
             }
         }
 
