%global source0_hash e2db585e1d738fc771bf08a151420d3ed193d9d895a36df7f6f8a9456b911ddc

# Generated by rust2rpm 24
%bcond check 0
%global debug_package %{nil}

%global crate khronos_api

Name:           rust-khronos_api
Version:        3.1.0
Release:        %autorelease
Summary:        Khronos XML API Registry, exposed as byte string constants

# * khronos_api crate itself is Apache-2.0
# * bundled Khronos API files are either Apache-2.0 or MIT
#   and contain either Apache-2.0 headers or full copies of the MIT license
License:        Apache-2.0 AND MIT
URL:            https://crates.io/crates/khronos_api
Source0:        %{crates_source}
# https://github.com/brendanzab/gl-rs/pull/520
Source1:        https://github.com/brendanzab/gl-rs/raw/khronos_api-v%{version}/LICENSE
# Manually created patch for downstream crate metadata changes
# * update license in crate metadata to reflect reality
Patch:          khronos_api-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
The Khronos XML API Registry, exposed as byte string constants.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
