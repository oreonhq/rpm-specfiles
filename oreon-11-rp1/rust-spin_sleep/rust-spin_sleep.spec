%global source0_hash 9c07347b7c0301b9adba4350bdcf09c039d0e7160922050db0439b3c6723c8ab

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate spin_sleep

Name:           rust-spin_sleep
Version:        1.3.3
Release:        %autorelease
Summary:        Accurate sleeping

License:        Apache-2.0
URL:            https://crates.io/crates/spin_sleep
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          spin_sleep-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Accurate sleeping. Only use native sleep as far as it can be trusted,
then spin.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nondeterministic_tests-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nondeterministic_tests-devel %{_description}

This package contains library source intended for building other packages which
use the "nondeterministic_tests" feature of the "%{crate}" crate.

%files       -n %{name}+nondeterministic_tests-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
