%global source0_hash c2db04e74f0a9a93103b50e90b96024c9b2bdca8bce6a632ec71b88736d3d359

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate doctest-file

Name:           rust-doctest-file
Version:        1.1.1
Release:        %autorelease
Summary:        Macro for sourcing doctests from dedicated files into Rustdoc

License:        0BSD
URL:            https://crates.io/crates/doctest-file
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Sources doctests from dedicated files into Rustdoc documentation while
hiding select lines.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * doc: some comments in lib.rs are interpreted as an invalid test
%cargo_test -- --bins
%cargo_test -- --lib
%endif

%changelog
%autochangelog
