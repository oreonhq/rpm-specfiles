%global source0_hash 84d57bc0c8b9a17920c178daa6bb924850d54a9c97ab45194bb8c17ad66bb660

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate serde_with_macros

Name:           rust-serde_with_macros
Version:        3.21.0
Release:        %autorelease
Summary:        proc-macro library for serde_with

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/serde_with_macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
proc-macro library for serde_with}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+schemars_0_8-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+schemars_0_8-devel %{_description}

This package contains library source intended for building other packages which
use the "schemars_0_8" feature of the "%{crate}" crate.

%files       -n %{name}+schemars_0_8-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+schemars_0_9-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+schemars_0_9-devel %{_description}

This package contains library source intended for building other packages which
use the "schemars_0_9" feature of the "%{crate}" crate.

%files       -n %{name}+schemars_0_9-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+schemars_1-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+schemars_1-devel %{_description}

This package contains library source intended for building other packages which
use the "schemars_1" feature of the "%{crate}" crate.

%files       -n %{name}+schemars_1-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
