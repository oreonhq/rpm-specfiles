%global source0_hash 265385731074b619ea9e08275ba10ebb2a3114677c9a63fb352427696c020616

# Generated by go2rpm
%bcond check 0

# https://github.com/j-keck/arping
%global goipath         github.com/j-keck/arping
Version:                1.0.2

%gometa

%global common_description %{expand:
Arping is a native Go library to ping a host per arp datagram, or query a host
mac address.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        17%{?dist}
Summary:        Native Go library to ping a host per arp datagram, or query a host mac address

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
# .: operation not permitted on some test
%gocheck -d .
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
