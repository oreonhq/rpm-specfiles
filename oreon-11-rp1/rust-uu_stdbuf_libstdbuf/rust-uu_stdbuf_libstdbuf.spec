%global source0_hash d21e77a5cf09f37a32d7be4d079f91a8d033ee130271ff9fd0b4c126d569b6a0

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate uu_stdbuf_libstdbuf

Name:           rust-uu_stdbuf_libstdbuf
Version:        0.7.0
Release:        %autorelease
Summary:        stdbuf/libstdbuf ~ (uutils); dynamic library required for stdbuf

License:        MIT
URL:            https://crates.io/crates/uu_stdbuf_libstdbuf
Source:         %{crates_source}
# * license file not linked: https://github.com/uutils/coreutils/pull/11417
Source10:       https://raw.githubusercontent.com/uutils/coreutils/refs/tags/%{version}/LICENSE
# Manually created patch for downstream crate metadata changes
# * change crate type from cdylib to lib so our tooling can process this
Patch:          uu_stdbuf_libstdbuf-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
stdbuf/libstdbuf ~ (uutils); dynamic library required for stdbuf.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -p %{SOURCE10} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
