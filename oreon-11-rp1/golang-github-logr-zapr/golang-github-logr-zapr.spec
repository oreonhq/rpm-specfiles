%global source0_hash d58c67d68321c0c9b49b11ad7a895acb1ea47c511e85e5513ade87aebd41dd3f

# Generated by go2rpm 1.8.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/go-logr/zapr
%global goipath         github.com/go-logr/zapr
Version:                1.2.4

%gometa

%global common_description %{expand:
A logr implementation using Zap.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        A logr implementation using Zap

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

BuildRequires:  golang(github.com/stretchr/testify)

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
