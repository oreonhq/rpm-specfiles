%global source0_hash 512c927e8c969132db5d60c1628e1bb241cc960785ead86ab94f15e64048c804

# Generated by go2rpm 1.9.0
%bcond check 0

# https://github.com/etcd-io/gofail
%global goipath         github.com/etcd-io/gofail
%global commit          ad7f989257ca7de90de65657c9748a3108b1460f

%gometa

%global common_description %{expand:
Failpoints for Go.}

%global golicenses      LICENSE NOTICE
%global godocs          examples README.md code-of-conduct.md

Name:           %{goname}
Version:        0
Release:        %autorelease -p
Summary:        Failpoints for Go

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%build
%gobuild -o %{gobuilddir}/bin/gofail %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE NOTICE
%doc examples README.md code-of-conduct.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
