%global source0_hash 260d3e9f774c1c138a5aaec7d0280feea0e8fdab8021218bb666974b08a8425c

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

# https://github.com/golang/oauth2
%global goipath         golang.org/x/oauth2
%global forgeurl        https://github.com/golang/oauth2
Version:                0.25.0

%gometa -L

%global common_description %{expand:
A client implementation for OAuth 2.0 spec.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

Name:           golang-x-oauth2
Release:        %autorelease
Summary:        Client implementation for OAuth 2.0 spec

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
