%global source0_hash 200dadedb81875c652bb439604d7ba4e25e88bd88640e4de708b1e0616d6992e

# Generated by go2rpm 1.6.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/zeebo/pcg
%global goipath         github.com/zeebo/pcg
Version:                1.0.1

%gometa

%global common_description %{expand:
PCG random number generator.}

%global golicenses      LICENSE
%global godocs          README.txt

Name:           %{goname}
Release:        %autorelease
Summary:        PCG random number generator

# Upstream license specification: CC0-1.0
# Automatically converted from old format: CC0 - review is highly recommended.
License:        CC0-1.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
