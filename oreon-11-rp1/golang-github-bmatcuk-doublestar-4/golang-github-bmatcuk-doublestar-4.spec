%global source0_hash 09dea26a4d3f1e2d4e6801fdc777eb5863957430d75255ec69d98b4c97ad44ed

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/bmatcuk/doublestar
%global goipath         github.com/bmatcuk/doublestar/v4
Version:                4.7.1

%gometa -f

%global common_description %{expand:
This package implements support for double star (**) matches in Golang's
path.Match and filepath.Glob.}

%global golicenses      LICENSE
%global godocs          examples README.md UPGRADING.md

Name:           golang-github-bmatcuk-doublestar-4
Release:        %autorelease
Summary:        Support for double star (**) matches in Go's path.Match and filepath.Glob

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
# The test/nopermission directory with 000 permissions is created during
# the TestMain test, which breaks the final rmbuild cleanup phase
chmod 755 test/nopermission
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
