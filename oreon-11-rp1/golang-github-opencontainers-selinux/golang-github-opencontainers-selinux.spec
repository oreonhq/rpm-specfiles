%global source0_hash 7b02d2132912765660329eaed4177dd8d84f1e1a4b17e92ac2ede25b294dd927

# Generated by go2rpm 1.8.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/opencontainers/selinux
%global goipath         github.com/opencontainers/selinux
Version:                1.10.1

%gometa

%global common_description %{expand:
Common SELinux package used across the container ecosystem.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

%global gosupfiles      ${selinux[@]}

Name:           %{goname}
Release:        %autorelease
Summary:        Common selinux implementation

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
mapfile -t selinux <<< $(find . -name "*.go" -type f)
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
