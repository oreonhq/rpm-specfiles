%global source0_hash ca9b1a63b9a7bec16e3bec40604556d585bfba6133e8293c1cd61bf1e2f40090

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/seborama/govcr
%global goipath         gopkg.in/seborama/govcr.v4
%global forgeurl        https://github.com/seborama/govcr
Version:                4.5.0

%gometa

%global goaltipaths     github.com/seborama/govcr

%global common_description %{expand:
HTTP mock for Golang: record and replay HTTP/HTTPS interactions for offline
testing.}

%global golicenses      LICENSE
%global godocs          examples README.md

Name:           %{goname}
Release:        %autorelease
Summary:        HTTP mock for Go: record and replay HTTP/HTTPS interactions for offline testing

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1
sed -i "s|github.com/seborama/govcr|gopkg.in/seborama/govcr.v4|" $(find . -iname "*.go" -type f)

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
rm -rfv govcr_example*_test.go
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
