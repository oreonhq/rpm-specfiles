%global source0_hash d14f7d57b0f6d25771bee3d7d5d038f532539e71bfc934772b1c0467ab90fa86

# Generated by go2rpm 1.10.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/google/go-cmp
%global goipath         github.com/google/go-cmp
Version:                0.6.0

%gometa -L

%global common_description %{expand:
This package is intended to be a more powerful and safer alternative
to reflect.DeepEqual for comparing whether two values are semantically
equal.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

Name:           golang-github-google-cmp
Release:        %autorelease
Summary:        Package for comparing Go values in tests

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
