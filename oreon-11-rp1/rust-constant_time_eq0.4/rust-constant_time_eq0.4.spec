%global source0_hash 3d52eff69cd5e647efe296129160853a42795992097e8af39800e1060caeea9b

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate constant_time_eq

Name:           rust-constant_time_eq0.4
Version:        0.4.2
Release:        %autorelease
Summary:        Compares two equal-sized byte strings in constant time.

License:        CC0-1.0 OR MIT-0 OR Apache-2.0
URL:            https://crates.io/crates/constant_time_eq
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Compares two equal-sized byte strings in constant time.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-CC0
%license %{crate_instdir}/LICENSE-MIT0
%doc %{crate_instdir}/README
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+count_instructions_test-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+count_instructions_test-devel %{_description}

This package contains library source intended for building other packages which
use the "count_instructions_test" feature of the "%{crate}" crate.

%files       -n %{name}+count_instructions_test-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
