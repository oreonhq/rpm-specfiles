%global source0_hash 4595346337e5a357f0322669f45d7f5f75f9c8636061c66edb72e1064597fc71

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate netmap_sys

Name:           rust-netmap_sys
Version:        0.1.4
Release:        %autorelease
Summary:        Bindings to netmap - the fast packet I/O framework

License:        BSD-2-Clause
URL:            https://crates.io/crates/netmap_sys
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Bindings to netmap - the fast packet I/O framework}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+netmap_with_libs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+netmap_with_libs-devel %{_description}

This package contains library source intended for building other packages which
use the "netmap_with_libs" feature of the "%{crate}" crate.

%files       -n %{name}+netmap_with_libs-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
