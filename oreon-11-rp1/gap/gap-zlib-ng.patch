--- configure.ac.orig	2024-06-13 06:28:34.627223700 -0600
+++ configure.ac	2024-09-04 15:10:03.103968083 -0600
@@ -615,7 +615,7 @@ BUILD_ZLIB=no
 ZLIB_SEARCH_PATH="DEFAULTS ${DEFAULT_SEARCH_PATH}"
 ZLIB_CPPFLAGS=
 ZLIB_LDFLAGS=
-ZLIB_LIBS="-lz"
+ZLIB_LIBS="-lz-ng"
 AS_CASE([$with_zlib],
   [builtin],[
     # user explicitly requested to use builtin zlib
@@ -652,8 +652,8 @@ for ZLIB_PREFIX in ${ZLIB_SEARCH_PATH} ;
   CFLAGS="${ZLIB_CPPFLAGS} ${save_CFLAGS}"
   LIBS="${ZLIB_LDFLAGS} ${ZLIB_LIBS} ${save_LIBS}"
   AC_LINK_IFELSE(
-    [AC_LANG_PROGRAM([[#include <zlib.h>]],
-              [[z_stream a; inflateEnd(&a);]])],
+    [AC_LANG_PROGRAM([[#include <zlib-ng.h>]],
+              [[zng_stream a; zng_inflateEnd(&a);]])],
     [zlib_found=yes
      AS_IF([test x"$ZLIB_PREFIX" != x""],[
        AC_MSG_RESULT([yes, at prefix ${ZLIB_PREFIX}])
--- src/sysfiles.c.orig	2024-06-12 13:59:55.000000000 -0600
+++ src/sysfiles.c	2024-09-04 15:05:58.323853784 -0600
@@ -79,7 +79,8 @@
 #include <readline/readline.h>
 #endif
 
-#include <zlib.h>
+#define WITH_GZFILEOP
+#include <zlib-ng.h>
 
 #include <sys/utsname.h>
 
@@ -159,7 +160,7 @@ static ssize_t echoandcheck(int fid, con
 {
   int ret;
   if (syBuf[fid].type == gzip_socket) {
-      ret = gzwrite(syBuf[fid].gzfp, buf, count);
+      ret = zng_gzwrite(syBuf[fid].gzfp, buf, count);
       if (ret < 0) {
           ErrorQuit(
               "Could not write to compressed file, see 'LastSystemError();'\n",
@@ -704,7 +705,7 @@ Int SyFopen(const Char * name, const Cha
 
     // try to open the file
     if (endsgz && transparent_compress &&
-        (syBuf[fid].gzfp = gzopen(name, mode))) {
+        (syBuf[fid].gzfp = zng_gzopen(name, mode))) {
         syBuf[fid].type = gzip_socket;
         syBuf[fid].fp = -1;
         syBuf[fid].bufno = -1;
@@ -716,7 +717,7 @@ Int SyFopen(const Char * name, const Cha
     }
     else if (*mode == 'r' && transparent_compress &&
              SyIsReadableFile(namegz) == 0 &&
-             (syBuf[fid].gzfp = gzopen(namegz, mode))) {
+             (syBuf[fid].gzfp = zng_gzopen(namegz, mode))) {
         syBuf[fid].type = gzip_socket;
         syBuf[fid].fp = -1;
         syBuf[fid].bufno = -1;
@@ -797,7 +798,7 @@ Int SyFclose (
     }
 
     if (syBuf[fid].type == gzip_socket) {
-        if (gzclose(syBuf[fid].gzfp) < 0) {
+        if (zng_gzclose(syBuf[fid].gzfp) < 0) {
             fputs("gap: 'SyFclose' cannot close compressed file", stderr);
         }
     }
@@ -1260,7 +1261,7 @@ Int SyFtell (
         ret = (Int)lseek(syBuf[fid].fp, 0, SEEK_CUR);
         break;
     case gzip_socket:
-        ret = (Int)gzseek(syBuf[fid].gzfp, 0, SEEK_CUR);
+        ret = (Int)zng_gzseek(syBuf[fid].gzfp, 0, SEEK_CUR);
         break;
     case unused_socket:
     default:
@@ -1299,7 +1300,7 @@ Int SyFseek (
     case raw_socket:
         return (Int)lseek(syBuf[fid].fp, pos, SEEK_SET);
     case gzip_socket:
-        return (Int)gzseek(syBuf[fid].gzfp, pos, SEEK_SET);
+        return (Int)zng_gzseek(syBuf[fid].gzfp, pos, SEEK_SET);
     case unused_socket:
     default:
         return -1;
@@ -1349,7 +1350,7 @@ Int SyRead(Int fid, void * ptr, size_t l
     }
 
     if (syBuf[fid].type == gzip_socket) {
-        return gzread(syBuf[fid].gzfp, ptr, len);
+        return zng_gzread(syBuf[fid].gzfp, ptr, len);
     }
     else {
         return read(syBuf[fid].fp, ptr, len);
@@ -1389,7 +1390,7 @@ Int SyWrite(Int fid, const void * ptr, s
     }
 
     if (syBuf[fid].type == gzip_socket) {
-        return gzwrite(syBuf[fid].gzfp, ptr, len);
+        return zng_gzwrite(syBuf[fid].gzfp, ptr, len);
     }
     else {
         return write(syBuf[fid].echo, ptr, len);
