%global source0_hash cd849112e67cbc7d84d5126a596beb40807d362774b0ba9a97d9787e007566f8

# Generated by go2rpm
%bcond check 0
%bcond_with bootstrap
%global debug_package %{nil}

# https://github.com/coreos/pkg
%global goipath         github.com/coreos/pkg
Version:                4

%gometa

%global goipaths0       github.com/coreos/pkg
%global goipathsex0     github.com/coreos/pkg/capnslog

%if %{without bootstrap}
%global goipaths1       github.com/coreos/pkg/capnslog
%endif

%global common_description %{expand:
A collection of Go utility packages.}

%global golicenses      LICENSE NOTICE
%global godocs          CONTRIBUTING.md README.md code-of-conduct.md example README-*.md

Name:           %{goname}
Release:        %autorelease
Summary:        A collection of Go utility packages

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%if %{without bootstrap}
BuildRequires:  golang(github.com/coreos/go-systemd/journal)
%endif
BuildRequires:  golang(golang.org/x/crypto/ssh/terminal)
BuildRequires:  golang(gopkg.in/yaml.v1)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
for d in capnslog health httputil; do
mv $d/README.md README-$d.md
done

%install
%gopkginstall

%if %{with check}
%check
# https://github.com/coreos/pkg/issues/76
for test in "TestCopyOne" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%if %{with bootstrap}
%gocheck -d capnslog
%else
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
