%global source0_hash e89e09a27118e180152ec2837471ad0badffaae6cf3588c1dcee20d935774644

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/Masterminds/vcs
%global goipath         github.com/Masterminds/vcs
Version:                1.13.1

%gometa

%global common_description %{expand:
Package Vcs provides the ability to work with varying version control systems
(VCS), also known as source control systems (SCM) though the same interface.}

%global golicenses      LICENSE.txt
%global godocs          CHANGELOG.md README.md

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Release:        %autorelease
Summary:        VCS Repo management through a common interface in Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

BuildRequires:  bzr

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .

%install
%gopkginstall

%if %{with check}
%check
# .: needs network
%gocheck -d .
%endif

%gopkgfiles

%changelog
%autochangelog
