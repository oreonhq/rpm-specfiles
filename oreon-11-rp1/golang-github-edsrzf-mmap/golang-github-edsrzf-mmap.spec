%global source0_hash 49c502c28de1e36f12005a70543ae62e15dcd85565572df4e973beb5812587bf

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/edsrzf/mmap-go
%global goipath         github.com/edsrzf/mmap-go
Version:                1.0.0

%gometa

%global common_description %{expand:
Mmap-go is a portable mmap package for the Go programming language. It has been
tested on Linux (386, amd64), OS X, and Windows (386). It should also work on
other Unix-like platforms, but hasn't been tested with them. I'm interested to
hear about the results.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Portable mmap package for Go

# Upstream license specification: BSD-3-Clause
# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(golang.org/x/sys/unix)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
