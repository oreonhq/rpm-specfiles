%global source0_hash dac4bce864c211474521a9b691bcd2329dab173bf6af4aacf0a3c9535e56707d

# Generated by go2rpm 1
# Needs network access
%bcond check 1
%global debug_package %{nil}

# https://github.com/schollz/progressbar/v3
%global goipath         github.com/schollz/progressbar/v3
%global forgeurl        https://github.com/schollz/progressbar
Version:                3.8.3

%gometa

%global common_description %{expand:
A very simple thread-safe progress bar which should work on every OS.}

%global golicenses      LICENSE
%global godocs          examples README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Really basic thread-safe progress bar for Golang applications

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/k0kubun/go-ansi)
BuildRequires:  golang(github.com/mattn/go-runewidth)
BuildRequires:  golang(github.com/mitchellh/colorstring)
BuildRequires:  golang(golang.org/x/crypto/ssh/terminal)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
