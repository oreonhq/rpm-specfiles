--- src/hitch.c.orig	2023-08-09 14:29:31.000000000 +0200
+++ src/hitch.c	2025-07-29 17:30:49.852744295 +0200
@@ -124,6 +124,8 @@
 #  define X509_NAME_ENTRY_get_data(entry) ((entry)->value)
 #endif
 
+# define ENGINE_METHOD_ALL               (unsigned int)0xFFFF
+
 /* logging.c */
 extern FILE *logfile;
 extern struct stat logf_st;
@@ -1328,6 +1330,16 @@
 void
 init_openssl(void)
 {
+       /* These are grabbed from openssl/engine.h, which is lacking from el10 */
+       extern const char *ENGINE_get_id(const ENGINE *e);
+       extern void ENGINE_load_builtin_engines(void);
+       extern int  ENGINE_register_all_complete(void);
+       extern int  ENGINE_init(ENGINE *e);
+       extern int  ENGINE_set_default(ENGINE *e, unsigned int flags);
+       extern int  ENGINE_finish(ENGINE *e);
+       extern int  ENGINE_free(ENGINE *e);
+       extern ENGINE *ENGINE_by_id(const char *id);
+
 	SSL_library_init();
 	SSL_load_error_strings();
 	OpenSSL_add_all_digests();
