%global source0_hash 363140ddedf035ef012e0b17a35d32210f51b7f160c3fb0e89d08aa83d1870e0

# Generated by go2rpm 1.9.0
# Test don't pass anymore, keep around for github.com/lestrrat-go/apache-logformat
%bcond check 1
%global debug_package %{nil}

# https://github.com/facebookgo/clock
%global goipath         github.com/facebookgo/clock
%global commit          600d898af40aa09a7a93ecb9265d87b0504b6f03

%gometa

%global common_description %{expand:
Clock is a small library for mocking time in Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        %autorelease -p
Summary:        Clock is a small library for mocking time in Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
