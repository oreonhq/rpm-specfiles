%global source0_hash 6d910bedd62c24733263d0bed247460853c9d22e8956bd4cd964302095e04e90

# Generated by rust2rpm 25
%bcond check 0
%global debug_package %{nil}

%global crate cedarwood

Name:           rust-cedarwood
Version:        0.4.6
Release:        %autorelease
Summary:        Efficiently-updatable double-array trie in Rust (ported from cedar)

License:        BSD-2-Clause
URL:            https://crates.io/crates/cedarwood
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
Patch:          cedarwood-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Efficiently-updatable double-array trie in Rust (ported from cedar).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+reduced-trie-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+reduced-trie-devel %{_description}

This package contains library source intended for building other packages which
use the "reduced-trie" feature of the "%{crate}" crate.

%files       -n %{name}+reduced-trie-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
