--- a/meson.build
+++ b/meson.build
@@ -187,7 +187,7 @@ if libxml2_dep.found()
   cdata.set('HAVE_LIBXML2', 1)
 endif
 
-libudfread_dep = dependency('libudfread', version: '>= 1.2.0', required: false)
+libudfread_dep = dependency('libudfread', version: '>= 1.1.0', required: false)
 if libudfread_dep.found()
   cdata.set('HAVE_LIBUDFREAD', 1)
 endif
