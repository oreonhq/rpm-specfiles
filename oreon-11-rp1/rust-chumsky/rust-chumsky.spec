%global source0_hash 8eebd66744a15ded14960ab4ccdbfb51ad3b81f51f3f04a80adac98c985396c9

# Generated by rust2rpm 25
# * missing dev-dependency: ariadne ^0.1.2
%bcond check 1
%global debug_package %{nil}

%global crate chumsky

Name:           rust-chumsky
Version:        0.9.3
Release:        %autorelease
Summary:        Parser library for humans with powerful error recovery

License:        MIT
URL:            https://crates.io/crates/chumsky
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A parser library for humans with powerful error recovery.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/tutorial.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ahash-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ahash-devel %{_description}

This package contains library source intended for building other packages which
use the "ahash" feature of the "%{crate}" crate.

%files       -n %{name}+ahash-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nightly-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nightly-devel %{_description}

This package contains library source intended for building other packages which
use the "nightly" feature of the "%{crate}" crate.

%files       -n %{name}+nightly-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+spill-stack-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+spill-stack-devel %{_description}

This package contains library source intended for building other packages which
use the "spill-stack" feature of the "%{crate}" crate.

%files       -n %{name}+spill-stack-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+stacker-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+stacker-devel %{_description}

This package contains library source intended for building other packages which
use the "stacker" feature of the "%{crate}" crate.

%files       -n %{name}+stacker-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
