%global source0_hash eaab0af7bc43f0393030b4a77e167ef88efeda8eed7484f870ef5bb47d6378cf

# Generated by go2rpm 1.3
%bcond check 0

# https://github.com/gorhill/cronexpr
%global goipath         github.com/gorhill/cronexpr
Version:                1.0.0
%global tag             1.0.0

%gometa

%global common_description %{expand:
Given a cron expression and a time stamp, you can get the next time stamp which
satisfies the cron expression.

In another project, I decided to use cron expression syntax to encode scheduling
information. Thus this standalone library to parse and apply time stamps to cron
expressions.

The time-matching algorithm in this implementation is efficient, it avoids as
much as possible to guess the next matching time stamp, a common technique seen
in a number of implementations out there.}

%global godocs          README.md cronexpr/README.md

Name:           %{goname}
Release:        19%{?dist}
Summary:        Cron expression parser in Go language

# Automatically converted from old format: GPLv3 or ASL 2.0 - review is highly recommended.
License:        GPL-3.0-only OR Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cronexpr; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%doc README.md cronexpr/README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
