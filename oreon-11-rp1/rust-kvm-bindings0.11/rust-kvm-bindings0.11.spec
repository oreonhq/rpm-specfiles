%global source0_hash 3b13baf7bdfda2e10bcb109fcb099ef40cff82374eb6b7cdcf4695bdec4e522c

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate kvm-bindings

# compile and run tests only on supported architectures
%global supported_arches aarch64 riscv64 x86_64

Name:           rust-kvm-bindings0.11
Version:        0.11.1
Release:        %autorelease
Summary:        Rust FFI bindings to KVM generated using bindgen

License:        Apache-2.0
URL:            https://crates.io/crates/kvm-bindings
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Rust FFI bindings to KVM generated using bindgen.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fam-wrappers-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fam-wrappers-devel %{_description}

This package contains library source intended for building other packages which
use the "fam-wrappers" feature of the "%{crate}" crate.

%files       -n %{name}+fam-wrappers-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+vmm-sys-util-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vmm-sys-util-devel %{_description}

This package contains library source intended for building other packages which
use the "vmm-sys-util" feature of the "%{crate}" crate.

%files       -n %{name}+vmm-sys-util-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%ifarch %{supported_arches}
%cargo_build
%endif

%install
%cargo_install

%if %{with check}
%ifarch %{supported_arches}
%check
%cargo_test
%endif
%endif

%changelog
%autochangelog
