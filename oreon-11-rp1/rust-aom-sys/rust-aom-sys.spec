%global source0_hash 623675d7dbe1d65db81582231b0971384277a4dd2006763021ba2436e86f812d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate aom-sys

Name:           rust-aom-sys
Version:        0.3.3
Release:        %autorelease
Summary:        FFI bindings to aom

License:        MIT
URL:            https://crates.io/crates/aom-sys
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
FFI bindings to aom}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+build_sources-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+build_sources-devel %{_description}

This package contains library source intended for building other packages which
use the "build_sources" feature of the "%{crate}" crate.

%files       -n %{name}+build_sources-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
