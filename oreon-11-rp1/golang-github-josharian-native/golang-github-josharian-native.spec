%global source0_hash 0edb42adb5f7405d33920e21acba412b00bf9a8fbe97075924fd57c58ca6cad1

# Generated by go2rpm 1.8.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/josharian/native
%global goipath         github.com/josharian/native
Version:                1.0.0

%gometa -f

%global common_description %{expand:
Easy to use native encoding/binary.ByteOrder.}

%global golicenses      license
%global godocs          readme.md

Name:           %{goname}
Release:        %autorelease
Summary:        Easy to use native encoding/binary.ByteOrder

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
