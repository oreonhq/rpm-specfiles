%global source0_hash f7c44f8e672c00fe5308fa235f821cb4198414e1c77935c1ab6948d3fd78550e

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate mime_guess

Name:           rust-mime_guess
Version:        2.0.5
Release:        %autorelease
Summary:        A simple crate for detection of a file's MIME type by its extension.

License:        MIT
URL:            https://crates.io/crates/mime_guess
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A simple crate for detection of a file's MIME type by its extension.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rev-mappings-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rev-mappings-devel %{_description}

This package contains library source intended for building other packages which
use the "rev-mappings" feature of the "%{crate}" crate.

%files       -n %{name}+rev-mappings-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
