%global source0_hash 37832ba820e47c93d66b4360198dccb004b43c74abc3ac1ce1fed54e65a80445

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate matrixcompare

Name:           rust-matrixcompare
Version:        0.3.0
Release:        %autorelease
Summary:        Tools for convenient comparison of matrices

License:        MIT
URL:            https://crates.io/crates/matrixcompare
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Update pretty_assertions dev-dependency to 1.4:
#   https://github.com/Andlon/matrixcompare/pull/11
# * Update proptest dev-dependencies to 1.0:
#   https://github.com/Andlon/matrixcompare/pull/12
Patch:          matrixcompare-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Tools for convenient comparison of matrices.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+proptest-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+proptest-devel %{_description}

This package contains library source intended for building other packages which
use the "proptest" feature of the "%{crate}" crate.

%files       -n %{name}+proptest-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+proptest-support-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+proptest-support-devel %{_description}

This package contains library source intended for building other packages which
use the "proptest-support" feature of the "%{crate}" crate.

%files       -n %{name}+proptest-support-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
