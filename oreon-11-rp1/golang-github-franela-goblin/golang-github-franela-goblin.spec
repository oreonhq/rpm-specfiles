%global source0_hash 7b31c9a5c92484ed9c913c0ca6fa30a0b80c1b8cd10886140e60717e210b2c5f

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/franela/goblin
%global goipath         github.com/franela/goblin
Version:                0.0.3
%global tag             0.0.3

%gometa

%global common_description %{expand:
A Mocha like BDD testing framework written in Go that requires no additional
dependencies. Requires no extensive documentation nor complicated steps to get
it running.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Minimal and beautiful Go testing framework

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
