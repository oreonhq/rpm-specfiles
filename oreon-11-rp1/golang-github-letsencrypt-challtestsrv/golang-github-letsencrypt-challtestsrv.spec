%global source0_hash 3ca72dc4c3558cd9ccf78d7e9314cc00f3230b4799c22f0e54bae924de73a317

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/letsencrypt/challtestsrv
%global goipath         github.com/letsencrypt/challtestsrv
Version:                1.3.3

%gometa -L -f

%global common_description %{expand:
Small TEST-ONLY server for mock DNS & responding to HTTP-01, DNS-01, and TLS-
ALPN-01 ACME challenges.}

%global golicenses      LICENSE
%global godocs          CODE_OF_CONDUCT.md README.md

Name:           golang-github-letsencrypt-challtestsrv
Release:        %autorelease
Summary:        Small TEST-ONLY server for mock ACME challenges

License:        MPL-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
