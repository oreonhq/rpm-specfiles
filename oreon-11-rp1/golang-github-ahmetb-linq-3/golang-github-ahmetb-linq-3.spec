%global source0_hash d8c77953bccf97b5983811e1bc79a528724307cb0c2509339f8da0b4af865888

# Generated by go2rpm 1.8.2
%bcond check 0
%global debug_package %{nil}

# https://github.com/ahmetb/go-linq
%global goipath         github.com/ahmetb/go-linq/v3
Version:                3.2.0

%gometa -f

%global common_description %{expand:
.NET LINQ capabilities in Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        .NET LINQ capabilities in Go

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
rm -rfv example_test.go
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
