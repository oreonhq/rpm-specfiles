%global source0_hash a26640b2e38f1f1aa1d209f3024239f5704a9f928a43e677baa80a8107d8249a

# Generated by go2rpm 1.11.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/go-openapi/validate
%global goipath         github.com/go-openapi/validate
Version:                0.24.0

%gometa -L

%global common_description %{expand:
Openapi toolkit validation helpers.}

%global golicenses      LICENSE
%global godocs          CODE_OF_CONDUCT.md README.md example

Name:           golang-github-openapi-validate
Release:        %autorelease
Summary:        Openapi toolkit validation helpers

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
for test in "ExampleSpec_second" "ExampleSpecValidator_Validate_url" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
rm -rf doc_test.go
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
