%global source0_hash 835f8504143f94493019afc150381b8013d9daca3fb41f71ee758d6c59f7f975

%global sha 96e6cb5ae90726883be095a3e467e847c06225f7

Name:           evtest
Version:        1.36
Release:        2%{?dist}
Summary:        Event device test program

License:        GPL-2.0-or-later
URL:            http://gitlab.freedesktop.org/libevdev/evtest/
Source0:        http://gitlab.freedesktop.org/libevdev/evtest/-/archive/%{name}-%{version}/%{name}-%{version}.tar.bz2

BuildRequires:  automake gcc make
BuildRequires:  asciidoc xmlto libxml2-devel

%description
%{name} is a simple utility to query information about input devices
and watch the event stream generated by input devices.

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{name}-%{name}-%{version}-%{sha}

%build
autoreconf -v --install --force || exit 1
%configure
make %{?_smp_mflags}

%install
rm -rf %{buildroot}
make install DESTDIR=%{buildroot}

%files
%{_bindir}/%{name}
%doc COPYING
%{_mandir}/man1/evtest.1*

%changelog
%autochangelog
