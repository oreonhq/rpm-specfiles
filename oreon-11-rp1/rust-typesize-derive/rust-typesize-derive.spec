%global source0_hash 536b6812192bda8551cfa0e52524e328c6a951b48e66529ee4522d6c721243d6

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate typesize-derive

Name:           rust-typesize-derive
Version:        0.1.11
Release:        %autorelease
Summary:        Internal proc-macro crate for typesize

License:        MIT
URL:            https://crates.io/crates/typesize-derive
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Internal proc-macro crate for typesize}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+details-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+details-devel %{_description}

This package contains library source intended for building other packages which
use the "details" feature of the "%{crate}" crate.

%files       -n %{name}+details-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
