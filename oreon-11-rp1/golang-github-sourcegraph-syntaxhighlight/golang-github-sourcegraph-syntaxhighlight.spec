%global source0_hash 241438e901b72b7272439e225e5be750bd2faa39b43f31212cce1a228778d9dd

# Generated by go2rpm
%bcond check 0

# https://github.com/sourcegraph/syntaxhighlight
%global goipath         github.com/sourcegraph/syntaxhighlight
%global commit          bd320f5d308e1a3c4314c678d8227a0d72574ae7

%gometa

%global common_description %{expand:
Syntaxhighlight provides syntax highlighting for code. It currently uses a
language-independent lexer and performs decently on JavaScript, Java, Ruby,
Python, Go, and C.}

%global golicenses      LICENSE
%global godocs          README.md benchmark.txt

Name:           %{goname}
Version:        0
Release:        0.26%{?dist}
Summary:        Go package for syntax highlighting of code

# Upstream license specification: BSD-3-Clause
# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/sourcegraph/annotate)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/kr/pretty)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md benchmark.txt
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
