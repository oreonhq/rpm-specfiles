%global source0_hash 7f4d7a0c34b924081f7fbdcb93121399f0d4182c0ec4bf9c01bfe4e7e21fcf16

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate selinux

Name:           rust-selinux
Version:        0.6.0
Release:        %autorelease
Summary:        Flexible Mandatory Access Control for Linux

License:        MIT
URL:            https://crates.io/crates/selinux
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Flexible Mandatory Access Control for Linux.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * test fixtures not shipped
%{cargo_test -- -- %{shrink:
    --skip avc::tests::access_vector_cache_
    --skip label::tests::labeler_
    --skip policy::tests::set_root_path
    --skip tests::se_user_and_level
    --skip tests::security_class_new
    --skip tests::security_context_
}}
%endif

%changelog
%autochangelog
