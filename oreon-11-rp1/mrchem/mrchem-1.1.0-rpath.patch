diff -up mrchem-1.1.0/src/CMakeLists.txt.rpath mrchem-1.1.0/src/CMakeLists.txt
--- mrchem-1.1.0/src/CMakeLists.txt.rpath	2022-09-28 08:33:07.342509803 +0000
+++ mrchem-1.1.0/src/CMakeLists.txt	2022-09-28 08:33:40.675910907 +0000
@@ -73,24 +73,6 @@ target_link_libraries(mrchem.x
     mrchem
   )
 
-# RPATH fixing
-file(RELATIVE_PATH _rel ${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_BINDIR} ${CMAKE_INSTALL_PREFIX})
-if(APPLE)
-  set(_rpath "@loader_path/${_rel}")
-else()
-  set(_rpath "\$ORIGIN/${_rel}")
-endif()
-file(TO_NATIVE_PATH "${_rpath}/${CMAKE_INSTALL_LIBDIR}" MRChem_RPATH)
-
-set_target_properties(mrchem.x
-  PROPERTIES
-    MACOSX_RPATH ON
-    SKIP_BUILD_RPATH OFF
-    BUILD_WITH_INSTALL_RPATH OFF
-    INSTALL_RPATH "${MRChem_RPATH}"
-    INSTALL_RPATH_USE_LINK_PATH ON
-  )
-
 install(
   TARGETS
     mrchem.x
