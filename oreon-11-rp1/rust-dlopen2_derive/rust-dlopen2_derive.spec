%global source0_hash f2b99bf03862d7f545ebc28ddd33a665b50865f4dfd84031a393823879bd4c54

# Generated by rust2rpm 26
%bcond check 0
%global debug_package %{nil}

%global crate dlopen2_derive

Name:           rust-dlopen2_derive
Version:        0.4.0
Release:        %autorelease
Summary:        Derive macros for the dlopen2 crate

License:        MIT
URL:            https://crates.io/crates/dlopen2_derive
Source:         %{crates_source}
# https://github.com/OpenByteDev/dlopen2/issues/12
Source100:      https://raw.githubusercontent.com/OpenByteDev/dlopen2/master/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Derive macros for the dlopen2 crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE100} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
