%global source0_hash 77c90badedccf4105eca100756a0b1289e191f6fcbdadd3cee1d2f614f97da8f

# Generated by rust2rpm 26
%bcond check 0
%global debug_package %{nil}

%global crate dotenv

Name:           rust-dotenv
Version:        0.15.0
Release:        %autorelease
Summary:        Dotenv implementation for Rust

License:        MIT
URL:            https://crates.io/crates/dotenv
Source0:        %{crates_source}
# https://github.com/dotenv-rs/dotenv/issues/47
Source1:        https://github.com/dotenv-rs/dotenv/raw/v%{version}/LICENSE.md
# Manually created patch for downstream crate metadata changes
# * prevent "dotenv" binary from being built and shipped
# * remove reference to readme file that is not included in published crates
Patch:          dotenv-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A `dotenv` implementation for Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
