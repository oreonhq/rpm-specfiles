%global source0_hash 6184e33543162437515c2e2b48714794e37845ec9851711914eec9d308f6ebe8

# Generated by rust2rpm 24
%bcond check 0
%global debug_package %{nil}

%global crate difflib

Name:           rust-difflib
Version:        0.4.0
Release:        %autorelease
Summary:        Port of Python's difflib library to Rust

License:        MIT
URL:            https://crates.io/crates/difflib
Source0:        %{crates_source}
# https://github.com/DimaKudosh/difflib/pull/7
Source1:        https://github.com/DimaKudosh/difflib/raw/f035fb8/LICENSE
# Manually created patch for downstream crate metadata changes
# * include license test in packaged files
Patch:          difflib-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Port of Python's difflib library to Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
