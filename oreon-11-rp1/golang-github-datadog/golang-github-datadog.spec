%global source0_hash 4bb2905d10dd9c7b02bfcc591749efd21120f7bd38d5235281870d2fba2030ed

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/DataDog/datadog-go
%global goipath         github.com/DataDog/datadog-go
Version:                4.2.0

%gometa

%global common_description %{expand:
Datadog-go is a library that provides a dogstatsd client in Golang.}

%global golicenses      LICENSE.txt
%global godocs          CHANGELOG.md README.md README-statsd.md

%global gosupfiles glide.lock glide.yaml

Name:           %{goname}
Release:        %autorelease
Summary:        Go dogstatsd client library for datadog

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
BuildRequires:  golang(github.com/stretchr/testify/mock)
BuildRequires:  golang(github.com/stretchr/testify/require)
BuildRequires:  golang(github.com/stretchr/testify/suite)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .
mv statsd/README.md README-statsd.md

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
