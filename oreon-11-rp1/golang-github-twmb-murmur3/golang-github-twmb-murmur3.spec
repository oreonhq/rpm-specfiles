%global source0_hash e72a62bc9770e6bc1282cee9cbb2f6fe7213a239528d51d2fb9b0a4f190b7022

# Generated by go2rpm 1.3
%bcond check 0

%global debug_package %{nil}

# https://github.com/twmb/murmur3
%global goipath         github.com/twmb/murmur3
Version:                1.1.5

%gometa

%global common_description %{expand:
Fast, fully fledged murmur3 in Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Fast, fully fledged murmur3 in Go

# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD

URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
BuildRequires:  gcc-c++
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
