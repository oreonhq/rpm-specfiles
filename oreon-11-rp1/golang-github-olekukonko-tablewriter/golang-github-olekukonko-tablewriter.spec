%global source0_hash 14a1294a8267facc9bc99a230b8871517e6db284ccc7e39030313befa124677f

# Generated by go2rpm
%bcond check 0

# https://github.com/olekukonko/tablewriter
%global goipath         github.com/olekukonko/tablewriter
Version:                0.0.5

%gometa

%global common_description %{expand:
ASCII Table Writer.}

%global golicenses      LICENSE.md
%global godocs          README.md README-csv2table.md

Name:           %{goname}
Release:        %autorelease
Summary:        ASCII table in Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/mattn/go-runewidth)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
mv csv2table/README.md README-csv2table.md

%build
for cmd in csv2table; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license %{golicenses}
%doc %{godocs}
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
