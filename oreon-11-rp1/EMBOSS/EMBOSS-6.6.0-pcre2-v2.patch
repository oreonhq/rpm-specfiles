diff -up EMBOSS-6.6.0/ajax/core/ajreg.c.pcre2 EMBOSS-6.6.0/ajax/core/ajreg.c
--- EMBOSS-6.6.0/ajax/core/ajreg.c.pcre2	2011-10-18 10:23:40.000000000 -0400
+++ EMBOSS-6.6.0/ajax/core/ajreg.c	2023-12-20 15:13:31.973668449 -0500
@@ -84,19 +84,22 @@ AjPRegexp ajRegCompC(const char* rexp)
 {
     AjPRegexp ret;
     int options = 0;
-    int errpos  = 0;
-    const char *errptr            = NULL;
-    const unsigned char *tableptr = NULL;
+
+    int errornumber;
+    PCRE2_SIZE erroroffset;
+    pcre2_compile_context *tableptr = NULL;
 
     AJNEW0(ret);
     AJCNEW0(ret->ovector, AJREG_OVECSIZE);
     ret->ovecsize = AJREG_OVECSIZE/3;
-    ret->pcre = pcre_compile(rexp, options, &errptr, &errpos, tableptr);
+    ret->pcre = pcre2_compile((PCRE2_SPTR)rexp, PCRE2_ZERO_TERMINATED, options, &errornumber, &erroroffset, tableptr);
 
     if(!ret->pcre)
     {
-	ajErr("Failed to compile regular expression '%s' at position %d: %s",
-	      rexp, errpos, errptr);
+	PCRE2_UCHAR buffer[256];
+	pcre2_get_error_message(errornumber, buffer, sizeof(buffer));
+	ajErr("Failed to compile regular expression at offset %d: %s",
+	      (int)erroroffset, buffer);
 	AJFREE(ret);
 	return NULL;
     }
@@ -146,20 +149,22 @@ AjPRegexp ajRegCompCase(const AjPStr rex
 AjPRegexp ajRegCompCaseC(const char* rexp)
 {
     AjPRegexp ret;
-    int options = PCRE_CASELESS;
-    int errpos  = 0;
-    const char *errptr            = NULL;
-    const unsigned char *tableptr = NULL;
+    int options = PCRE2_CASELESS;
+    int errornumber;
+    PCRE2_SIZE erroroffset;
+    pcre2_compile_context *tableptr = NULL;
 
     AJNEW0(ret);
     AJCNEW0(ret->ovector, AJREG_OVECSIZE);
     ret->ovecsize = AJREG_OVECSIZE/3;
-    ret->pcre = pcre_compile(rexp, options, &errptr, &errpos, tableptr);
+    ret->pcre = pcre2_compile((PCRE2_SPTR)rexp, PCRE2_ZERO_TERMINATED, options, &errornumber, &erroroffset, tableptr);
 
     if(!ret->pcre)
     {
-	ajErr("Failed to compile regular expression '%s' at position %d: %s",
-	      rexp, errpos, errptr);
+        PCRE2_UCHAR buffer[256];
+        pcre2_get_error_message(errornumber, buffer, sizeof(buffer));
+        ajErr("Failed to compile regular expression at offset %d: %s",
+              (int)erroroffset, buffer);
 	AJFREE(ret);
 
 	return NULL;
@@ -204,9 +209,11 @@ AjBool ajRegExec(AjPRegexp prog, const A
     int options     = 0;
     int status      = 0;
 
-    status = pcre_exec(prog->pcre, prog->extra, ajStrGetPtr(str),
-                       ajStrGetLen(str), startoffset, options, prog->ovector,
-                       3*prog->ovecsize);
+    prog->extra = pcre2_match_data_create_from_pattern(prog->pcre, NULL);
+
+    status = pcre2_match(prog->pcre, (PCRE2_SPTR)ajStrGetPtr(str),
+                       ajStrGetLen(str), startoffset, options, prog->extra,
+                       NULL);
 
     if(status >= 0)
     {
@@ -215,6 +222,8 @@ AjBool ajRegExec(AjPRegexp prog, const A
 	if(status == 0)
 	    ajWarn("ajRegExec too many substrings");
 
+	prog->ovector = pcre2_get_ovector_pointer(prog->extra);
+
 	return ajTrue;
     }
 
@@ -223,6 +232,8 @@ AjBool ajRegExec(AjPRegexp prog, const A
 	ajDebug("ajRegExec returned unexpected status '%d'\n", status);
 	prog->orig = ajStrGetPtr(str);	/* needed for the trace */
 	ajRegTrace(prog);
+	/* pcre2_match_data_free(prog->extra);
+	pcre2_code_free(prog->pcre); */
     }
 
     prog->orig = NULL;
@@ -255,8 +266,10 @@ AjBool ajRegExecC(AjPRegexp prog, const
     int options     = 0;
     int status      = 0;
 
-    status = pcre_exec(prog->pcre, prog->extra, str, strlen(str),
-		       startoffset, options, prog->ovector, 3*prog->ovecsize);
+    prog->extra = pcre2_match_data_create_from_pattern(prog->pcre, NULL);
+
+    status = pcre2_match(prog->pcre, (PCRE2_SPTR)str, strlen(str),
+		       startoffset, options, prog->extra, NULL);
 
     if(status >= 0)
     {
@@ -265,6 +278,8 @@ AjBool ajRegExecC(AjPRegexp prog, const
 	if(status == 0)
 	    ajWarn("ajRegExecC too many substrings");
 
+	prog->ovector = pcre2_get_ovector_pointer(prog->extra);
+
 	return ajTrue;
     }
 
@@ -273,6 +288,8 @@ AjBool ajRegExecC(AjPRegexp prog, const
 	ajDebug("ajRegExecC returned unexpected status '%d'\n", status);
 	prog->orig = str;		/* needed for the trace */
 	ajRegTrace(prog);
+	/* pcre2_match_data_free(prog->extra);
+	pcre2_code_free(prog->pcre); */
     }
 
     prog->orig = NULL;
@@ -308,11 +325,13 @@ AjBool ajRegExecall(AjPRegexp prog, cons
 
     if(!regDfaWorkspace)
         AJCNEW(regDfaWorkspace, regDfaWsCount);
-        
-    prog->matches = pcre_dfa_exec(prog->pcre, prog->extra,
-                                  ajStrGetPtr(str), ajStrGetLen(str),
+
+    prog->extra = pcre2_match_data_create_from_pattern(prog->pcre, NULL);
+
+    prog->matches = pcre2_dfa_match(prog->pcre,
+                                  (PCRE2_SPTR)ajStrGetPtr(str), ajStrGetLen(str),
                                   startoffset, options,
-                                  prog->ovector, 3*prog->ovecsize,
+                                  prog->extra, NULL,
                                   regDfaWorkspace, regDfaWsCount);
 
     if(prog->matches >= 0)
@@ -322,6 +341,8 @@ AjBool ajRegExecall(AjPRegexp prog, cons
 	if(prog->matches == 0)
 	    ajWarn("ajRegExecall too many substrings");
 
+	prog->ovector = pcre2_get_ovector_pointer(prog->extra);
+
 	return ajTrue;
     }
 
@@ -331,6 +352,9 @@ AjBool ajRegExecall(AjPRegexp prog, cons
                 prog->matches);
 	prog->orig = ajStrGetPtr(str);	/* needed for the trace */
 	ajRegTrace(prog);
+
+	/* pcre2_match_data_free(prog->extra);
+	pcre2_code_free(prog->pcre); */
     }
 
     prog->orig = NULL;
@@ -366,11 +390,13 @@ AjBool ajRegExecallC(AjPRegexp prog, con
 
     if(!regDfaWorkspace)
         AJCNEW(regDfaWorkspace, regDfaWsCount);
-        
-    prog->matches = pcre_dfa_exec(prog->pcre, prog->extra,
-                                  str, strlen(str),
+
+    prog->extra = pcre2_match_data_create_from_pattern(prog->pcre, NULL);
+
+    prog->matches = pcre2_dfa_match(prog->pcre,
+                                  (PCRE2_SPTR)str, strlen(str),
                                   startoffset, options,
-                                  prog->ovector, 3*prog->ovecsize,
+                                  prog->extra, NULL,
                                   regDfaWorkspace, regDfaWsCount);
 
     if(prog->matches >= 0)
@@ -380,6 +406,8 @@ AjBool ajRegExecallC(AjPRegexp prog, con
 	if(prog->matches == 0)
 	    ajWarn("ajRegExecallC too many substrings");
 
+	prog->ovector = pcre2_get_ovector_pointer(prog->extra);
+
 	return ajTrue;
     }
 
@@ -389,6 +417,9 @@ AjBool ajRegExecallC(AjPRegexp prog, con
                 prog->matches);
 	prog->orig = str;		/* needed for the trace */
 	ajRegTrace(prog);
+
+	/* pcre2_match_data_free(prog->extra);
+	pcre2_code_free(prog->pcre); */
     }
 
     prog->orig = NULL;
@@ -712,7 +743,7 @@ void ajRegFree(AjPRegexp* pexp)
 
     AJFREE(exp->pcre);
     AJFREE(exp->extra);
-    AJFREE(exp->ovector);
+    /* AJFREE(exp->ovector); */
     AJFREE(*pexp);
 
     return;
diff -up EMBOSS-6.6.0/ajax/core/ajreg.h.pcre2 EMBOSS-6.6.0/ajax/core/ajreg.h
--- EMBOSS-6.6.0/ajax/core/ajreg.h.pcre2	2023-12-20 15:12:15.747640017 -0500
+++ EMBOSS-6.6.0/ajax/core/ajreg.h	2023-12-20 15:12:15.758640165 -0500
@@ -39,8 +39,12 @@
 #include "ajdefine.h"
 #include "ajstr.h"
 
-#include <pcre.h>
-#include <pcreposix.h>
+#include <ctype.h>
+#include <string.h>
+#define PCRE2_CODE_UNIT_WIDTH 8
+
+#include <pcre2.h>
+#include <pcre2posix.h>
 
 AJ_BEGIN_DECLS
 
@@ -73,8 +77,8 @@ AJ_BEGIN_DECLS
 ** @alias AjSRegexp
 ** @alias AjORegexp
 **
-** @attr pcre [real_pcre*] PCRE compiled expression
-** @attr extra [pcre_extra*] PCRE study data (if available, else NULL)
+** @attr pcre [pcre2_code*] PCRE compiled expression
+** @attr extra [pcre2_match_data*] PCRE study data (if available, else NULL)
 ** @attr ovector [int*] Output vector offsets
 ** @attr orig [const char*] Original string
 ** @attr ovecsize [int] Output vector size
@@ -83,9 +87,9 @@ AJ_BEGIN_DECLS
 
 typedef struct AjSRegexp
 {
-    struct real_pcre *pcre;
-    pcre_extra *extra;
-    int *ovector;
+    pcre2_code *pcre;
+    pcre2_match_data *extra;
+    PCRE2_SIZE *ovector;
     const char* orig;
     int ovecsize;
     int matches;
diff -up EMBOSS-6.6.0/configure.in.pcre2 EMBOSS-6.6.0/configure.in
--- EMBOSS-6.6.0/configure.in.pcre2	2023-12-20 15:12:15.750640057 -0500
+++ EMBOSS-6.6.0/configure.in	2023-12-20 15:12:15.758640165 -0500
@@ -809,7 +809,7 @@ AS_IF([test "x${enable_large}" = "xno"],
 AC_ARG_ENABLE([systemlibs],
 [AS_HELP_STRING([--enable-systemlibs], [utility for RPM/dpkg bundles])])
 if test "x${enable_systemlibs}" = "xyes" ; then
-   AC_CHECK_LIB(pcre, pcre_compile)
+   AC_CHECK_LIB(pcre2-8, pcre2_compile_8)
    AC_CHECK_LIB(expat, XML_ParserCreate)
    AC_CHECK_LIB(z, compress)
 fi
