%global source0_hash 6ce53fe368d6777b7aee47c2e2e0558d7089d08d23b311f4b84613ba08edc99b

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/jackc/puddle
%global goipath         github.com/jackc/puddle
Version:                1.1.3

%gometa

%global common_description %{expand:
Generic resource pool for Go.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Generic resource pool for Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
