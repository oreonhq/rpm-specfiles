%global source0_hash fb3d67f2982131c5e6047af4278d8fe750266767e57b58bc15f2e11e190eef36

# Generated by rust2rpm 26
%bcond check 0
%global debug_package %{nil}

%global crate relm4-components

Name:           rust-relm4-components
Version:        0.9.1
Release:        %autorelease
Summary:        Idiomatic GUI library inspired by Elm and based on gtk4-rs

License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/relm4-components
Source:         %{crates_source}
# https://github.com/Relm4/Relm4/issues/655
Source100:      https://raw.githubusercontent.com/Relm4/Relm4/main/LICENSE-APACHE
Source101:      https://raw.githubusercontent.com/Relm4/Relm4/main/LICENSE-MIT

# Manually created patch for downstream crate metadata changes
Patch:          relm4-components-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An idiomatic GUI library inspired by Elm and based on gtk4-rs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+libadwaita-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+libadwaita-devel %{_description}

This package contains library source intended for building other packages which
use the "libadwaita" feature of the "%{crate}" crate.

%files       -n %{name}+libadwaita-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+reqwest-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+reqwest-devel %{_description}

This package contains library source intended for building other packages which
use the "reqwest" feature of the "%{crate}" crate.

%files       -n %{name}+reqwest-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+web-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+web-devel %{_description}

This package contains library source intended for building other packages which
use the "web" feature of the "%{crate}" crate.

%files       -n %{name}+web-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE100} %{SOURCE101} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
