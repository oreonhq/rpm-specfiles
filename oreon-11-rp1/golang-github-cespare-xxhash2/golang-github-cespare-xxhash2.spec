%global source0_hash 0e3dda07b03a5f3733506218860ecb2d50c0f01f16299b5d60902ef5158cbde5

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%if %{with bootstrap}
%global debug_package %{nil}
%endif

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/cespare/xxhash
%global goipath         github.com/cespare/xxhash/v2
Version:                2.3.0

%gometa -L -f

%global common_description %{expand:
A Go implementation of the 64-bit xxHash algorithm (XXH64).}

%global golicenses      LICENSE.txt
%global godocs          README.md

Name:           golang-github-cespare-xxhash2
Release:        %autorelease
Summary:        A Go implementation of the 64-bit xxHash algorithm (XXH64)

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%if %{without bootstrap}
%build
%gobuild -o %{gobuilddir}/bin/goxxhsum %{goipath}/xxhsum
%endif

%install
%gopkginstall
%if %{without bootstrap}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/
%endif

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%if %{without bootstrap}
%files
%license LICENSE.txt
%doc README.md
%{_bindir}/goxxhsum
%endif

%gopkgfiles

%changelog
%autochangelog
