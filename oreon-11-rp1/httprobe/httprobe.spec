%global source0_hash 3e422617d8db3c343c2360cda6c1010bf00d615bf33857dee94429dc44a2594f

# Generated by go2rpm 1
%bcond check 0

# https://github.com/tomnomnom/httprobe
%global goipath         github.com/tomnomnom/httprobe
Version:                0.1.2

%gometa

%global common_description %{expand:
Take a list of domains and probe for working HTTP and HTTPS servers.}

%global godocs          README.md

Name:           httprobe
Release:        20%{?dist}
Summary:        Probing tool for working HTTP and HTTPS servers

License:        MIT

URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
%gobuild -o %{gobuilddir}/bin/httprobe %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
