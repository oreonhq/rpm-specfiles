%global source0_hash d8584d27c2bad03a07a9ed8190e3e0e5d66745a591fc51c30b6e0ac6669cc304

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/tidwall/gjson
%global goipath         github.com/tidwall/gjson
Version:                1.18.0

%gometa -L -f

%global common_description %{expand:
Get JSON values quickly - JSON parser for Go.}

%global golicenses      LICENSE
%global godocs          README.md SYNTAX.md

Name:           golang-github-tidwall-gjson
Release:        %autorelease
Summary:        Get JSON values quickly - JSON parser for Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
