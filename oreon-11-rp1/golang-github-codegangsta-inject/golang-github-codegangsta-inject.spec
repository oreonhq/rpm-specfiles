%global source0_hash 60c2983497f697b496d0ea0ff67acaa6ce11721b6da2493040ed8558c0d3b422

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/codegangsta/inject
%global goipath         github.com/codegangsta/inject
Version:                1.0.0
%global commit          33e0aa1cb7c019ccc3fbe049a8262a6403d30504

%gometa

%global common_description %{expand:
Package Inject provides utilities for mapping and injecting dependencies in
various ways.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Dependency injection for go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
