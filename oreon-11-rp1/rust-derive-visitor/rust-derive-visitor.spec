%global source0_hash d47165df83b9707cbada3216607a5d66125b6a66906de0bc1216c0669767ca9e

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate derive-visitor

Name:           rust-derive-visitor
Version:        0.4.0
Release:        %autorelease
Summary:        Derive visitor pattern for arbitrary data structures

License:        MIT
URL:            https://crates.io/crates/derive-visitor
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Derive visitor pattern for arbitrary data structures}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+std-types-drive-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-types-drive-devel %{_description}

This package contains library source intended for building other packages which
use the "std-types-drive" feature of the "%{crate}" crate.

%files       -n %{name}+std-types-drive-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
