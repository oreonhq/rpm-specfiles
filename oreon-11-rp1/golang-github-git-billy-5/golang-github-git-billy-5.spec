%global source0_hash d3be06b6dd74527741fa6102792386962ab4c68a897dae6a8db749a7c40ac24d

# Generated by go2rpm 1.11.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/go-git/go-billy
%global goipath         github.com/go-git/go-billy/v5
Version:                5.6.1

%gometa

%global common_description %{expand:
The missing interface filesystem abstraction for Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-git-billy-5
Release:        %autorelease
Summary:        The missing interface filesystem abstraction for Go

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
