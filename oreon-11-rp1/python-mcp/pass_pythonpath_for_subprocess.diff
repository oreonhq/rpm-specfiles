--- a/tests/issues/test_1027_win_unreachable_cleanup.py
+++ b/tests/issues/test_1027_win_unreachable_cleanup.py
@@ -89,7 +89,8 @@
 
     try:
         # Launch the MCP server
-        params = StdioServerParameters(command=sys.executable, args=[server_script])
+        import os
+        params = StdioServerParameters(command=sys.executable, env={"PYTHONPATH": os.getenv("PYTHONPATH")}, args=[server_script])
 
         async with stdio_client(params) as (read, write):
             async with ClientSession(read, write) as session:
