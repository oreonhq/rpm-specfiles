%global source0_hash 10b44fcd6f66e9dcc7c7315392232619a46cb4467ca9450b2b15bc384ffc722b

# Generated by rust2rpm 27
%bcond check 0

%global crate asahi-btsync

Name:           rust-asahi-btsync
Version:        0.2.4
Release:        %autorelease
Summary:        Tool to sync Bluetooth pairing keys with macos on ARM Macs

License:        MIT
URL:            https://crates.io/crates/asahi-btsync
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  systemd-rpm-macros

%global _description %{expand:
A tool to sync Bluetooth pairing keys with macos on ARM Macs.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
# Zlib
License:        MIT AND Zlib AND (Apache-2.0 OR MIT) AND (Unlicense OR MIT) AND Unicode-DFS-2016
# LICENSE.dependencies contains a full license breakdown
Recommends:     bluez

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%{_bindir}/asahi-btsync
%{_unitdir}/asahi-btsync.service
%{_udevrulesdir}/96-asahi-btsync.rules

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
install -p -m 0644 -D etc/systemd/system/asahi-btsync.service %{buildroot}%{_unitdir}/asahi-btsync.service
install -p -m 0644 -D etc/udev/rules.d/96-asahi-btsync.rules %{buildroot}%{_udevrulesdir}/96-asahi-btsync.rules

%if %{with check}
%check
%cargo_test
%endif

%post -n %{crate}
%systemd_post asahi-btsync.service

%preun -n %{crate}
%systemd_preun asahi-btsync.service

%postun -n %{crate}
%systemd_postun_with_restart asahi-btsync.service

%changelog
%autochangelog
