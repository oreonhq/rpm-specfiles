%global source0_hash a9b6d9f10c3aa329ae8df0ab175fa7552a02005f38f233b4bf24b7431e172b6d

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/uber-go/config
%global goipath         go.uber.org/config
%global forgeurl        https://github.com/uber-go/config
Version:                1.4.0

%gometa

%global common_description %{expand:
Configuration for Go applications.}

%global golicenses      LICENSE.txt
%global godocs          README.md CHANGELOG.md

Name:           %{goname}
Release:        %autorelease
Summary:        Configuration for Go applications

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(go.uber.org/multierr)
BuildRequires:  golang(golang.org/x/text/transform)
BuildRequires:  golang(gopkg.in/yaml.v2)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
BuildRequires:  golang(github.com/stretchr/testify/require)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
