%global source0_hash b8765b90061cba6c22b5831f675da109ae5561588290f9fa2317adab2714d5a6

# Generated by rust2rpm 28
# * checks can't run because the 'tests' directory is omitted from the crate:
#   https://github.com/mbrubeck/tree_magic/issues/17
%bcond check 0
%global debug_package %{nil}

%global crate tree_magic_mini

Name:           rust-tree_magic_mini
Version:        3.2.2
Release:        %autorelease
Summary:        Determines the MIME type of a file by traversing a filetype tree

License:        MIT
URL:            https://crates.io/crates/tree_magic_mini
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Determines the MIME type of a file by traversing a filetype tree.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+with-gpl-data-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+with-gpl-data-devel %{_description}

This package contains library source intended for building other packages which
use the "with-gpl-data" feature of the "%{crate}" crate.

%files       -n %{name}+with-gpl-data-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
