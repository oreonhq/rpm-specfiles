%global source0_hash da882b89e91da979a91f6cc32f54e5bdbb18c6b46280a032a2225e30a03390fd

# Generated by rust2rpm 24
# * missing dev-dependencies: byte-strings, byte-strings-proc_macros
%bcond check 1
%global debug_package %{nil}

%global crate pci-driver

Name:           rust-pci-driver
Version:        0.1.4
Release:        %autorelease
Summary:        PCI/PCIe driver development library

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/pci-driver
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
PCI/PCIe driver development library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+_unsafe-op-in-unsafe-fn-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+_unsafe-op-in-unsafe-fn-devel %{_description}

This package contains library source intended for building other packages which
use the "_unsafe-op-in-unsafe-fn" feature of the "%{crate}" crate.

%files       -n %{name}+_unsafe-op-in-unsafe-fn-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+libc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+libc-devel %{_description}

This package contains library source intended for building other packages which
use the "libc" feature of the "%{crate}" crate.

%files       -n %{name}+libc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+vfio-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vfio-devel %{_description}

This package contains library source intended for building other packages which
use the "vfio" feature of the "%{crate}" crate.

%files       -n %{name}+vfio-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
