%global source0_hash d6ab5630b3d5eaf232620167977f95eb51f3432fc76852328774afbd242d4358

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate askama_parser

Name:           rust-askama_parser
Version:        0.14.0
Release:        %autorelease
Summary:        Parser for Askama templates

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/askama_parser
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Omit benchmark-only criterion dev-dependency
# * Do not install benchmarks, which are unnecessary and contain bundled content
#   from librustdoc (albeit under the same SPDX license expression as the crate
#   itself)
Patch:          askama_parser-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Parser for Askama templates.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+config-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+config-devel %{_description}

This package contains library source intended for building other packages which
use the "config" feature of the "%{crate}" crate.

%files       -n %{name}+config-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
