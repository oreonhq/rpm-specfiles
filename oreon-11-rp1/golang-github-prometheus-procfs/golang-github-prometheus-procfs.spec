%global source0_hash ba96bb6d45f1fcbff820c7d844e6acdef4416bb0ff2d9e24656f5cbacb8e46b5

# Generated by go2rpm 1.15.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/prometheus/procfs
%global goipath         github.com/prometheus/procfs
Version:                0.15.1

%gometa -L -f

%global common_description %{expand:
Procfs provides functions to retrieve system, kernel and process metrics from
the pseudo-filesystem proc.}

%global golicenses      LICENSE NOTICE
%global godocs          CODE_OF_CONDUCT.md CONTRIBUTING.md MAINTAINERS.md\\\
                        README.md SECURITY.md

Name:           golang-github-prometheus-procfs
Release:        %autorelease
Summary:        Retrieve system, kernel and process metrics from proc

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
# Tests require that fixtures are extracted
./ttar -x -C testdata -f ./testdata/fixtures.ttar
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
