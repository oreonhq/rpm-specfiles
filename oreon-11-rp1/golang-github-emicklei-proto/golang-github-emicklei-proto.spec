%global source0_hash 9025f59443fd225e772358c4be5c3a95b0accf9b9c64214fdbfc51487c16d1b6

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/emicklei/proto
%global goipath         github.com/emicklei/proto
Version:                1.11.2

%gometa -f

%global common_description %{expand:
Parser for Google ProtocolBuffers definition.}

%global golicenses      LICENSE
%global godocs          CHANGES.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Parser for Google ProtocolBuffers definition

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
