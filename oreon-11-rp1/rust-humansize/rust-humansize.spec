%global source0_hash 6cb51c9a029ddc91b07a787f1d86b53ccfa49b0e86688c946ebe8d3555685dd7

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate humansize

Name:           rust-humansize
Version:        2.1.3
Release:        %autorelease
Summary:        A configurable crate to easily represent sizes in a human-readable fo...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/humansize
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A configurable crate to easily represent sizes in a human-readable format.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/changelog.md
%{crate_instdir}/

%package     -n %{name}+impl_style-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+impl_style-devel %{_description}

This package contains library source intended for building other packages which
use the "impl_style" feature of the "%{crate}" crate.

%files       -n %{name}+impl_style-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+no_alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+no_alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "no_alloc" feature of the "%{crate}" crate.

%files       -n %{name}+no_alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
