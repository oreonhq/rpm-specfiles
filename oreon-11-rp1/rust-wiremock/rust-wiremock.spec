%global source0_hash 08db1edfb05d9b3c1542e521aea074442088292f00b5f28e435c714a98f85031

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate wiremock

Name:           rust-wiremock
Version:        0.6.5
Release:        %autorelease
Summary:        HTTP mocking to test Rust applications

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/wiremock
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Omit deprecated async-std dev-dependency
# * Remove test modules for which *all* tests would require async-std.
# * Update deadpool to 0.13.0:
#   https://github.com/LukeMathWalker/wiremock-rs/pull/176
Patch:          wiremock-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
HTTP mocking to test Rust applications.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CODE_OF_CONDUCT.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * Nearly all "doc" and "tests" tests require async-std; running the few
#   exceptions does not seem worth the extensive patching effort that would be
#   required.
%cargo_test -- --lib
%endif

%changelog
%autochangelog
