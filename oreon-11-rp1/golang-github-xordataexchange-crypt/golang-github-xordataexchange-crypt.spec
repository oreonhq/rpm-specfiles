%global source0_hash 0a77ee274ae3bf264616ae99fc5bfeb47c89e74e2e2febdd5fcb61d9608a5180

# Generated by go2rpm
%bcond check 0

# https://github.com/xordataexchange/crypt
%global goipath         github.com/xordataexchange/crypt
Version:                0.0.2
%global commit          b2862e3d0a775f18c7cfe02273500ae307b61218

%gometa

%global common_description %{expand:
Store and retrieve encrypted configs from etcd or consul.}

%global golicenses      LICENSE
%global godocs          README.md README-crypt.md README-config.md

Name:           %{goname}
Release:        %autorelease
Summary:        Store and retrieve encrypted configs from etcd or consul

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/armon/consul-api)
BuildRequires:  golang(go.etcd.io/etcd/client/v2)
BuildRequires:  golang(golang.org/x/crypto/openpgp)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
sed -i "s|github.com/coreos/etcd/client|go.etcd.io/etcd/client/v2|" $(find . -iname "*.go" -type f)
mv bin/crypt/README.md README-crypt.md
mv config/README.md README-config.md

%build
for cmd in bin/crypt; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license %{golicenses}
%doc %{godocs}
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
