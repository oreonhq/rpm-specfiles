%global source0_hash a97512398a192253d9e9e164834be6469723b6b9502392c071a87978d4bb0b48

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate ovs-unixctl

Name:           rust-ovs-unixctl
Version:        0.1.0
Release:        %autorelease
Summary:        Control OVS daemons using the Unixctl interface (a.k.a ovs-appctl)

License:        LGPL-2.1-or-later
URL:            https://crates.io/crates/ovs-unixctl
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Control OVS daemons using the Unixctl interface (a.k.a ovs-appctl).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+test_integration-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+test_integration-devel %{_description}

This package contains library source intended for building other packages which
use the "test_integration" feature of the "%{crate}" crate.

%files       -n %{name}+test_integration-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
