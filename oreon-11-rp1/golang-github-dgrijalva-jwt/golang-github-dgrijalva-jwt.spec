%global source0_hash 197465ef53219f3aeb1a6940b70e16d288fe4e4108d4831b91ea101118440e63

# Generated by go2rpm
%bcond check 0

# https://github.com/dgrijalva/jwt-go
%global goipath         github.com/dgrijalva/jwt-go
Version:                3.2.0

%gometa

# Remove in F33:
%global godevelheader %{expand:
Obsoletes:      golang-github-dgrijalva-jwt-go-devel < 3.2.0-3
}

%global common_description %{expand:
Golang implementation of json web tokens (jwt).}

%global golicenses      LICENSE
%global godocs          MIGRATION_GUIDE.md VERSION_HISTORY.md README.md README-jwt.md

%global gosupfiles glide.lock glide.yaml

Name:           %{goname}
Release:        25%{?dist}
Summary:        Golang implementation of json web tokens (jwt)

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.lock
Source2:        glide.yaml

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .
mv cmd/jwt/README.md README-jwt.md

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license %{golicenses}
%doc %{godocs}
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
