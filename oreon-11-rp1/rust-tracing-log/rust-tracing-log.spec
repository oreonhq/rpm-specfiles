%global source0_hash ee855f1f400bd0e5c02d150ae5de3840039a3f54b025156404e34c23c03f47c3

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate tracing-log

Name:           rust-tracing-log
Version:        0.2.0
Release:        %autorelease
Summary:        Provides compatibility between `tracing` and the `log` crate.

License:        MIT
URL:            https://crates.io/crates/tracing-log
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Provides compatibility between `tracing` and the `log` crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ahash-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ahash-devel %{_description}

This package contains library source intended for building other packages which
use the "ahash" feature of the "%{crate}" crate.

%files       -n %{name}+ahash-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+interest-cache-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+interest-cache-devel %{_description}

This package contains library source intended for building other packages which
use the "interest-cache" feature of the "%{crate}" crate.

%files       -n %{name}+interest-cache-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+log-tracer-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+log-tracer-devel %{_description}

This package contains library source intended for building other packages which
use the "log-tracer" feature of the "%{crate}" crate.

%files       -n %{name}+log-tracer-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+lru-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+lru-devel %{_description}

This package contains library source intended for building other packages which
use the "lru" feature of the "%{crate}" crate.

%files       -n %{name}+lru-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
