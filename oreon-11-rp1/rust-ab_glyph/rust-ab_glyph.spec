%global source0_hash 01c0457472c38ea5bd1c3b5ada5e368271cb550be7a4ca4a0b4634e9913f6cc2

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate ab_glyph

Name:           rust-ab_glyph
Version:        0.2.32
Release:        %autorelease
Summary:        API for loading, scaling, positioning and rasterizing OpenType font glyphs

License:        Apache-2.0
URL:            https://crates.io/crates/ab_glyph
Source:         %{crates_source}
# * Sample font used as test data, included as an additional source as permitted
#   in
# * License is OFL-1.1 (use fonttools ttx to find it in the metadata)
Source10:       https://github.com/alexheretic/ab-glyph/raw/refs/tags/ab-glyph-%{version}/dev/fonts/Cantarell-VF.otf
# * Sample font used as test data, included as an additional source as permitted
#   in
# * License is OFL-1.1 (use fonttools ttx to find it in the metadata)
Source11:       https://github.com/alexheretic/ab-glyph/raw/refs/tags/ab-glyph-%{version}/dev/fonts/Exo2-Light.otf
# * Sample font used as test data, included as an additional source as permitted
#   in
# * License is Apache-2.0 (use fonttools ttx to find it in the metadata)
Source12:       https://github.com/alexheretic/ab-glyph/raw/refs/tags/ab-glyph-%{version}/dev/fonts/OpenSans-Italic.ttf

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
API for loading, scaling, positioning and rasterizing OpenType font
glyphs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gvar-alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gvar-alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "gvar-alloc" feature of the "%{crate}" crate.

%files       -n %{name}+gvar-alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+variable-fonts-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+variable-fonts-devel %{_description}

This package contains library source intended for building other packages which
use the "variable-fonts" feature of the "%{crate}" crate.

%files       -n %{name}+variable-fonts-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# Copy in sample fonts used as test data – required to run tests, but not
# included in the released crate. By doing so here in %%check rather than in
# %%prep, we prove that the binary RPMs are derived solely from the published
# crate sources.
install -D -t src/../../dev/fonts/ -m 0644 -p \
    '%{SOURCE10}' '%{SOURCE11}' '%{SOURCE12}'

%cargo_test
%endif

%changelog
%autochangelog
