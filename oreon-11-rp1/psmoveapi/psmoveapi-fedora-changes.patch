diff --git a/CMakeLists.txt b/CMakeLists.txt
index 0f43972..18cd005 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -2,18 +2,6 @@ cmake_minimum_required(VERSION 3.16)
 
 project(PSMoveAPI)
 
-if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/external/hidapi/hidapi/hidapi.h")
-    message(FATAL_ERROR "hidapi not found, did you forget to run 'git submodule update --init'?")
-endif()
-
-if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/external/libusb-1.0/libusb/libusb.h")
-    message(FATAL_ERROR "libusb not found, did you forget to run 'git submodule update --init'?")
-endif()
-
-if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/external/PS3EYEDriver/src/ps3eye.h")
-    message(FATAL_ERROR "PS3EYEDriver not found, did you forget to run 'git submodule update --init'?")
-endif()
-
 # get rid of Visual Studio's default "Debug" and "Release" output directories
 if(${CMAKE_SYSTEM_NAME} MATCHES "Windows")
     set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG   ${CMAKE_BINARY_DIR})
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 32f12d0..1665ead 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -15,8 +15,6 @@ set(PSMOVEAPI_PLATFORM_SRC)    # Container for source files
 set(PSMOVEAPI_INSTALL_TARGETS) # Container for build targets to be installed
 set(PSMOVEAPI_MATH_SRC)        # Container for math related source files
 
-include_directories(${ROOT_DIR}/external/hidapi/hidapi)
-include_directories(${ROOT_DIR}/external/glm)
 include_directories(${CMAKE_BINARY_DIR})
 
 set(INFO_LICENSE "BSD")
@@ -93,7 +91,8 @@ ELSE()
     include_directories(${BLUEZ_INCLUDE_DIRS})
     list(APPEND PSMOVEAPI_REQUIRED_LIBS ${BLUEZ_LIBRARIES})
 
-    set(HIDAPI_SRC ${ROOT_DIR}/external/hidapi/linux/hid.c)
+    pkg_check_modules(HIDAPI REQUIRED hidapi-hidraw)
+    list(APPEND PSMOVEAPI_REQUIRED_LIBS ${HIDAPI_LIBRARIES})
 
     list(APPEND PSMOVEAPI_PLATFORM_SRC
         ${CMAKE_CURRENT_LIST_DIR}/platform/psmove_port_linux.cpp)
@@ -141,7 +140,6 @@ source_group("Source Files\\math" FILES ${PSMOVEAPI_MATH_SRC})
 # Source files that are needed for both the shared and static library
 
 set(PSMOVEAPI_LIBRARY_SRC)
-list(APPEND PSMOVEAPI_LIBRARY_SRC ${HIDAPI_SRC})
 list(APPEND PSMOVEAPI_LIBRARY_SRC ${PSMOVEAPI_SRC})
 list(APPEND PSMOVEAPI_LIBRARY_SRC ${PSMOVEAPI_MOVED_SRC})
 list(APPEND PSMOVEAPI_LIBRARY_SRC ${PSMOVEAPI_MOVED_HEADER})
@@ -154,7 +152,8 @@ list(APPEND PSMOVEAPI_LIBRARY_SRC ${PSMOVEAPI_MATH_SRC})
 add_library(psmoveapi SHARED ${PSMOVEAPI_LIBRARY_SRC})
 target_link_libraries(psmoveapi ${PSMOVEAPI_REQUIRED_LIBS})
 set_target_properties(psmoveapi PROPERTIES
-    COMPILE_FLAGS -DBUILDING_SHARED_LIBRARY)
+    COMPILE_FLAGS -DBUILDING_SHARED_LIBRARY
+    SOVERSION 4.0.12)
 list(APPEND PSMOVEAPI_INSTALL_TARGETS psmoveapi)
 
 # For system-wide installation
@@ -184,7 +183,7 @@ endif()
 if (${CMAKE_SYSTEM_NAME} MATCHES "Linux")
     # Only install the udev configuration file on Linux
     install(FILES ${ROOT_DIR}/contrib/99-psmove.rules
-        DESTINATION /etc/udev/rules.d)
+        DESTINATION /usr/lib/udev/rules.d)
 endif()
 
 install(TARGETS ${PSMOVEAPI_INSTALL_TARGETS}
@@ -195,7 +194,6 @@ install(TARGETS ${PSMOVEAPI_INSTALL_TARGETS}
 
 install(FILES ${PSMOVEAPI_HEADERS} DESTINATION include/psmoveapi)
 install(FILES ${CMAKE_BINARY_DIR}/psmove_config.h DESTINATION include/psmoveapi)
-install(FILES README.md COPYING DESTINATION share/psmoveapi)
 
 message("")
 message("  Build configuration")
diff --git a/src/psmove.c b/src/psmove.c
index 268e1db..1504b60 100644
--- a/src/psmove.c
+++ b/src/psmove.c
@@ -47,7 +47,7 @@
 #include <limits.h>
 
 #include "daemon/moved_client.h"
-#include "hidapi.h"
+#include <hidapi/hidapi.h>
 
 /* Begin private definitions */
 
diff --git a/src/tracker/CMakeLists.txt b/src/tracker/CMakeLists.txt
index 013d9a0..15e85c7 100644
--- a/src/tracker/CMakeLists.txt
+++ b/src/tracker/CMakeLists.txt
@@ -155,7 +155,8 @@ if(PSMOVE_BUILD_TRACKER)
         ${PSMOVEAPI_REQUIRED_LIBS}
         ${PSMOVEAPI_TRACKER_REQUIRED_LIBS})
     set_target_properties(psmoveapi_tracker PROPERTIES
-        COMPILE_FLAGS -DBUILDING_SHARED_LIBRARY)
+        COMPILE_FLAGS -DBUILDING_SHARED_LIBRARY
+        SOVERSION 4.0.12)
     list(APPEND PSMOVEAPI_INSTALL_TARGETS psmoveapi_tracker)
 endif()
 
