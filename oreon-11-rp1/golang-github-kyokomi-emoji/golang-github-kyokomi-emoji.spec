%global source0_hash 412bcec93e56fd7309914e259b1d240d9bbaf834595b4aea8d8b493fa3178103

# Generated by go2rpm
%bcond check 0

# https://github.com/kyokomi/emoji
%global goipath         github.com/kyokomi/emoji
Version:                2.2.8

%gometa

%global goaltipaths     github.com/kyokomi/emoji/v2

%global common_description %{expand:
Emoji terminal output for Go.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        17%{?dist}
Summary:        Emoji terminal output for Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/PuerkitoBio/goquery)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/generateEmojiCodeMap ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc example README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
