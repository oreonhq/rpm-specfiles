%global source0_hash f0bcc11dcde3a6ff180277e45c24642d3da3c8690900e38f44495efbc9064411

Name:           R-jquerylib
Version:        %R_rpm_version 0.1.4
Release:        %autorelease
Summary:        Obtain jQuery as an HTML Dependency Object

License:        MIT
URL:            %{cran_url}
Source:         %{cran_source}

BuildArch:      noarch
BuildRequires:  R-devel

Provides:       bundled(js-jquery1) = 1.12.4
Provides:       bundled(js-jquery2) = 2.2.4
Provides:       bundled(js-jquery3) = 3.6.0

%description
Obtain any major version of jQuery (<https://code.jquery.com/>) and use it in
any webpage generated by htmltools (e.g. shiny, htmlwidgets, and rmarkdown).
Most R users don't need to use this package directly, but other R packages
(e.g. shiny, rmarkdown, etc.) depend on this package to avoid bundling
redundant copies of jQuery.

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -c

%generate_buildrequires
%R_buildrequires

%build

%install
%R_install
%R_save_files

%check
%R_check

%files -f %{R_files}

%changelog
%autochangelog
