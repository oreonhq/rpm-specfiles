%global source0_hash 9f5b3c9cb4c4e5464cfe4fdf75e8ea757e323a711ca2f884af05ae6bc90c8555

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/google/renameio
%global goipath         github.com/google/renameio
Version:                1.0.0

%gometa

%global common_description %{expand:
Package Renameio provides a way to atomically create or replace a file or
symbolic link.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Atomically create or replace a file or symbolic link

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
