%global source0_hash 4ad7fb7616dcbdc2d394ff98350226659e10dfe4dde9ed3256881351b0d515bb

# Generated by go2rpm 1.8.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/google/gofuzz
%global goipath         github.com/google/gofuzz
Version:                1.2.0

%gometa

%global common_description %{expand:
Gofuzz is a library for populating Go objects with random values.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

Name:           %{goname}
Release:        11%{?dist}
Summary:        Fuzz testing for Go

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}
# https://github.com/google/gofuzz/issues/58
# https://github.com/google/gofuzz/pull/59
Patch0:         0001-Fix-int-overflow.patch

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%patch -P0 -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
