diff -up lemon-1.3.1/cmake/FindSOPLEX.cmake.orig lemon-1.3.1/cmake/FindSOPLEX.cmake
--- lemon-1.3.1/cmake/FindSOPLEX.cmake.orig	2014-07-07 08:40:24.896315416 -0600
+++ lemon-1.3.1/cmake/FindSOPLEX.cmake	2025-01-15 10:11:26.605298825 -0700
@@ -16,7 +16,7 @@ IF(SOPLEX_FOUND)
   SET(SOPLEX_INCLUDE_DIRS ${SOPLEX_INCLUDE_DIR})
   SET(SOPLEX_LIBRARIES ${SOPLEX_LIBRARY})
   IF(CMAKE_SYSTEM_NAME STREQUAL "Linux")
-    SET(SOPLEX_LIBRARIES "${SOPLEX_LIBRARIES};z")
+    SET(SOPLEX_LIBRARIES "${SOPLEX_LIBRARIES};z-ng")
   ENDIF(CMAKE_SYSTEM_NAME STREQUAL "Linux")
 ENDIF(SOPLEX_FOUND)
 
diff -up lemon-1.3.1/lemon/soplex.cc.orig lemon-1.3.1/lemon/soplex.cc
--- lemon-1.3.1/lemon/soplex.cc.orig	2014-07-07 08:40:24.967315818 -0600
+++ lemon-1.3.1/lemon/soplex.cc	2025-11-25 20:24:23.903126916 -0700
@@ -19,7 +19,6 @@
 #include <iostream>
 #include <lemon/soplex.h>
 
-#include <soplex.h>
 #include <spxout.h>
 
 
@@ -41,7 +40,7 @@ namespace lemon {
     cols = lp.cols;
 
     soplex = new soplex::SoPlex;
-    (*static_cast<soplex::SPxLP*>(soplex)) = *(lp.soplex);
+    *soplex = *(lp.soplex);
 
     _col_names = lp._col_names;
     _col_names_ref = lp._col_names_ref;
@@ -73,22 +72,22 @@ namespace lemon {
     soplex::LPCol c;
     c.setLower(-soplex::infinity);
     c.setUpper(soplex::infinity);
-    soplex->addCol(c);
+    soplex->addColReal(c);
 
     _col_names.push_back(std::string());
 
-    return soplex->nCols() - 1;
+    return soplex->numCols() - 1;
   }
 
   int SoplexLp::_addRow() {
     soplex::LPRow r;
     r.setLhs(-soplex::infinity);
     r.setRhs(soplex::infinity);
-    soplex->addRow(r);
+    soplex->addRowReal(r);
 
     _row_names.push_back(std::string());
 
-    return soplex->nRows() - 1;
+    return soplex->numRows() - 1;
   }
 
   int SoplexLp::_addRow(Value l, ExprIterator b, ExprIterator e, Value u) {
@@ -97,16 +96,16 @@ namespace lemon {
       v.add(it->first, it->second);
     }
     soplex::LPRow r(l, v, u);
-    soplex->addRow(r);
+    soplex->addRowReal(r);
 
     _row_names.push_back(std::string());
 
-    return soplex->nRows() - 1;
+    return soplex->numRows() - 1;
   }
 
 
   void SoplexLp::_eraseCol(int i) {
-    soplex->removeCol(i);
+    soplex->removeColReal(i);
     _col_names_ref.erase(_col_names[i]);
     _col_names[i] = _col_names.back();
     _col_names_ref[_col_names.back()] = i;
@@ -114,7 +113,7 @@ namespace lemon {
   }
 
   void SoplexLp::_eraseRow(int i) {
-    soplex->removeRow(i);
+    soplex->removeRowReal(i);
     _row_names_ref.erase(_row_names[i]);
     _row_names[i] = _row_names.back();
     _row_names_ref[_row_names.back()] = i;
@@ -176,16 +175,16 @@ namespace lemon {
 
 
   void SoplexLp::_setRowCoeffs(int i, ExprIterator b, ExprIterator e) {
-    for (int j = 0; j < soplex->nCols(); ++j) {
-      soplex->changeElement(i, j, 0.0);
+    for (int j = 0; j < soplex->numCols(); ++j) {
+      soplex->changeElementReal(i, j, 0.0);
     }
     for(ExprIterator it = b; it != e; ++it) {
-      soplex->changeElement(i, it->first, it->second);
+      soplex->changeElementReal(i, it->first, it->second);
     }
   }
 
   void SoplexLp::_getRowCoeffs(int i, InsertIterator b) const {
-    const soplex::SVector& vec = soplex->rowVector(i);
+    const soplex::SVector& vec = soplex->rowVectorRealInternal(i);
     for (int k = 0; k < vec.size(); ++k) {
       *b = std::make_pair(vec.index(k), vec.value(k));
       ++b;
@@ -193,16 +192,16 @@ namespace lemon {
   }
 
   void SoplexLp::_setColCoeffs(int j, ExprIterator b, ExprIterator e) {
-    for (int i = 0; i < soplex->nRows(); ++i) {
-      soplex->changeElement(i, j, 0.0);
+    for (int i = 0; i < soplex->numRows(); ++i) {
+      soplex->changeElementReal(i, j, 0.0);
     }
     for(ExprIterator it = b; it != e; ++it) {
-      soplex->changeElement(it->first, j, it->second);
+      soplex->changeElementReal(it->first, j, it->second);
     }
   }
 
   void SoplexLp::_getColCoeffs(int i, InsertIterator b) const {
-    const soplex::SVector& vec = soplex->colVector(i);
+    const soplex::SVector& vec = soplex->colVectorRealInternal(i);
     for (int k = 0; k < vec.size(); ++k) {
       *b = std::make_pair(vec.index(k), vec.value(k));
       ++b;
@@ -210,65 +209,65 @@ namespace lemon {
   }
 
   void SoplexLp::_setCoeff(int i, int j, Value value) {
-    soplex->changeElement(i, j, value);
+    soplex->changeElementReal(i, j, value);
   }
 
   SoplexLp::Value SoplexLp::_getCoeff(int i, int j) const {
-    return soplex->rowVector(i)[j];
+    return soplex->rowVectorRealInternal(i)[j];
   }
 
   void SoplexLp::_setColLowerBound(int i, Value value) {
     LEMON_ASSERT(value != INF, "Invalid bound");
-    soplex->changeLower(i, value != -INF ? value : -soplex::infinity);
+    soplex->changeLowerReal(i, value != -INF ? value : -soplex::infinity);
   }
 
   SoplexLp::Value SoplexLp::_getColLowerBound(int i) const {
-    double value = soplex->lower(i);
+    double value = soplex->lowerReal(i);
     return value != -soplex::infinity ? value : -INF;
   }
 
   void SoplexLp::_setColUpperBound(int i, Value value) {
     LEMON_ASSERT(value != -INF, "Invalid bound");
-    soplex->changeUpper(i, value != INF ? value : soplex::infinity);
+    soplex->changeUpperReal(i, value != INF ? value : soplex::infinity);
   }
 
   SoplexLp::Value SoplexLp::_getColUpperBound(int i) const {
-    double value = soplex->upper(i);
+    double value = soplex->upperReal(i);
     return value != soplex::infinity ? value : INF;
   }
 
   void SoplexLp::_setRowLowerBound(int i, Value lb) {
     LEMON_ASSERT(lb != INF, "Invalid bound");
-    soplex->changeRange(i, lb != -INF ? lb : -soplex::infinity, soplex->rhs(i));
+    soplex->changeRangeReal(i, lb != -INF ? lb : -soplex::infinity, soplex->rhsReal(i));
   }
 
   SoplexLp::Value SoplexLp::_getRowLowerBound(int i) const {
-    double res = soplex->lhs(i);
+    double res = soplex->lhsReal(i);
     return res == -soplex::infinity ? -INF : res;
   }
 
   void SoplexLp::_setRowUpperBound(int i, Value ub) {
     LEMON_ASSERT(ub != -INF, "Invalid bound");
-    soplex->changeRange(i, soplex->lhs(i), ub != INF ? ub : soplex::infinity);
+    soplex->changeRangeReal(i, soplex->lhsReal(i), ub != INF ? ub : soplex::infinity);
   }
 
   SoplexLp::Value SoplexLp::_getRowUpperBound(int i) const {
-    double res = soplex->rhs(i);
+    double res = soplex->rhsReal(i);
     return res == soplex::infinity ? INF : res;
   }
 
   void SoplexLp::_setObjCoeffs(ExprIterator b, ExprIterator e) {
-    for (int j = 0; j < soplex->nCols(); ++j) {
-      soplex->changeObj(j, 0.0);
+    for (int j = 0; j < soplex->numCols(); ++j) {
+      soplex->changeObjReal(j, 0.0);
     }
     for (ExprIterator it = b; it != e; ++it) {
-      soplex->changeObj(it->first, it->second);
+      soplex->changeObjReal(it->first, it->second);
     }
   }
 
   void SoplexLp::_getObjCoeffs(InsertIterator b) const {
-    for (int j = 0; j < soplex->nCols(); ++j) {
-      Value coef = soplex->obj(j);
+    for (int j = 0; j < soplex->numCols(); ++j) {
+      Value coef = soplex->objReal(j);
       if (coef != 0.0) {
         *b = std::make_pair(j, coef);
         ++b;
@@ -277,11 +276,11 @@ namespace lemon {
   }
 
   void SoplexLp::_setObjCoeff(int i, Value obj_coef) {
-    soplex->changeObj(i, obj_coef);
+    soplex->changeObjReal(i, obj_coef);
   }
 
   SoplexLp::Value SoplexLp::_getObjCoeff(int i) const {
-    return soplex->obj(i);
+    return soplex->objReal(i);
   }
 
   SoplexLp::SolveExitStatus SoplexLp::_solve() {
@@ -304,7 +303,7 @@ namespace lemon {
 
   SoplexLp::Value SoplexLp::_getPrimal(int i) const {
     if (_primal_values.empty()) {
-      _primal_values.resize(soplex->nCols());
+      _primal_values.resize(soplex->numCols());
       soplex::Vector pv(_primal_values.size(), &_primal_values.front());
       soplex->getPrimal(pv);
     }
@@ -313,7 +312,7 @@ namespace lemon {
 
   SoplexLp::Value SoplexLp::_getDual(int i) const {
     if (_dual_values.empty()) {
-      _dual_values.resize(soplex->nRows());
+      _dual_values.resize(soplex->numRows());
       soplex::Vector dv(_dual_values.size(), &_dual_values.front());
       soplex->getDual(dv);
     }
@@ -321,11 +320,11 @@ namespace lemon {
   }
 
   SoplexLp::Value SoplexLp::_getPrimalValue() const {
-    return soplex->objValue();
+    return soplex->objValueReal();
   }
 
   SoplexLp::VarStatus SoplexLp::_getColStatus(int i) const {
-    switch (soplex->getBasisColStatus(i)) {
+    switch (soplex->basisColStatus(i)) {
     case soplex::SPxSolver::BASIC:
       return BASIC;
     case soplex::SPxSolver::ON_UPPER:
@@ -343,7 +342,7 @@ namespace lemon {
   }
 
   SoplexLp::VarStatus SoplexLp::_getRowStatus(int i) const {
-    switch (soplex->getBasisRowStatus(i)) {
+    switch (soplex->basisRowStatus(i)) {
     case soplex::SPxSolver::BASIC:
       return BASIC;
     case soplex::SPxSolver::ON_UPPER:
@@ -362,18 +361,18 @@ namespace lemon {
 
   SoplexLp::Value SoplexLp::_getPrimalRay(int i) const {
     if (_primal_ray.empty()) {
-      _primal_ray.resize(soplex->nCols());
+      _primal_ray.resize(soplex->numCols());
       soplex::Vector pv(_primal_ray.size(), &_primal_ray.front());
-      soplex->getDualfarkas(pv);
+      soplex->getDualFarkas(pv);
     }
     return _primal_ray[i];
   }
 
   SoplexLp::Value SoplexLp::_getDualRay(int i) const {
     if (_dual_ray.empty()) {
-      _dual_ray.resize(soplex->nRows());
+      _dual_ray.resize(soplex->numRows());
       soplex::Vector dv(_dual_ray.size(), &_dual_ray.front());
-      soplex->getDualfarkas(dv);
+      soplex->getDualFarkas(dv);
     }
     return _dual_ray[i];
   }
@@ -407,18 +406,18 @@ namespace lemon {
   void SoplexLp::_setSense(Sense sense) {
     switch (sense) {
     case MIN:
-      soplex->changeSense(soplex::SPxSolver::MINIMIZE);
+      soplex->setIntParam(soplex::SoPlex::OBJSENSE, soplex::SoPlex::OBJSENSE_MINIMIZE);
       break;
     case MAX:
-      soplex->changeSense(soplex::SPxSolver::MAXIMIZE);
+      soplex->setIntParam(soplex::SoPlex::OBJSENSE, soplex::SoPlex::OBJSENSE_MAXIMIZE);
     }
   }
 
   SoplexLp::Sense SoplexLp::_getSense() const {
-    switch (soplex->spxSense()) {
-    case soplex::SPxSolver::MAXIMIZE:
+    switch (soplex->intParam(soplex::SoPlex::OBJSENSE)) {
+    case soplex::SoPlex::OBJSENSE_MAXIMIZE:
       return MAX;
-    case soplex::SPxSolver::MINIMIZE:
+    case soplex::SoPlex::OBJSENSE_MINIMIZE:
       return MIN;
     default:
       LEMON_ASSERT(false, "Wrong sense.");
@@ -427,7 +426,8 @@ namespace lemon {
   }
 
   void SoplexLp::_clear() {
-    soplex->clear();
+    soplex->clearLPReal();
+    soplex->clearBasis();
     _col_names.clear();
     _col_names_ref.clear();
     _row_names.clear();
@@ -440,25 +440,25 @@ namespace lemon {
   void SoplexLp::_messageLevel(MessageLevel level) {
     switch (level) {
     case MESSAGE_NOTHING:
-      _message_level = -1;
+      _message_level = soplex::SPxOut::Verbosity::VERB_INFO3;
       break;
     case MESSAGE_ERROR:
-      _message_level = soplex::SPxOut::ERROR;
+      _message_level = soplex::SPxOut::Verbosity::VERB_ERROR;
       break;
     case MESSAGE_WARNING:
-      _message_level = soplex::SPxOut::WARNING;
+      _message_level = soplex::SPxOut::Verbosity::VERB_WARNING;
       break;
     case MESSAGE_NORMAL:
-      _message_level = soplex::SPxOut::INFO2;
+      _message_level = soplex::SPxOut::Verbosity::VERB_INFO2;
       break;
     case MESSAGE_VERBOSE:
-      _message_level = soplex::SPxOut::DEBUG;
+      _message_level = soplex::SPxOut::Verbosity::VERB_DEBUG;
       break;
     }
   }
 
   void SoplexLp::_applyMessageLevel() {
-    soplex::Param::setVerbose(_message_level);
+    soplex->spxout.setVerbosity(_message_level);
   }
 
 } //namespace lemon
diff -up lemon-1.3.1/lemon/soplex.h.orig lemon-1.3.1/lemon/soplex.h
--- lemon-1.3.1/lemon/soplex.h.orig	2014-07-07 08:40:24.968315824 -0600
+++ lemon-1.3.1/lemon/soplex.h	2025-11-25 20:22:01.176852849 -0700
@@ -26,11 +26,7 @@
 #include <string>
 
 #include <lemon/lp_base.h>
-
-// Forward declaration
-namespace soplex {
-  class SoPlex;
-}
+#include <soplex.h>
 
 namespace lemon {
 
@@ -148,7 +144,7 @@ namespace lemon {
     void _messageLevel(MessageLevel m);
     void _applyMessageLevel();
 
-    int _message_level;
+    soplex::SPxOut::Verbosity _message_level;
 
   };
 
diff -up lemon-1.3.1/test/CMakeLists.txt.orig lemon-1.3.1/test/CMakeLists.txt
--- lemon-1.3.1/test/CMakeLists.txt.orig	2014-07-07 08:40:24.971315841 -0600
+++ lemon-1.3.1/test/CMakeLists.txt	2023-02-11 15:04:46.842624883 -0700
@@ -63,7 +63,7 @@ IF(LEMON_HAVE_LP)
     ADD_EXECUTABLE(lp_test EXCLUDE_FROM_ALL lp_test.cc)
   ENDIF()
 
-  SET(LP_TEST_LIBS lemon)
+  SET(LP_TEST_LIBS lemon mpfr gmp)
 
   IF(LEMON_HAVE_GLPK)
     SET(LP_TEST_LIBS ${LP_TEST_LIBS} ${GLPK_LIBRARIES})
diff -up lemon-1.3.1/test/lp_test.cc.orig lemon-1.3.1/test/lp_test.cc
--- lemon-1.3.1/test/lp_test.cc.orig	2014-07-07 08:40:24.981315897 -0600
+++ lemon-1.3.1/test/lp_test.cc	2023-02-11 14:09:44.594993043 -0700
@@ -18,7 +18,6 @@
 
 #include <sstream>
 #include <lemon/lp_skeleton.h>
-#include "test_tools.h"
 #include <lemon/tolerance.h>
 
 #include <lemon/config.h>
@@ -33,6 +32,7 @@
 
 #ifdef LEMON_HAVE_SOPLEX
 #include <lemon/soplex.h>
+#undef check
 #endif
 
 #ifdef LEMON_HAVE_CLP
@@ -42,6 +42,9 @@
 #ifdef LEMON_HAVE_LP
 #include <lemon/lp.h>
 #endif
+
+#include "test_tools.h"
+
 using namespace lemon;
 
 int countCols(LpBase & lp) {
