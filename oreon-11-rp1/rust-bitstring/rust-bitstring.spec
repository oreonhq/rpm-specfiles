%global source0_hash 3742e5902f4b964758253f98bbe24afc2efd8245778448cd3440c026c36e0fd6

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate bitstring

Name:           rust-bitstring
Version:        0.2.1
Release:        %autorelease
Summary:        Bitstring traits and implementations

License:        MIT
URL:            https://crates.io/crates/bitstring
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Bitstring traits and implementations}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+bigendian-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+bigendian-devel %{_description}

This package contains library source intended for building other packages which
use the "bigendian" feature of the "%{crate}" crate.

%files       -n %{name}+bigendian-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fixed-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fixed-devel %{_description}

This package contains library source intended for building other packages which
use the "fixed" feature of the "%{crate}" crate.

%files       -n %{name}+fixed-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+net-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+net-devel %{_description}

This package contains library source intended for building other packages which
use the "net" feature of the "%{crate}" crate.

%files       -n %{name}+net-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
