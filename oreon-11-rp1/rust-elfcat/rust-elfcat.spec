%global source0_hash 227d2865b4e318a251257ec3718542c4ad9f9afbd6d7deb052ae7942a0d52f97

# Generated by rust2rpm 27
%bcond check 1

%global crate elfcat

Name:           rust-elfcat
Version:        0.1.10
Release:        %autorelease
Summary:        ELF visualizer. Generates HTML files from ELF binaries

License:        Zlib
URL:            https://crates.io/crates/elfcat
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
ELF visualizer. Generates HTML files from ELF binaries.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}

%description -n %{crate} %{_description}

%files       -n %{crate}
%license license
%doc changelog.md
%doc readme.md
%{_bindir}/elfcat

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
