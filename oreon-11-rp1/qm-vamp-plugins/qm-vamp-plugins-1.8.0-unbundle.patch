diff -up qm-vamp-plugins-1.8.0/build/general/Makefile.inc.unbundle qm-vamp-plugins-1.8.0/build/general/Makefile.inc
--- qm-vamp-plugins-1.8.0/build/general/Makefile.inc.unbundle	2020-01-27 18:11:39.000000000 +0100
+++ qm-vamp-plugins-1.8.0/build/general/Makefile.inc	2025-04-02 13:20:27.520584130 +0200
@@ -4,13 +4,11 @@ PLUGIN	?= qm-vamp-plugins$(PLUGIN_EXT)
 CXX	?= g++
 CC	?= gcc
 
-QM_DSP_DIR	:= lib/qm-dsp
 VAMP_SDK_DIR    := lib/vamp-plugin-sdk
 VAMP_SRC_DIR    := lib/vamp-plugin-sdk/src/vamp-sdk
 
-CFLAGS		:= $(CFLAGS) -I. -I$(QM_DSP_DIR) -I$(VAMP_SDK_DIR)
-CXXFLAGS	:= $(CXXFLAGS) -I. -I$(QM_DSP_DIR) -I$(VAMP_SDK_DIR)
-LDFLAGS		:= -L$(QM_DSP_DIR) $(LDFLAGS)
+CFLAGS		:= $(CFLAGS) -I. -I$(VAMP_SDK_DIR)
+CXXFLAGS	:= $(CXXFLAGS) -I. -I$(VAMP_SDK_DIR)
 
 HEADERS := plugins/AdaptiveSpectrogram.h \
            plugins/BarBeatTrack.h \
@@ -52,22 +50,17 @@ SOURCES    := $(SOURCES) $(VAMP_SOURCES)
 OBJECTS := $(SOURCES:.cpp=.o)
 OBJECTS := $(OBJECTS:.c=.o)
 
-all: $(QM_DSP_DIR) $(PLUGIN)
+all: $(PLUGIN)
 
 MF   := $(wildcard build/*/Makefile$(MAKEFILE_EXT))
 
-.PHONY: $(QM_DSP_DIR)
-$(QM_DSP_DIR): 
-		$(MAKE) -C $@ -f $(MF)
-
-$(PLUGIN):	$(OBJECTS) $(QM_DSP_DIR)/libqm-dsp.a
+$(PLUGIN):	$(OBJECTS)
 		$(CXX) -o $@ $^ $(LDFLAGS)
 
 test:		all
 		bash test/regression.sh
 
 clean:		
-		$(MAKE) -C $(QM_DSP_DIR) -f $(MF) clean
 		rm -f $(OBJECTS)
 
 distclean:	clean
