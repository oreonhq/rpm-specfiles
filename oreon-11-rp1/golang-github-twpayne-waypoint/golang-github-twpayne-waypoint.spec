%global source0_hash 4a1bd6a8520aa61dccba7e4a850e711fc8e2f993f78766d0fdb5e927261b28b9

# Generated by go2rpm 1.3
%bcond check 0

# https://github.com/twpayne/go-waypoint
%global goipath         github.com/twpayne/go-waypoint
%global commit          4f8e6bfac4df0b7c87ed44ca1ba73f77f47475ed

%gometa

%global common_description %{expand:
Package Waypoint reads and writes waypoints in formats used by the free flying
community.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        0.18%{?dist}
Summary:        Reads and writes waypoints in formats used by the free flying community

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
BuildRequires:  golang(github.com/stretchr/testify/require)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
