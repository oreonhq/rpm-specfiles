%global source0_hash 3aa954171903797d5623e047d9ab69d91b493657917bdfb8c2c80ecaf9cdb6f4

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate color-print

Name:           rust-color-print
Version:        0.3.7
Release:        %autorelease
Summary:        Colorize and stylize strings for terminal at compile-time, by

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/color-print
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Colorize and stylize strings for terminal at compile-time, by using an
HTML-like syntax}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+terminfo-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+terminfo-devel %{_description}

This package contains library source intended for building other packages which
use the "terminfo" feature of the "%{crate}" crate.

%files       -n %{name}+terminfo-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
