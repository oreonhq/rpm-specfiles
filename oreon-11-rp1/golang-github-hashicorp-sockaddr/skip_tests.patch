From 543b6f8ae7382f92dc4dd29139db7b0e46afd5b9 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Alejandro=20S=C3=A1ez?= <asm@redhat.com>
Date: Wed, 9 Apr 2025 10:19:54 +0200
Subject: [PATCH] Skip tests inside Fedora's CI

---
 ifaddr_test.go            |  7 +++++++
 ifaddrs_test.go           | 13 +++++++++++++
 ipaddrs_test.go           |  4 ++++
 template/template_test.go |  5 +++++
 4 files changed, 29 insertions(+)

diff --git a/ifaddr_test.go b/ifaddr_test.go
index 45a0cc7..edcaf44 100644
--- a/ifaddr_test.go
+++ b/ifaddr_test.go
@@ -46,6 +46,9 @@ func havePublicIP() bool {
 }
 
 func TestGetPrivateIP(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
 	reportOnPrivate := func(args ...interface{}) {
 		if havePrivateIP() {
 			t.Fatalf(args[0].(string), args[1:]...)
@@ -64,6 +67,9 @@ func TestGetPrivateIP(t *testing.T) {
 }
 
 func TestGetPrivateIPs(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
 	reportOnPrivate := func(args ...interface{}) {
 		if havePrivateIP() {
 			t.Fatalf(args[0].(string), args[1:]...)
@@ -164,6 +170,7 @@ func TestIfAddrAttr(t *testing.T) {
 }
 
 func TestIfAddrMath(t *testing.T) {
+
 	tests := []struct {
 		name      string
 		ifAddr    sockaddr.IfAddr
diff --git a/ifaddrs_test.go b/ifaddrs_test.go
index c89af4a..e3cc437 100644
--- a/ifaddrs_test.go
+++ b/ifaddrs_test.go
@@ -3,6 +3,7 @@ package sockaddr_test
 import (
 	"fmt"
 	"net"
+	"os"
 	"reflect"
 	"runtime"
 	"testing"
@@ -610,6 +611,9 @@ func TestFilterIfByType(t *testing.T) {
 // doesn't do much in the way of testing beyond verifying that `lo0` has a
 // loopback address present.
 func TestGetIfAddrs(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
 	ifAddrs, err := sockaddr.GetAllInterfaces()
 	if err != nil {
 		t.Fatalf("Unable to proceed: %v", err)
@@ -677,6 +681,9 @@ func TestGetIfAddrs(t *testing.T) {
 // TestGetDefaultIfName tests to make sure a default interface name is always
 // returned from getDefaultIfName().
 func TestGetDefaultInterface(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
 	reportOnDefault := func(args ...interface{}) {
 		if havePublicIP() || havePrivateIP() {
 			t.Fatalf(args[0].(string), args[1:]...)
@@ -787,6 +794,9 @@ func TestGetAllInterfaces(t *testing.T) {
 }
 
 func TestGetDefaultInterfaces(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
 	reportOnDefault := func(args ...interface{}) {
 		if havePublicIP() || havePrivateIP() {
 			t.Fatalf(args[0].(string), args[1:]...)
@@ -806,6 +816,9 @@ func TestGetDefaultInterfaces(t *testing.T) {
 }
 
 func TestGetPrivateInterfaces(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
 	reportOnPrivate := func(args ...interface{}) {
 		if havePrivateIP() {
 			t.Fatalf(args[0].(string), args[1:]...)
diff --git a/ipaddrs_test.go b/ipaddrs_test.go
index a72f77f..9ae21ad 100644
--- a/ipaddrs_test.go
+++ b/ipaddrs_test.go
@@ -2,6 +2,7 @@ package sockaddr_test
 
 import (
 	"fmt"
+	"os"
 	"sort"
 	"testing"
 
@@ -336,6 +337,9 @@ func TestSockAddr_IPAddrs_IPAddrsByNetworkSize(t *testing.T) {
 				// }
 				// prevAddr = ipAddr
 
+				if os.Getenv("RPM_BUILD_ROOT") != "" {
+					t.Skip("Network not available inside the Fedora's CI")
+				}
 				if !ipAddr.Equal(sortedAddrs[i]) {
 					t.Errorf("[%d] Sort equality failed: expected %s, received %s", i, sortedAddrs[i], ipAddr)
 				}
diff --git a/template/template_test.go b/template/template_test.go
index ec9822e..d08fe36 100644
--- a/template/template_test.go
+++ b/template/template_test.go
@@ -1,12 +1,17 @@
 package template_test
 
 import (
+	"os"
 	"testing"
 
 	socktmpl "github.com/hashicorp/go-sockaddr/template"
 )
 
 func TestSockAddr_Parse(t *testing.T) {
+	if os.Getenv("RPM_BUILD_ROOT") != "" {
+		t.Skip("Network not available inside the Fedora's CI")
+	}
+
 	tests := []struct {
 		name          string
 		input         string
-- 
2.49.0

