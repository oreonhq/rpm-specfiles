%global source0_hash 72ce181e3f6bf82d6c1dc569103ca7b1bd964c60ba03d7e6cdfbb3e3eb7f7405

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate plotters-bitmap

Name:           rust-plotters-bitmap
Version:        0.3.7
Release:        %autorelease
Summary:        Plotters Bitmap Backend

License:        MIT
URL:            https://crates.io/crates/plotters-bitmap
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Plotters Bitmap Backend}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gif-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gif-devel %{_description}

This package contains library source intended for building other packages which
use the "gif" feature of the "%{crate}" crate.

%files       -n %{name}+gif-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gif_backend-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gif_backend-devel %{_description}

This package contains library source intended for building other packages which
use the "gif_backend" feature of the "%{crate}" crate.

%files       -n %{name}+gif_backend-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+image_encoder-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+image_encoder-devel %{_description}

This package contains library source intended for building other packages which
use the "image_encoder" feature of the "%{crate}" crate.

%files       -n %{name}+image_encoder-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
