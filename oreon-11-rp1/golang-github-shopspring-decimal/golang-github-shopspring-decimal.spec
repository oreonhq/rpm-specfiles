%global source0_hash 59fa76645f63d7383d3f306fe2f7136d923e2aa514d9a9353938d9654b842f58

# Generated by go2rpm
# https://github.com/shopspring/decimal/issues/101
%ifnarch aarch64
%bcond check 0
%endif

%global debug_package %{nil}

# https://github.com/shopspring/decimal
%global goipath         github.com/shopspring/decimal
Version:                1.2.0

%gometa

%global common_description %{expand:
Package Decimal implements an arbitrary precision fixed-point decimal.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Arbitrary-precision fixed-point decimal numbers in Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
