%global source0_hash 310b08c538c04bae779a1c4786430e974801e8880a4c5256dc0877bc82b61af0

# Generated by rust2rpm 27
%bcond check 1

# prevent library files from being installed
%global cargo_install_lib 0

Name:           spacer
Version:        0.5.0
Release:        %autorelease
Summary:        CLI utility for adding spacers when command output stops

SourceLicense:  MIT
License:        %{shrink:
                MIT AND
                Apache-2.0 AND
                ISC AND
                Unicode-DFS-2016 AND
                (0BSD OR MIT OR Apache-2.0) AND
                (Apache-2.0 OR MIT) AND
                (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND
                (MIT OR Zlib OR Apache-2.0) AND
                (Unlicense OR MIT)
                }
# LICENSE.dependencies contains a full license breakdown

URL:            https://github.com/samwho/spacer
Source:         %{url}/archive/v%{version}/spacer-%{version}.tar.gz

BuildRequires:  cargo-rpm-macros >= 26

# https://fedoraproject.org/wiki/Changes/EncourageI686LeafRemoval
ExcludeArch:    %{ix86}

%description
A CLI utility for adding spacers when command output stops.

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%files
%license LICENSE
%license LICENSE.dependencies
%{_bindir}/spacer

%changelog
%autochangelog
