%global source0_hash 1f9f08af0c877571712e2e3e686ad79efad9657dbf0f7c3c8ba943ff6c38932d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate packed_simd

Name:           rust-packed_simd
Version:        0.3.9
Release:        %autorelease
Summary:        Portable Packed SIMD vectors

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/packed_simd
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Portable Packed SIMD vectors}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/contributing.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+core_arch-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+core_arch-devel %{_description}

This package contains library source intended for building other packages which
use the "core_arch" feature of the "%{crate}" crate.

%files       -n %{name}+core_arch-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+into_bits-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+into_bits-devel %{_description}

This package contains library source intended for building other packages which
use the "into_bits" feature of the "%{crate}" crate.

%files       -n %{name}+into_bits-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+libcore_neon-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+libcore_neon-devel %{_description}

This package contains library source intended for building other packages which
use the "libcore_neon" feature of the "%{crate}" crate.

%files       -n %{name}+libcore_neon-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
