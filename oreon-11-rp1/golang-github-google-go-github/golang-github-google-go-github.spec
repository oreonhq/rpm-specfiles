%global source0_hash e34c33fae76b0514788c72df68279230e4feedcddf767445f49d275256197157

# Generated by go2rpm 1.9.0
# Needs network
%bcond check 1
%global debug_package %{nil}

# https://github.com/google/go-github
%global goipath         github.com/google/go-github
Version:                55.0.0

%gometa

%global goname golang-github-google-go-github

%global goaltipaths     github.com/google/go-github/v55

%global common_description %{expand:
Go library for accessing the GitHub v3 API.}

%global golicenses      LICENSE
%global godocs          AUTHORS CONTRIBUTING.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Go library for accessing the GitHub v3 API

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1
rm -rf example

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
