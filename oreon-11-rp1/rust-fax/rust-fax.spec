%global source0_hash caf1079563223d5d59d83c85886a56e586cfd5c1a26292e971a0fa266531ac5a

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate fax

Name:           rust-fax
Version:        0.2.7
Release:        %autorelease
Summary:        Decoder and Encoder for CCITT Group 3 and 4 bi-level image encodings...

License:        MIT
URL:            https://crates.io/crates/fax
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Decoder and Encoder for CCITT Group 3 and 4 bi-level image encodings used by fax machines TIFF and PDF.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+debug-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+debug-devel %{_description}

This package contains library source intended for building other packages which
use the "debug" feature of the "%{crate}" crate.

%files       -n %{name}+debug-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fax_derive-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fax_derive-devel %{_description}

This package contains library source intended for building other packages which
use the "fax_derive" feature of the "%{crate}" crate.

%files       -n %{name}+fax_derive-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+generate_bitmaps-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+generate_bitmaps-devel %{_description}

This package contains library source intended for building other packages which
use the "generate_bitmaps" feature of the "%{crate}" crate.

%files       -n %{name}+generate_bitmaps-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
