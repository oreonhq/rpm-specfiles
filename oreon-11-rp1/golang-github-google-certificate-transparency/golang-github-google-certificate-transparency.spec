%global source0_hash 8fa4793033ba4e72b4619724a089e2d5770467332220df64d484e9b52e127e23

# Generated by go2rpm 1.6.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/google/certificate-transparency-go
%global goipath         github.com/google/certificate-transparency-go
Version:                1.1.3

%gometa

%global common_description %{expand:
Auditing for TLS certificates (Go code).}

%global golicenses      LICENSE
%global godocs          AUTHORS CONTRIBUTING.md CHANGELOG.md CONTRIBUTORS\\\
                        README.md examples

Name:           %{goname}
Release:        %autorelease
Summary:        Auditing for TLS certificates, Go code

License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
for test in "TestRateLimiterGoroutines" \
            "TestSECP192R1" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%if 0%{?__isa_bits} == 32
%gocheck -d client -d loglist -d x509
%else
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
