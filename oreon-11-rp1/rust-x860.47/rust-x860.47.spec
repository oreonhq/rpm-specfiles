%global source0_hash 55b5be8cc34d017d8aabec95bc45a43d0f20e8b2a31a453cabc804fe996f8dca

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate x86

Name:           rust-x860.47
Version:        0.47.0
Release:        %autorelease
Summary:        Library to program x86 (amd64) hardware. Contains x86 specific data s...

License:        MIT
URL:            https://crates.io/crates/x86
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Library to program x86 (amd64) hardware. Contains x86 specific data structure descriptions, data-tables, as well as convenience function to call assembly instructions typically not exposed in higher level languages.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+performance-counter-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+performance-counter-devel %{_description}

This package contains library source intended for building other packages which
use the "performance-counter" feature of the "%{crate}" crate.

%files       -n %{name}+performance-counter-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+phf-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+phf-devel %{_description}

This package contains library source intended for building other packages which
use the "phf" feature of the "%{crate}" crate.

%files       -n %{name}+phf-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+utest-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+utest-devel %{_description}

This package contains library source intended for building other packages which
use the "utest" feature of the "%{crate}" crate.

%files       -n %{name}+utest-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+vmtest-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+vmtest-devel %{_description}

This package contains library source intended for building other packages which
use the "vmtest" feature of the "%{crate}" crate.

%files       -n %{name}+vmtest-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
