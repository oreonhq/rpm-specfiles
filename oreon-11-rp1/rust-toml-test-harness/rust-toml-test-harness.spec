%global source0_hash 7bc04950ffde8357bee530f27f94e8ae57ba26361fe53bc1cd562cefccf8785c

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate toml-test-harness

Name:           rust-toml-test-harness
Version:        1.13.0
Release:        %autorelease
Summary:        Cargo test harness for verifying TOML parsers

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/toml-test-harness
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Cargo test harness for verifying TOML parsers}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+snapbox-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+snapbox-devel %{_description}

This package contains library source intended for building other packages which
use the "snapbox" feature of the "%{crate}" crate.

%files       -n %{name}+snapbox-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+snapshot-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+snapshot-devel %{_description}

This package contains library source intended for building other packages which
use the "snapshot" feature of the "%{crate}" crate.

%files       -n %{name}+snapshot-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
