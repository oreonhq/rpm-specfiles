%global source0_hash 9398cb52d62b73d3a0837c6cb197a439d8a14334c778a174cb258c7c09ce4cf2

# Generated by rust2rpm 26
%bcond check 0

%global crate shellcheck-sarif

Name:           rust-shellcheck-sarif
Version:        0.5.0
Release:        %autorelease
Summary:        Convert shellcheck output to SARIF

License:        MIT
URL:            https://crates.io/crates/shellcheck-sarif
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Convert shellcheck output to SARIF.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
License:        MIT AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 OR MIT)
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/shellcheck-sarif

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
