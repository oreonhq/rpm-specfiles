%global source0_hash af066a9c399a26e020ada66a034357a868728e72cd426f3adcd35f80d88d88c8

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate proc-macro2-diagnostics

Name:           rust-proc-macro2-diagnostics
Version:        0.10.1
Release:        %autorelease
Summary:        Diagnostics for proc-macro2.

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/proc-macro2-diagnostics
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Diagnostics for proc-macro2.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+colors-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+colors-devel %{_description}

This package contains library source intended for building other packages which
use the "colors" feature of the "%{crate}" crate.

%files       -n %{name}+colors-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+yansi-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+yansi-devel %{_description}

This package contains library source intended for building other packages which
use the "yansi" feature of the "%{crate}" crate.

%files       -n %{name}+yansi-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
