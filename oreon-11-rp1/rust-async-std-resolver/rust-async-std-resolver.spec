%global source0_hash b766684a183c8a439f2ca24d6973cf8bfdc1353ae9c54b2b91e81d2630dd034e

# Generated by rust2rpm 27
# Skip tests as they are required network connection
%bcond check 0
%global debug_package %{nil}

%global crate async-std-resolver

Name:           rust-async-std-resolver
Version:        0.24.4
Release:        %autorelease
Summary:        DNS resolver implementation based on async-std and Hickory DNS

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/async-std-resolver
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Hickory DNS is a safe and secure DNS library, for async-std. This
Resolver library uses the hickory-proto library to perform all DNS
queries. The Resolver is intended to be a high-level library for any DNS
record resolution see Resolver and AsyncResolver for supported
resolution types. The Client can be used for other queries.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+system-config-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+system-config-devel %{_description}

This package contains library source intended for building other packages which
use the "system-config" feature of the "%{crate}" crate.

%files       -n %{name}+system-config-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
