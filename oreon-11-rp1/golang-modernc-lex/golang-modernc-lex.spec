%global source0_hash 1591d32f4b88b078f86566a1db1e91e036b564a17ef2e62283bdcb58bc310fff

# Generated by go2rpm 1.8.0
%bcond check 0
%global debug_package %{nil}

# https://gitlab.com/cznic/lexer
%global goipath         modernc.org/lex
%global forgeurl        https://gitlab.com/cznic/lex
Version:                1.1.0
%global tag             v1.1.0

%gometa

%global common_description %{expand:
Support for a *nix (f)lex like tool on .l sources.}

%global golicenses      LICENSE
%global godocs          AUTHORS CONTRIBUTORS README

Name:           %{goname}
Release:        %autorelease
Summary:        Support for a *nix (f)lex like tool on .l sources

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires
echo "golang(modernc.org/lexer)"

%build
%gobuild -o %{gobuilddir}/bin/golex %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc AUTHORS CONTRIBUTORS README
%{_bindir}/golex

%gopkgfiles

%changelog
%autochangelog
