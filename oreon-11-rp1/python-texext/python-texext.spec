%global source0_hash 6c3ccc5578f93a1bd0051fcba25e92d259620d3d9c9031738570e7d78603e7a3

Name:           python-texext
Version:        0.6.8
Release:        %autorelease
Summary:        Sphinx extensions for working with LaTeX math

# BSD-2-Clause: the project as a whole
# Unlicense: _version.py generated by versioneer
License:        BSD-2-Clause AND Unlicense
URL:            https://github.com/matthew-brett/texext
VCS:            git:%{url}.git
Source:         %pypi_source texext
# Remove undeclared dependency on six
# https://github.com/matthew-brett/texext/pull/15
Patch:          %{name}-remove-six.patch

BuildArch:      noarch
BuildSystem:    pyproject
BuildOption(generate_buildrequires): -x test
BuildOption(install): -l texext

%description
This package contains Sphinx extensions for working with LaTeX math.

%package -n     python3-texext
Summary:        Sphinx extensions for working with LaTeX math

%description -n python3-texext
This package contains Sphinx extensions for working with LaTeX math.

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n texext-%{version} -p1

%build -a
rst2html --no-datestamp README.rst README.html

%check
%pytest -v

%files -n python3-texext -f %{pyproject_files}
%doc README.html

%changelog
%autochangelog
