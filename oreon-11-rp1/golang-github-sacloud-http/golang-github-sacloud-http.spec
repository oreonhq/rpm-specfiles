%global source0_hash 417660f8fd96b5132a503fd9207f9797b343ca7fcc11b1df671c08d5e996cc16

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/sacloud/go-http
%global goipath         github.com/sacloud/go-http
Version:                0.1.6

%gometa -f

%global common_description %{expand:
HTTP client library for SAKURA cloud in Go.}

%global golicenses      LICENSE includes/LICENSE
%global godocs          AUTHORS README.md includes/AUTHORS includes/README.md

Name:           %{goname}
Release:        %autorelease
Summary:        HTTP client library for SAKURA cloud in Go

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
