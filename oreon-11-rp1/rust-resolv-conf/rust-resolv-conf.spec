%global source0_hash 1e061d1b48cb8d38042de4ae0a7a6401009d6143dc80d2e2d6f31f0bdd6470c7

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate resolv-conf

Name:           rust-resolv-conf
Version:        0.7.6
Release:        %autorelease
Summary:        The resolv.conf file parser

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/resolv-conf
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
The resolv.conf file parser}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+system-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+system-devel %{_description}

This package contains library source intended for building other packages which
use the "system" feature of the "%{crate}" crate.

%files       -n %{name}+system-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
