diff -up ocp-3.1.1/playtimidity/timidityconfig.c.cfgfile ocp-3.1.1/playtimidity/timidityconfig.c
--- ocp-3.1.1/playtimidity/timidityconfig.c.cfgfile	2026-01-17 17:39:23.000000000 -0500
+++ ocp-3.1.1/playtimidity/timidityconfig.c	2026-01-18 16:13:03.045209034 -0500
@@ -700,6 +700,7 @@ other_global:
 	scan_sf2_directory (API, API->configAPI->DataPath);
 #else
 	if (strcmp(CONFIG_FILE, "/etc/timidity/timidity.cfg") &&
+	    strcmp(CONFIG_FILE, "/etc/timidity++.cfg") &&
 	    strcmp(CONFIG_FILE, "/etc/timidity.cfg") &&
 	    strcmp(CONFIG_FILE, "/usr/local/share/timidity/timidity.cfg") &&
 	    strcmp(CONFIG_FILE, "/usr/share/timidity/timidity.cfg"))
@@ -707,6 +708,7 @@ other_global:
 		try_global (API, CONFIG_FILE);
 	}
 	try_global (API, "/etc/timidity/timidity.cfg");
+	try_global (API, "/etc/timidity++.cfg");
 	try_global (API, "/etc/timidity.cfg");
 	try_global (API, "/usr/local/share/timidity/timidity.cfg");
 	try_global (API, "/usr/share/timidity/timidity.cfg");
diff -up ocp-3.1.1/playtimidity/timidityplay.c.cfgfile ocp-3.1.1/playtimidity/timidityplay.c
--- ocp-3.1.1/playtimidity/timidityplay.c.cfgfile	2026-01-17 17:39:23.000000000 -0500
+++ ocp-3.1.1/playtimidity/timidityplay.c	2026-01-18 16:02:55.682104178 -0500
@@ -1029,6 +1029,7 @@ other_global:
 #else
 		/* test CONFIG_FILE first if it is defined to be something else than one of our standard paths */
 		if (strcmp(CONFIG_FILE, "/etc/timidity/timidity.cfg") &&
+		    strcmp(CONFIG_FILE, "/etc/timidity++.cfg") &&
 		    strcmp(CONFIG_FILE, "/etc/timidity.cfg") &&
 		    strcmp(CONFIG_FILE, "/usr/local/share/timidity/timidity.cfg") &&
 		    strcmp(CONFIG_FILE, "/usr/share/timidity/timidity.cfg") && !read_config_file(&tc, CONFIG_FILE, 0, 0))
@@ -1037,6 +1038,9 @@ other_global:
 		} else if (!read_config_file(&tc, "/etc/timidity/timidity.cfg", 0, 0))
 		{
 			c->got_a_configuration = 1;
+		} else if (!read_config_file(&tc, "/etc/timidity++.cfg", 0, 0))
+		{
+			c->got_a_configuration = 1;
 		} else if (!read_config_file(&tc, "/etc/timidity.cfg", 0, 0))
 		{
 			c->got_a_configuration = 1;
