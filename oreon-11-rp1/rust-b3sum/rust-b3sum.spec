%global source0_hash 549398f8f677d3d3700b9ee4eeaf6d9c1fad88eea324f0633523a315fbcd8dff

# Generated by rust2rpm 28
%bcond check 1

%global crate b3sum

Name:           rust-b3sum
Version:        1.8.3
Release:        %autorelease
Summary:        Command line implementation of the BLAKE3 hash function

License:        Apache-2.0 OR Apache-2.0 WITH LLVM-exception
URL:            https://crates.io/crates/b3sum
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
#   expression (but note that LICENSE still includes CC0-1.0 text); see
# * Allow older duct dev-dependency 0.13 (vs. 1.0) until it can be updated,
#   https://bugzilla.redhat.com/show_bug.cgi?id=2364936
Patch:          b3sum-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  help2man

%global _description %{expand:
A command line implementation of the BLAKE3 hash function.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Apache-2.0 OR Apache-2.0 WITH LLVM-exception
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# BSD-2-Clause
# MIT
# MIT OR Apache-2.0
# MIT-0 OR Apache-2.0
License:        %{shrink:
                (Apache-2.0 OR MIT) AND
                (Apache-2.0 WITH LLVM-exception OR Apache-2.0) AND
                (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND
                BSD-2-Clause AND
                MIT AND
                (MIT-0 OR Apache-2.0)
                }
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE_A2
%license LICENSE_A2LLVM
%license LICENSE.dependencies
%doc README.md
%doc what_does_check_do.md
%{_bindir}/b3sum
%{_mandir}/man1/b3sum.1*

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies
help2man --no-info --output=b3sum.1 target/rpm/b3sum

%install
%cargo_install
install -t %{buildroot}%{_mandir}/man1 -D -p -m 0644 b3sum.1

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
