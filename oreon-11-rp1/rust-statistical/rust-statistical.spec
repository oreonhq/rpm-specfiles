%global source0_hash 49d57902bb128e5e38b5218d3681215ae3e322d99f65d5420e9849730d2ea372

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate statistical

Name:           rust-statistical
Version:        1.0.0
Release:        %autorelease
Summary:        Simple statistics library

License:        MIT
URL:            https://crates.io/crates/statistical
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump num dependency from 0.2 to 0.3
# * bump rand dependency from 0.6 to 0.8
Patch:          statistical-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A simple statistics library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/AUTHORS
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# https://github.com/JeffBelgum/statistical/issues/16
%cargo_test -- -- --skip univariate_::test::test_pkurtosis
%endif

%changelog
%autochangelog
