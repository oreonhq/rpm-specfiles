%global source0_hash 8192fa1e574a067848e404651275f19603ac4ec0d8957997597a0495614ddc85

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate remoteprocess

Name:           rust-remoteprocess
Version:        0.5.1
Release:        %autorelease
Summary:        Cross platform api for getting information on a running processes

License:        MIT
URL:            https://crates.io/crates/remoteprocess
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          remoteprocess-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * Update nix from 0.26 to 0.31:
#   https://github.com/benfred/remoteprocess/pull/116; this requires an
#   accompanying source-code patch.
Patch:          remoteprocess-fix-metadata.diff
# * Remove execute permissions from Rust sources
Patch10:        https://github.com/benfred/remoteprocess/pull/112.patch
# * Source-code patch required to update nix from 0.26 to 0.31
Patch11:        0001-Update-nix-from-0.26-to-0.31.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Cross platform api for getting information on a running processes.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unwind-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unwind-devel %{_description}

This package contains library source intended for building other packages which
use the "unwind" feature of the "%{crate}" crate.

%files       -n %{name}+unwind-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
