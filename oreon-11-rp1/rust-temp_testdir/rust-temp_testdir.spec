%global source0_hash 921f1e9c427802414907a48b21a6504ff6b3a15a1a3cf37e699590949ad9befc

# Generated by rust2rpm 24
%bcond check 0
%global debug_package %{nil}

%global crate temp_testdir

Name:           rust-temp_testdir
Version:        0.2.3
Release:        %autorelease
Summary:        Little crate to use temp directory in crate

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/temp_testdir
Source:         %{crates_source}
# Add missing license files: https://github.com/la10736/temp_testdir/pull/5
Source:         https://github.com/la10736/temp_testdir/raw/b2ad5cf/LICENSE-APACHE
Source:         https://github.com/la10736/temp_testdir/raw/b2ad5cf/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Little crate to use temp directory in crate. You can chose if delete it
after use or not to debugging purpose.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
