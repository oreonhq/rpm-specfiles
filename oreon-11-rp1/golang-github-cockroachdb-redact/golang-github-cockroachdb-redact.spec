%global source0_hash 763be66aaf9d8a304a2dc1a12173672a59e5c5fd7c75136de44c74e0f7ff53d5

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/cockroachdb/redact
%global goipath         github.com/cockroachdb/redact
Version:                1.0.9

%gometa

%global common_description %{expand:
Utilities to redact Go strings for confidentiality.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Utilities to redact Go strings for confidentiality

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
