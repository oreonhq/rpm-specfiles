%global source0_hash 6d423ab1731036e1ebaba37fb7e9d2a2c3d4b5736d2f7f0cad2f1bab44197027

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/uber-go/ratelimit
%global goipath         go.uber.org/ratelimit
%global forgeurl        https://github.com/uber-go/ratelimit
Version:                0.3.1

%gometa -L -f

%global common_description %{expand:
A Go blocking leaky-bucket rate limit implementation.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           golang-uber-ratelimit
Release:        %autorelease
Summary:        Golang blocking leaky-bucket rate limit implementation

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
