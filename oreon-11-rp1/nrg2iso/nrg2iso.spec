%global source0_hash 25049d864680ec12bbe31b20597ce8c1ba3a4fe7a7f11e25742b83e2fda94aa3

Name:           nrg2iso
Version:        0.4
Release:        37%{?dist}
Summary:        Convert Nero Burning Rom image files into ISO

# There is an included copy of the GPL, but the source is clearly marked LGPL.
License:        LGPL-2.0-or-later
URL:            http://gregory.kokanosky.free.fr/v4/linux/nrg2iso.en.html
Source0:        http://gregory.kokanosky.free.fr/v4/linux/%{name}-%{version}.tar.gz
Patch0:         nrg2iso-optflags.patch

BuildRequires: make
BuildRequires:  gcc
%description
Nrg2Iso is a linux utility for converting CD (or DVD) image generated by
Nero Burning Rom to ISO format.

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%setup -q
%patch -P0 -p0 -b .optflags

%build
make %{?_smp_mflags} CFLAGS="$RPM_OPT_FLAGS"

%install
rm -rf $RPM_BUILD_ROOT
install -D %{name} $RPM_BUILD_ROOT%{_bindir}/nrg2iso

%files
%doc gpl.txt CHANGELOG
%{_bindir}/*

%changelog
%autochangelog
