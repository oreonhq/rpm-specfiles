%global source0_hash 7b60429dd142c12b2e42e60fd49bb3aa1ec0c8452a1b6989bc2d47a001254ffd

# Generated by go2rpm
%bcond check 1
%global debug_package %{nil}

# https://github.com/linuxdeepin/go-gir-generator
%global goipath         github.com/linuxdeepin/go-gir
Version:                3.0.4

%gometa -L

%global common_description %{expand:
Project go-gir-generator implements GObject-introspection based bindings
generator for Go.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           deepin-gir-generator
Release:        %autorelease
Summary:        GObject-introspection based bindings generator for Go
License:        GPL-3.0-or-later
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
BuildRequires:  pkgconfig(gobject-introspection-1.0)
BuildRequires:  pkgconfig(gdk-3.0)
BuildRequires:  pkgconfig(gudev-1.0)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
# work around issues with GCC 15
export CGO_CFLAGS="%{optflags} -std=gnu17"
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
