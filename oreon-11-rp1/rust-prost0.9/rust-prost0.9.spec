%global source0_hash 444879275cb4fd84958b1a1d5420d15e6fcf7c235fe47f053c9c2a80aceb6001

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate prost

Name:           rust-prost0.9
Version:        0.9.0
Release:        %autorelease
Summary:        A Protocol Buffers implementation for the Rust Language.

License:        Apache-2.0
URL:            https://crates.io/crates/prost
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A Protocol Buffers implementation for the Rust Language.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+no-recursion-limit-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+no-recursion-limit-devel %{_description}

This package contains library source intended for building other packages which
use the "no-recursion-limit" feature of the "%{crate}" crate.

%files       -n %{name}+no-recursion-limit-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+prost-derive-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+prost-derive-devel %{_description}

This package contains library source intended for building other packages which
use the "prost-derive" feature of the "%{crate}" crate.

%files       -n %{name}+prost-derive-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
