%global source0_hash d47d1f906fe2321c1f6629ba8ff040f17f1a7467f104fc6b746ce911c1e26062

# Generated by rust2rpm 28
%bcond check 1

%global crate startup-disk
%global app_id org.startup_disk.StartupDisk

Name:           rust-startup-disk
Version:        0.1.6
Release:        %autorelease
Summary:        Interface to choose the startup volume on Apple Silicon systems

License:        MIT
URL:            https://crates.io/crates/startup-disk
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  make
%if %{with check}
BuildRequires:  desktop-file-utils
BuildRequires:  libappstream-glib
%endif

%global _description %{expand:
Startup Disk provides a simple interface to choose the startup volume on Apple
Silicon Macs running Asahi Linux.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
# Zlib
License:        (Apache-2.0 OR MIT) AND MIT AND (MIT OR Apache-2.0) AND (Unlicense OR MIT) AND Zlib
# LICENSE.dependencies contains a full license breakdown
Requires:       hicolor-icon-theme
Requires:       polkit
Requires:       sudo

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/startup-disk
%{_datadir}/applications/%{app_id}.desktop
%{_datadir}/icons/hicolor/scalable/apps/%{app_id}.svg
%{_datadir}/polkit-1/actions/%{app_id}.policy
%{_metainfodir}/%{app_id}.metainfo.xml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
make install-data DESTDIR="%{buildroot}" DATADIR="%{_datadir}"

%if %{with check}
%check
%cargo_test
make check-data
%endif

%changelog
%autochangelog
