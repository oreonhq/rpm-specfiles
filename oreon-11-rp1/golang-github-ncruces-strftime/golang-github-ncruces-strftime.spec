%global source0_hash 67e1785acddfe20586da40628b74773b467e052dd4e133df0acefd1d6f259bd3

# Generated by go2rpm 1.11.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/ncruces/go-strftime
%global goipath         github.com/ncruces/go-strftime
Version:                0.1.9

%gometa -L -f

%global common_description %{expand:
Strftime/strptime for Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-ncruces-strftime
Release:        %autorelease
Summary:        Strftime/strptime for Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
