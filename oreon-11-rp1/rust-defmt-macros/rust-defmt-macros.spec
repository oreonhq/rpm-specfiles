%global source0_hash bad9c72e7ca2137e0dc3813245a0d282fd6daad32fd800af018306a9169b5fe8

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate defmt-macros

Name:           rust-defmt-macros
Version:        1.1.1
Release:        %autorelease
Summary:        defmt macros

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/defmt-macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
defmt macros}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+unstable-test-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-test-devel %{_description}

This package contains library source intended for building other packages which
use the "unstable-test" feature of the "%{crate}" crate.

%files       -n %{name}+unstable-test-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
