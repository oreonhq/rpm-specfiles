%global source0_hash f2eb25a7ab146f4058d67a74dfea52e25c133c575f08ce5851da97d224e3ad8d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate marked-yaml

Name:           rust-marked-yaml
Version:        0.7.2
Release:        %autorelease
Summary:        A simplified YAML structure with provenance spans

License:        MIT
URL:            https://crates.io/crates/marked-yaml
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A simplified YAML structure with provenance spans}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-path-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-path-devel %{_description}

This package contains library source intended for building other packages which
use the "serde-path" feature of the "%{crate}" crate.

%files       -n %{name}+serde-path-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde_path_to_error-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde_path_to_error-devel %{_description}

This package contains library source intended for building other packages which
use the "serde_path_to_error" feature of the "%{crate}" crate.

%files       -n %{name}+serde_path_to_error-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
