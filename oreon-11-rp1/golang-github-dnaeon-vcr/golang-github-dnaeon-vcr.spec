%global source0_hash ff224fe74a54635b148566bf2ef9f3b440ab927239b4a5549c68a575d2b4a423

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/dnaeon/go-vcr
%global goipath         github.com/dnaeon/go-vcr
Version:                1.1.0

%gometa

%global common_description %{expand:
Go-vcr simplifies testing by recording your HTTP interactions and replaying them
in future runs in order to provide fast, deterministic and accurate testing of
your code.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Record your http interactions for fast, deterministic and accurate tests

# Upstream license specification: BSD-2-Clause
# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(gopkg.in/yaml.v2)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
