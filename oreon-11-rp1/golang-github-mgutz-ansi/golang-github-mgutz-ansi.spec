%global source0_hash 394eb0e1f63e4e8ee099e7cf843f6fd17ab93867b283e2385cb9613b435d57c7

# Generated by go2rpm
%bcond check 0

# https://github.com/mgutz/ansi
%global goipath         github.com/mgutz/ansi
%global commit          d51e80ef957dba7f19388ca64afefbd5a096af30

%gometa

%global common_description %{expand:
Package Ansi is a small, fast library to create ANSI colored strings and codes.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        0.27%{?dist}
Summary:        Small, fast library to create ANSI colored strings and codes
License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/mattn/go-colorable)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license %{golicenses}
%doc %{godocs}
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
