%global source0_hash 3640c1c38b8e4e43584d8df18be5fc6b0aa314ce6ebf51b53313d4306cca8e46

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate is-terminal

Name:           rust-is-terminal
Version:        0.4.17
Release:        %autorelease
Summary:        Test whether a given stream is a terminal

License:        MIT
URL:            https://crates.io/crates/is-terminal
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          is-terminal-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * Drop dev-dependency on unmaintained crate atty
Patch:          is-terminal-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Test whether a given stream is a terminal.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-MIT
%license %{crate_instdir}/LICENSE-MIT-atty
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * Lib tests would require the unmaintained crate atty
%cargo_test -- --doc
%endif

%changelog
%autochangelog
