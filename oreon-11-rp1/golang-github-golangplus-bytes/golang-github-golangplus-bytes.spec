%global source0_hash 1b0116b6b25a8594536c51eb198fd3b22a54b0c4f57861a4e7b75bfbbc8720e6

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/golangplus/bytes
%global goipath         github.com/golangplus/bytes
Version:                1.0.0

%gometa

%global common_description %{expand:
Plus to the standard "bytes" package.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Plus to the standard "bytes" package

# Upstream license specification: BSD-3-Clause
# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/golangplus/testing/assert)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
