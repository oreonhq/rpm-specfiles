diff -up credentials-fetcher-v.1.3.7/api/tests/CMakeLists.txt.eldetect credentials-fetcher-v.1.3.7/api/tests/CMakeLists.txt
--- credentials-fetcher-v.1.3.7/api/tests/CMakeLists.txt.eldetect	2025-07-17 17:45:33.159642664 -0400
+++ credentials-fetcher-v.1.3.7/api/tests/CMakeLists.txt	2025-07-17 17:48:49.647360655 -0400
@@ -7,7 +7,17 @@ else()
     set(IS_FEDORA FALSE)
 endif()
 
-if (NOT IS_FEDORA)
+# Check if the system is a Enterprise Linux (in the Red Hat family)
+# Fedora has a symlink here so this will set true as well, but there's not really a smarter way that I can think of atm
+if(EXISTS "/etc/redhat-release")
+   set(IS_EL TRUE)
+   message(STATUS "/etc/redhat-release found, IS_EL TRUE")
+else()
+   set(IS_EL FALSE)
+   message(STATUS "/etc/redhat-release NOT found, IS_EL FALSE")
+endif()
+
+if (NOT (IS_FEDORA OR IS_EL))
 # set the project name
 project(api-tests)
 
