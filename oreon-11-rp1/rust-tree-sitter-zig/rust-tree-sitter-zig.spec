%global source0_hash ab11fc124851b0db4dd5e55983bbd9631192e93238389dcd44521715e5d53e28

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate tree-sitter-zig

Name:           rust-tree-sitter-zig
Version:        1.1.2
Release:        %autorelease
Summary:        Zig grammar for tree-sitter

License:        MIT
URL:            https://crates.io/crates/tree-sitter-zig
Source:         %{crates_source}
# * LICENSE file not shipped, see
#   https://github.com/tree-sitter-grammars/tree-sitter-zig/commit/b71affffdb4222ff2d2dea6e164f76603b0be6bc
Source1:        https://raw.githubusercontent.com/michel-slm/tree-sitter-zig/b71affffdb4222ff2d2dea6e164f76603b0be6bc/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Zig grammar for tree-sitter.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
cp -p %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install
cp -p LICENSE %{buildroot}%{crate_instdir}/

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
