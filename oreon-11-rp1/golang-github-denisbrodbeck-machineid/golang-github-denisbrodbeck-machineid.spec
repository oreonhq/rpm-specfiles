%global source0_hash 0a01d2b131419cb2b9f7d910a2c90b824449bd91005b47efb235dc707665337a

# Generated by go2rpm 1.3
%bcond check 0

# https://github.com/denisbrodbeck/machineid
%global goipath         github.com/denisbrodbeck/machineid
Version:                1.0.1

%gometa

%global common_description %{expand:
Get the unique machine id of any host (without admin privileges).}

%global golicenses      LICENSE.md
%global godocs          README.md logo.png

Name:           %{goname}
Release:        %autorelease
Summary:        Get the unique machine id of any host (without admin privileges)

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%package -n     machineid
Summary:        %{summary}
%description -n machineid
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files -n machineid
%license LICENSE.md
%doc README.md logo.png
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
