Index: trac/db/postgres_backend.py
===================================================================
--- trac/db/postgres_backend.py	(revision 17860)
+++ trac/db/postgres_backend.py	(revision 17861)
@@ -63,5 +63,5 @@
                         _f.read(), re.VERBOSE)
                     if _match:
-                        _libpq_pathname = _match.group(1)
+                        _libpq_pathname = _match.group(1).decode('utf-8')
                 else:
                     if re.search(r'\0libpq\.dll\0'.encode('utf-8'), _f.read(),
