%global source0_hash afd62b6a104ccbb42b276699e3ad461ad381a02dab147876e3b65a5f97f725ca

# Generated by go2rpm 1.15.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/OpenPrinting/goipp
%global goipath         github.com/OpenPrinting/goipp
Version:                1.2.0

%gometa -L

%global common_description %{expand:
Package goipp implements the IPP core protocol in pure Go (RFC 8010).}

%global golicenses      LICENSE
%global godocs          README.md index.md

Name:           golang-github-openprinting-goipp
Release:        %autorelease
Summary:        Package goipp implements the IPP core protocol in pure Go (RFC 8010)

License:        BSD-2-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%(test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; })
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
* Tue Mar 17 2026 Oreon Packaging Team <packaging@oreonhq.com> - 1.2.0-1
- Prepare for Oreon 11 (RP1)
