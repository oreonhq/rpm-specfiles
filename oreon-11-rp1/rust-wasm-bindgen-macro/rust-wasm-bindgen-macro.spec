%global source0_hash 167ce5e579f6bcf889c4f7175a8a5a585de84e8ff93976ce393efa5f2837aab1

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate wasm-bindgen-macro

Name:           rust-wasm-bindgen-macro
Version:        0.2.126
Release:        %autorelease
Summary:        Definition of the `#[wasm_bindgen]` attribute, an internal dependency

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/wasm-bindgen-macro
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Definition of the `#[wasm_bindgen]` attribute, an internal dependency}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+strict-macro-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+strict-macro-devel %{_description}

This package contains library source intended for building other packages which
use the "strict-macro" feature of the "%{crate}" crate.

%files       -n %{name}+strict-macro-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
