%global source0_hash e92addababc6e94d319606d45fa9ed7fe7b0e0748d8a918ae8acf5b0b87d7889

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/gin-contrib/sse
%global goipath         github.com/gin-contrib/sse
Version:                0.1.0

%gometa

%global common_description %{expand:
Server-sent events (SSE) is a technology where a browser receives automatic
updates from a server via HTTP connection. The Server-Sent Events EventSource
API is standardized as part of HTML5[1] by the W3C.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Server-sent events implementation in Go. used by the gin framework

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/assert)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
