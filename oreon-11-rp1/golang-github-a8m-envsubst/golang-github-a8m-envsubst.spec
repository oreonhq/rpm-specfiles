%global source0_hash 01ca48a2beaeeda5b9fbca9c46bb9e9acc0e55354761d90e4beba5ca1c97aa8b

# Generated by go2rpm 1.6.0
%bcond check 0

# https://github.com/a8m/envsubst
%global goipath         github.com/a8m/envsubst
Version:                1.4.3

%gometa

%global common_description %{expand:
Go package for substituting environment variables.}

%global golicenses      LICENSE
%global godocs          _example README.md

Name:           %{goname}
Release:        6%{?dist}
Summary:        Environment variables substitution for Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%build
# Renamed to goenvsubst so this package doesn't conflict with gettext.
%gobuild -o %{gobuilddir}/bin/goenvsubst %{goipath}/cmd/envsubst

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc _example README.md
%{_bindir}/goenvsubst

%gopkgfiles

%changelog
%autochangelog
