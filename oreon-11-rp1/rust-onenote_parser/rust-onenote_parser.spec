%global source0_hash eeff533445292266865afd73da60b79e748716797634356f1574770b7b1c328b

# Generated by rust2rpm 26
%bcond check 0
%global debug_package %{nil}

# prevent executables from being installed
%global cargo_install_bin 0

%global crate onenote_parser

Name:           rust-onenote_parser
Version:        0.3.1
Release:        %autorelease
Summary:        Parser for Microsoft OneNote files

License:        MPL-2.0
URL:            https://crates.io/crates/onenote_parser
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Adjust versions of dependencies
Patch:          onenote_parser-fix-metadata.diff
# * Upstream bump of dependencies - merged but not yet released
# * Modified to remove Cargo.toml changes since it gets reformatted and drop one
# * https://github.com/msiemens/onenote.rs/pull/9
Patch2:        rust-onenote_parser-updates.patch
# * Apply Cisco-Talos patch as this is really only needed for clamav
Patch3:        https://github.com/msiemens/onenote.rs/pull/13.patch

BuildRequires:  cargo-rpm-macros >= 26

%global _description %{expand:
A parser for Microsoft OneNote files.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+backtrace-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+backtrace-devel %{_description}

This package contains library source intended for building other packages which
use the "backtrace" feature of the "%{crate}" crate.

%files       -n %{name}+backtrace-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
