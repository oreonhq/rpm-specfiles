%global source0_hash ee8f9f5867d00518457d84089e564ba052f860abf18b12a445acbc8f51b0f65e

# Generated by go2rpm 1.15.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/gliderlabs/ssh
%global goipath         github.com/gliderlabs/ssh
Version:                0.3.8

%gometa -L -f

%global common_description %{expand:
Easy SSH servers in Golang.}

%global golicenses      LICENSE
%global godocs          _examples README.md

Name:           golang-github-gliderlabs-ssh
Release:        %autorelease
Summary:        Easy SSH servers in Golang

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
