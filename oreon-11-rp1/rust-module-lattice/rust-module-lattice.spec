%global source0_hash 0c61b87c9683ab7cb1c6871d261ad5479b6b10ceb52c4352aaca3b5d35a8febe

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate module-lattice

Name:           rust-module-lattice
Version:        0.2.3
Release:        %autorelease
Summary:        Functionality shared between the `ml-kem` and `ml-dsa` crates, includ...

License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/module-lattice
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Functionality shared between the `ml-kem` and `ml-dsa` crates, including linear algebra with
degree-256 polynomials over a prime-order field, vectors of such polynomials, and NTT polynomials
and vectors, as well as packing of polynomials into coefficients with a specified number of bits}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ctutils-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ctutils-devel %{_description}

This package contains library source intended for building other packages which
use the "ctutils" feature of the "%{crate}" crate.

%files       -n %{name}+ctutils-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zeroize-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zeroize-devel %{_description}

This package contains library source intended for building other packages which
use the "zeroize" feature of the "%{crate}" crate.

%files       -n %{name}+zeroize-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
