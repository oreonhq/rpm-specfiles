%global source0_hash 2c7dfec993ed0dc1fb94a7c7a8168c3604e932d6a37463fb5a1e95f0cf374f83

# Generated by go2rpm 1.6.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/mattn/go-sqlite3
%global goipath         github.com/mattn/go-sqlite3
Version:                1.14.14

%gometa

%global common_description %{expand:
Package Sqlite3 provides interface to SQLite3 databases.}

%global golicenses      LICENSE
%global godocs          _example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Sqlite3 driver for go using database/sql

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
