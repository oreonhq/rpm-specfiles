%global source0_hash 220044e6a1bb31ddee4e3db724d29767f352de47445a6cd75e1a173142136c83

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate ansitok

Name:           rust-ansitok
Version:        0.2.0
Release:        %autorelease
Summary:        Library for parsing ANSI Escape Codes

License:        MIT
URL:            https://crates.io/crates/ansitok
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Update vte to 0.15; see
#   https://gitlab.com/zhiburt/ansitok/-/commit/43dbaaf4586f0f9cd030e567217c6bd9f81f9fa1,
#   https://gitlab.com/zhiburt/ansitok/-/commit/900d077e16cd2af7d5a0d2d5a08dc2d8d5606e33
Patch:          ansitok-fix-metadata.diff
# * Source-code change for vte >=0.14; part of
#   https://gitlab.com/zhiburt/ansitok/-/commit/900d077e16cd2af7d5a0d2d5a08dc2d8d5606e33
Patch10:        ansitok-0.2.0-vte-0.15.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A library for parsing ANSI Escape Codes.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
