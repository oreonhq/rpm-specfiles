%global source0_hash 3582f63211428f83597b51b2ddb88e2a91a9d52d12831f9d08f5e624e8977422

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate rle-decode-fast

Name:           rust-rle-decode-fast
Version:        1.0.3
Release:        %autorelease
Summary:        Deprecated: this is available in stable Rust since 1.53 as Vec::exten...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/rle-decode-fast
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Deprecated: this is available in stable Rust since 1.53 as Vec::extend_from_within().

Previsouly, the fastest way to implement any kind of decoding for Run Length Encoded data in Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+bench-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+bench-devel %{_description}

This package contains library source intended for building other packages which
use the "bench" feature of the "%{crate}" crate.

%files       -n %{name}+bench-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+criterion-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+criterion-devel %{_description}

This package contains library source intended for building other packages which
use the "criterion" feature of the "%{crate}" crate.

%files       -n %{name}+criterion-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
