%global source0_hash e4c26af3226cee1486004da7ebb1f0b8485df62513cca6671edf7c4d420fadfd

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/go-logfmt/logfmt
%global goipath         github.com/go-logfmt/logfmt
Version:                0.6.0

%gometa -L -f

%global common_description %{expand:
Package logfmt marshals and unmarshals logfmt messages.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           golang-github-logfmt
Release:        %autorelease
Summary:        Package logfmt marshals and unmarshals logfmt messages

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
