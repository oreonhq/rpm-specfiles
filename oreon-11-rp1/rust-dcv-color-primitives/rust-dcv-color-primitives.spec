%global source0_hash 07ad62edfed069700a5b33af6babd29c498d7e33eb01d96ffa8841ee1841634c

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate dcv-color-primitives

Name:           rust-dcv-color-primitives
Version:        0.6.1
Release:        %autorelease
Summary:        a library to perform image color model conversion

License:        MIT-0
URL:            https://crates.io/crates/dcv-color-primitives
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
a library to perform image color model conversion}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+test_instruction_sets-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+test_instruction_sets-devel %{_description}

This package contains library source intended for building other packages which
use the "test_instruction_sets" feature of the "%{crate}" crate.

%files       -n %{name}+test_instruction_sets-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
