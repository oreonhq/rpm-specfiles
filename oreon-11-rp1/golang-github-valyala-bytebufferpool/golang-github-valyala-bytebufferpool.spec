%global source0_hash 089013e3429ebe7fd2bc3527f003bf3f3f639891e5d8ba6a56010e3671465e1f

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/valyala/bytebufferpool
%global goipath         github.com/valyala/bytebufferpool
Version:                1.0.0

%gometa

%global common_description %{expand:
An implementation of a pool of byte buffers with anti-memory-waste protection.

The pool may waste limited amount of memory due to fragmentation. This amount
equals to the maximum total size of the byte buffers in concurrent use.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Anti-memory-waste byte buffer pool

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
