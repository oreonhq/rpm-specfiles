%global source0_hash 301df61f80b6b54b512dfab6b3a1905c83253aeeaf97ad6a7631cd071ee75f13

# Generated by go2rpm 1.3
# Needs go modules
%bcond check 1

# https://github.com/gobuffalo/here
%global goipath         github.com/gobuffalo/here
Version:                0.6.2

%gometa

%global common_description %{expand:
Here will get you accurate Go information about the directory of package
requested.}

%global golicenses      LICENSE
%global godocs          README.md SHOULDERS.md

Name:           %{goname}
Release:        19%{?dist}
Summary:        Get you accurate Go information about the directory of package requested

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/stretchr/testify/require)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md SHOULDERS.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
