%global source0_hash 213241f76fb1e37e27de3b6aa1b068a2c333233b59cca6634f634b80a27ecf1e

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate signal-hook-tokio

Name:           rust-signal-hook-tokio
Version:        0.3.1
Release:        %autorelease
Summary:        Tokio support for signal-hook

# Upstream license specification: Apache-2.0/MIT
License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/signal-hook-tokio
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump serial_test dev-dependency from 0.5 to 3
Patch:          signal-hook-tokio-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Tokio support for signal-hook.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+futures-core-0_3-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+futures-core-0_3-devel %{_description}

This package contains library source intended for building other packages which
use the "futures-core-0_3" feature of the "%{crate}" crate.

%files       -n %{name}+futures-core-0_3-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+futures-v0_3-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+futures-v0_3-devel %{_description}

This package contains library source intended for building other packages which
use the "futures-v0_3" feature of the "%{crate}" crate.

%files       -n %{name}+futures-v0_3-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
