diff -rupN osgearth-osgearth-3.7.2/src/osgEarth/GDAL.cpp osgearth-osgearth-3.7.2-new/src/osgEarth/GDAL.cpp
--- osgearth-osgearth-3.7.2/src/osgEarth/GDAL.cpp	2025-04-03 22:14:59.000000000 +0200
+++ osgearth-osgearth-3.7.2-new/src/osgEarth/GDAL.cpp	2025-11-27 05:55:30.179479981 +0100
@@ -422,7 +422,7 @@ namespace osgEarth
 //...................................................................
 
 
-GDAL::Driver::~Driver()
+osgEarth::GDAL::Driver::~Driver()
 {
     if (_warpedDS)
         GDALClose(_warpedDS);
@@ -431,16 +431,16 @@ GDAL::Driver::~Driver()
 }
 
 void
-GDAL::Driver::setExternalDataset(GDAL::ExternalDataset* value)
+osgEarth::GDAL::Driver::setExternalDataset(GDAL::ExternalDataset* value)
 {
     _externalDataset = value;
 }
 
 // Open the data source and prepare it for reading
 Status
-GDAL::Driver::open(
+osgEarth::GDAL::Driver::open(
     const std::string& name,
-    const GDAL::Options& options,
+    const osgEarth::GDAL::Options& options,
     unsigned tileSize,
     const Profile* fallback_profile,
     DataExtentList* layerDataExtents,
@@ -808,14 +808,14 @@ GDAL::Driver::open(
 }
 
 void
-GDAL::Driver::pixelToGeo(double x, double y, double &geoX, double &geoY)
+osgEarth::GDAL::Driver::pixelToGeo(double x, double y, double &geoX, double &geoY)
 {
     geoX = _geotransform[0] + _geotransform[1] * x + _geotransform[2] * y;
     geoY = _geotransform[3] + _geotransform[4] * x + _geotransform[5] * y;
 }
 
 void
-GDAL::Driver::geoToPixel(double geoX, double geoY, double &x, double &y)
+osgEarth::GDAL::Driver::geoToPixel(double geoX, double geoY, double &x, double &y)
 {
     x = _invtransform[0] + _invtransform[1] * geoX + _invtransform[2] * geoY;
     y = _invtransform[3] + _invtransform[4] * geoX + _invtransform[5] * geoY;
@@ -830,7 +830,7 @@ GDAL::Driver::geoToPixel(double geoX, do
 }
 
 bool
-GDAL::Driver::isValidValue(float v, float noDataValue) const
+osgEarth::GDAL::Driver::isValidValue(float v, float noDataValue) const
 {
     //Check to see if the value is equal to the bands specified no data
     if (noDataValue == v)
@@ -851,7 +851,7 @@ GDAL::Driver::isValidValue(float v, floa
 }
 
 bool
-GDAL::Driver::isValidValue(float v, GDALRasterBand* band) const
+osgEarth::GDAL::Driver::isValidValue(float v, GDALRasterBand* band) const
 {
     float bandNoData = -32767.0f;
     int success;
@@ -880,7 +880,7 @@ GDAL::Driver::isValidValue(float v, GDAL
 }
 
 float
-GDAL::Driver::getInterpolatedDEMValueWorkspace(GDALRasterBand* band, double u, double v, float* data, int width, int height)
+osgEarth::GDAL::Driver::getInterpolatedDEMValueWorkspace(GDALRasterBand* band, double u, double v, float* data, int width, int height)
 {
     float result = 0.0f;
 
@@ -963,7 +963,7 @@ GDAL::Driver::getInterpolatedDEMValueWor
 }
 
 float
-GDAL::Driver::getInterpolatedDEMValue(GDALRasterBand* band, double x, double y, bool applyOffset)
+osgEarth::GDAL::Driver::getInterpolatedDEMValue(GDALRasterBand* band, double x, double y, bool applyOffset)
 {
     double r, c;
     geoToPixel(x, y, c, r);
@@ -1110,13 +1110,13 @@ GDAL::Driver::getInterpolatedDEMValue(GD
 }
 
 bool
-GDAL::Driver::intersects(const TileKey& key)
+osgEarth::GDAL::Driver::intersects(const TileKey& key)
 {
     return key.getExtent().intersects(_extents);
 }
 
 osg::Image*
-GDAL::Driver::createImage(const TileKey& key,
+osgEarth::GDAL::Driver::createImage(const TileKey& key,
     unsigned tileSize,
     bool isCoverage,
     ProgressCallback* progress)
@@ -1535,7 +1535,7 @@ namespace
 
 #if 0
 osg::HeightField*
-GDAL::Driver::createHeightField(const TileKey& key, unsigned tileSize, ProgressCallback* progress)
+osgEarth::GDAL::Driver::createHeightField(const TileKey& key, unsigned tileSize, ProgressCallback* progress)
 {
     if (_maxDataLevel.isSet() && key.getLevelOfDetail() > _maxDataLevel.get())
     {
@@ -1634,7 +1634,7 @@ GDAL::Driver::createHeightField(const Ti
 }
 #else
 osg::HeightField*
-GDAL::Driver::createHeightField(const TileKey& key, unsigned tileSize, ProgressCallback* progress)
+osgEarth::GDAL::Driver::createHeightField(const TileKey& key, unsigned tileSize, ProgressCallback* progress)
 {
     if (_maxDataLevel.isSet() && key.getLevelOfDetail() > _maxDataLevel.get())
     {
@@ -1755,7 +1755,7 @@ GDAL::Driver::createHeightField(const Ti
 #endif
 
 osg::HeightField*
-GDAL::Driver::createHeightFieldWithVRT(const TileKey& key,
+osgEarth::GDAL::Driver::createHeightFieldWithVRT(const TileKey& key,
     unsigned tileSize,
     ProgressCallback* progress)
 {
@@ -1864,13 +1864,13 @@ GDAL::Driver::createHeightFieldWithVRT(c
 }
 //...................................................................
 
-GDAL::Options::Options(const ConfigOptions& input)
+osgEarth::GDAL::Options::Options(const ConfigOptions& input)
 {
     readFrom(input.getConfig());
 }
 
 void
-GDAL::Options::readFrom(const Config& conf)
+osgEarth::GDAL::Options::readFrom(const Config& conf)
 {
     conf.get("url", _url);
     conf.get("connection", _connection);
@@ -1896,7 +1896,7 @@ GDAL::Options::readFrom(const Config& co
 }
 
 void
-GDAL::Options::writeTo(Config& conf) const
+osgEarth::GDAL::Options::writeTo(Config& conf) const
 {
     conf.set("url", _url);
     conf.set("connection", _connection);
@@ -1921,12 +1921,12 @@ namespace
     template<typename T>
     Status openOnThisThread(
         const T* layer,
-        GDAL::Driver::Ptr& driver,
+        osgEarth::GDAL::Driver::Ptr& driver,
         osg::ref_ptr<const Profile>* in_out_profile,
         DataExtentList* out_dataExtents,
         bool verbose)
     {
-        driver = std::make_shared<GDAL::Driver>();
+        driver = std::make_shared<osgEarth::GDAL::Driver>();
 
         if (layer->options().noDataValue().isSet())
             driver->setNoDataValue(layer->options().noDataValue().get());
@@ -2023,7 +2023,7 @@ GDALImageLayer::openImplementation()
 
     // Note: no need to mutex the _driverSingleThreaded instance since we are in open
     // and open is single-threaded by definition.
-    GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded : _driverPerThread.get();
+    osgEarth::GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded : _driverPerThread.get();
 
     DataExtentList dataExtents;
 
@@ -2071,7 +2071,7 @@ GDALImageLayer::createImageImplementatio
     if (!isOpen())
         return GeoImage::INVALID;
 
-    GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded : _driverPerThread.get();
+    osgEarth::GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded : _driverPerThread.get();
 
     if (driver == nullptr)
     {
@@ -2130,7 +2130,7 @@ void GDALElevationLayer::setSingleThread
 bool GDALElevationLayer::getSingleThreaded() const { return options().singleThreaded().get(); }
 
 void
-GDALElevationLayer::setExternalDataset(GDAL::ExternalDataset* value)
+GDALElevationLayer::setExternalDataset(osgEarth::GDAL::ExternalDataset* value)
 {
     //_driver->setExternalDataset(value);
     OE_WARN << LC << "setExternalDataset NOT IMPLEMENTED" << std::endl;
@@ -2156,7 +2156,7 @@ GDALElevationLayer::openImplementation()
     // https://trac.osgeo.org/gdal/wiki/FAQMiscellaneous#IstheGDALlibrarythread-safe
 
     // Open the dataset temporarily to query the profile and extents.
-    GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded :  _driverPerThread.get();
+    osgEarth::GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded :  _driverPerThread.get();
 
     DataExtentList dataExtents;
 
@@ -2205,7 +2205,7 @@ GDALElevationLayer::createHeightFieldImp
     if (!isOpen())
         return GeoHeightField::INVALID;
 
-    GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded : _driverPerThread.get();
+    osgEarth::GDAL::Driver::Ptr& driver = getSingleThreaded() ? _driverSingleThreaded : _driverPerThread.get();
 
     if (driver == nullptr)
     {
