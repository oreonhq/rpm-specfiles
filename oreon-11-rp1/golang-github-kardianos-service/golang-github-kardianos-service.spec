%global source0_hash 81643f8ef06b75718f7071830018f1fc403d03c79fef15ef70580ead8aa13eab

# Generated by go2rpm
%bcond check 0

# https://github.com/kardianos/service
%global goipath         github.com/kardianos/service
Version:                1.2.4
%global debug_package   %{nil}

%gometa

%global common_description %{expand:
service will install / un-install, start / stop, and run a program as a
service (daemon). Currently supports Windows XP+, Linux/(systemd | Upstart |
SysV), and OSX/Launchd.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        %{autorelease}
Summary:        Run go programs as a service on major platforms

# Upstream license specification: Zlib
License:        zlib
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck -d .
%endif

%gopkgfiles

%changelog
%autochangelog
