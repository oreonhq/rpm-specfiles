%global source0_hash fc0d2e3286aa5a15d93cccddef56661de8ca7a0e229f08d86edacabbe61e9ec0

# Generated by go2rpm 1.3
# Tests work fine locally but seem to randomly fail in koji
%bcond check 1
%global debug_package %{nil}

# https://github.com/cornelk/hashmap
%global goipath         github.com/cornelk/hashmap
Version:                1.0.1

%gometa

%global common_description %{expand:
A Golang lock-free thread-safe HashMap optimized for fastest read access.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        A Golang lock-free thread-safe HashMap optimized for fastest read access

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/dchest/siphash)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
