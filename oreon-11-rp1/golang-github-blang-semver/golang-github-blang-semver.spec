%global source0_hash 873e979323df6060cb4f843bc920f07fa59c05002359bf5d4a3311c8911f6640

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/blang/semver
%global goipath         github.com/blang/semver
Version:                4.0.0

%gometa

%global common_description %{expand:
Semver is a Semantic Versioning library written in Go. It fully covers spec
version 2.0.0.}

%global golicenses      LICENSE
%global godocs          examples README.md

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Release:        %autorelease
Summary:        Semantic Versioning library written in Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
