diff -ruN picasso-2.8.0/CMakeLists.txt picasso-2.8.0-mod3/CMakeLists.txt
--- picasso-2.8.0/CMakeLists.txt	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/CMakeLists.txt	2025-04-02 09:29:34.096276473 +0300
@@ -31,6 +31,12 @@
 	set(CMAKE_BUILD_TYPE Debug CACHE STRING "Build Type" FORCE)
 endif()
 
+find_library(ZLIB z)
+find_library(PNG png)
+find_library(JPEG jpeg)
+find_library(GIF gif)
+find_library(WEBP webp)
+
 include (${CMAKE_CURRENT_LIST_DIR}/build/configs.cmake)
 include (${CMAKE_CURRENT_LIST_DIR}/build/defines.cmake)
 include (${CMAKE_CURRENT_LIST_DIR}/third_party/third_party.cmake)
diff -ruN picasso-2.8.0/ext/image_loader/gif/gif.cmake picasso-2.8.0-mod3/ext/image_loader/gif/gif.cmake
--- picasso-2.8.0/ext/image_loader/gif/gif.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/ext/image_loader/gif/gif.cmake	2025-04-02 14:36:52.087245359 +0300
@@ -15,8 +15,12 @@
 
 add_definitions(-DEXPORT)
 add_library(${LIBX_GIF} ${PXGIF_SOURCES})
+if(OPT_SYSTEM_GIF AND GIF)
+target_link_libraries(${LIBX_GIF} PRIVATE ${GIF})
+else()
 target_link_libraries(${LIBX_GIF} PRIVATE gif)
-install(TARGETS ${LIBX_GIF} LIBRARY DESTINATION lib/modules ARCHIVE DESTINATION lib/modules)
+endif()
+install(TARGETS ${LIBX_GIF} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso)
 
 set_target_properties(${LIBX_GIF}
     PROPERTIES
diff -ruN picasso-2.8.0/ext/image_loader/image.cmake picasso-2.8.0-mod3/ext/image_loader/image.cmake
--- picasso-2.8.0/ext/image_loader/image.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/ext/image_loader/image.cmake	2025-04-02 11:48:09.342006654 +0300
@@ -28,7 +28,8 @@
 
 add_definitions(-DEXPORT)
 add_library(${LIBX_IMAGE} ${PXIMG_SOURCES})
-install(TARGETS ${LIBX_IMAGE} LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
+include(GNUInstallDirs)
+install(TARGETS ${LIBX_IMAGE} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR} ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
 set_target_properties(${LIBX_IMAGE} PROPERTIES VERSION ${VERSION_INFO} SOVERSION 1)
 
 include_directories(${PXIMG_DIR} ${PROJECT_ROOT}/include)
diff -ruN picasso-2.8.0/ext/image_loader/jpeg/jpeg.cmake picasso-2.8.0-mod3/ext/image_loader/jpeg/jpeg.cmake
--- picasso-2.8.0/ext/image_loader/jpeg/jpeg.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/ext/image_loader/jpeg/jpeg.cmake	2025-04-02 14:39:10.368363908 +0300
@@ -15,8 +15,12 @@
 
 add_definitions(-DEXPORT)
 add_library(${LIBX_JPEG} ${PXJPEG_SOURCES})
+if(OPT_SYSTEM_JPEG AND JPEG)
+target_link_libraries(${LIBX_JPEG} PRIVATE ${JPEG})
+else()
 target_link_libraries(${LIBX_JPEG} PRIVATE jpeg)
-install(TARGETS ${LIBX_JPEG} LIBRARY DESTINATION lib/modules ARCHIVE DESTINATION lib/modules)
+endif()
+install(TARGETS ${LIBX_JPEG} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso)
 
 set_target_properties(${LIBX_JPEG}
     PROPERTIES
diff -ruN picasso-2.8.0/ext/image_loader/png/png.cmake picasso-2.8.0-mod3/ext/image_loader/png/png.cmake
--- picasso-2.8.0/ext/image_loader/png/png.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/ext/image_loader/png/png.cmake	2025-04-02 14:37:38.846979980 +0300
@@ -15,8 +15,12 @@
 
 add_definitions(-DEXPORT)
 add_library(${LIBX_PNG} ${PXPNG_SOURCES})
+if(OPT_SYSTEM_PNG AND PNG)
+target_link_libraries(${LIBX_PNG} PRIVATE ${PNG})
+else()
 target_link_libraries(${LIBX_PNG} PRIVATE png)
-install(TARGETS ${LIBX_PNG} LIBRARY DESTINATION lib/modules ARCHIVE DESTINATION lib/modules)
+endif()
+install(TARGETS ${LIBX_PNG} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso)
 
 set_target_properties(${LIBX_PNG}
     PROPERTIES
diff -ruN picasso-2.8.0/ext/image_loader/webp/webp.cmake picasso-2.8.0-mod3/ext/image_loader/webp/webp.cmake
--- picasso-2.8.0/ext/image_loader/webp/webp.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/ext/image_loader/webp/webp.cmake	2025-04-02 14:38:21.183989976 +0300
@@ -15,8 +15,12 @@
 
 add_definitions(-DEXPORT)
 add_library(${LIBX_WEBP} ${PXWEBP_SOURCES})
+if(OPT_SYSTEM_WEBP AND WEBP})
+target_link_libraries(${LIBX_WEBP} PRIVATE ${WEBP})
+else()
 target_link_libraries(${LIBX_WEBP} PRIVATE webp)
-install(TARGETS ${LIBX_WEBP} LIBRARY DESTINATION lib/modules ARCHIVE DESTINATION lib/modules)
+endif()
+install(TARGETS ${LIBX_WEBP} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}/picasso)
 
 set_target_properties(${LIBX_WEBP}
     PROPERTIES
diff -ruN picasso-2.8.0/src/src.cmake picasso-2.8.0-mod3/src/src.cmake
--- picasso-2.8.0/src/src.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/src/src.cmake	2025-04-02 22:12:26.212226667 +0300
@@ -30,7 +30,8 @@
 
 add_definitions(-DEXPORT)
 add_library(${LIB_NAME} ${SOURCES})
-install(TARGETS ${LIB_NAME} LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
+include(GNUInstallDirs)
+install(TARGETS ${LIB_NAME} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR} ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
 
 if (UNIX AND NOT APPLE)
     find_package(Freetype REQUIRED)
diff -ruN picasso-2.8.0/third_party/libpng.cmake picasso-2.8.0-mod3/third_party/libpng.cmake
--- picasso-2.8.0/third_party/libpng.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/third_party/libpng.cmake	2025-04-02 10:39:23.923866942 +0300
@@ -32,5 +32,12 @@
 install(TARGETS png LIBRARY DESTINATION lib ARCHIVE DESTINATION lib)
 
 include_directories(${PNG_DIR} ${CMAKE_CURRENT_BINARY_DIR}/include)
-target_link_libraries(png PRIVATE zlib)
 
+if(ZLIB AND OPT_SYSTEM_ZLIB)
+if(NOT OPT_SYSTEM_ZLIB)
+message("System zlib not found, building internal zlib")
+endif()
+target_link_libraries(png PRIVATE ${ZLIB})
+else()
+target_link_libraries(png PRIVATE zlib)
+endif()
diff -ruN picasso-2.8.0/third_party/third_party.cmake picasso-2.8.0-mod3/third_party/third_party.cmake
--- picasso-2.8.0/third_party/third_party.cmake	2024-07-15 13:12:46.000000000 +0300
+++ picasso-2.8.0-mod3/third_party/third_party.cmake	2025-04-02 09:47:48.791801655 +0300
@@ -4,9 +4,38 @@
 # Contact: onecoolx@gmail.com
 
 if (NOT APPLE)
+if((NOT ZLIB) OR (NOT OPT_SYSTEM_ZLIB))
+if(NOT ZLIB)
+message("System zlib not found, building internal zlib")
+endif()
 include (${CMAKE_CURRENT_LIST_DIR}/zlib.cmake)
+endif()
+
+if((NOT PNG) OR (NOT OPT_SYSTEM_PNG))
+if(NOT PNG)
+message("System libpng not found, building internal libpng")
+endif()
 include (${CMAKE_CURRENT_LIST_DIR}/libpng.cmake)
+endif()
+
+if((NOT JPEG) OR (NOT OPT_SYSTEM_JPEG))
+if(NOT JPEG)
+message("System libjpeg not found, building internal libjpeg")
+endif()
 include (${CMAKE_CURRENT_LIST_DIR}/libjpeg.cmake)
+endif()
+
+if((NOT GIF) OR (NOT OPT_SYSTEM_GIF))
+if(NOT GIF)
+message("System giflib not found, building internal giflib")
+endif()
 include (${CMAKE_CURRENT_LIST_DIR}/giflib.cmake)
+endif()
+
+if((NOT WEBP) OR (NOT OPT_SYSTEM_WEBP))
+if(NOT WEBP)
+message("System libwebp not found, building internal libwebp")
+endif()
 include (${CMAKE_CURRENT_LIST_DIR}/libwebp.cmake)
 endif()
+endif()
