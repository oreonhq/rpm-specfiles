%global source0_hash d4e7e0360395852f1f6ff5b7b82c72dc6557d181073188df1d60ec469ea69c66

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate tree-sitter-verilog

Name:           rust-tree-sitter-verilog
Version:        1.0.3
Release:        %autorelease
Summary:        Verilog grammar for tree-sitter

License:        MIT
URL:            https://crates.io/crates/tree-sitter-verilog
Source:         %{crates_source}
# * LICENSE file not shipped, see
#   https://github.com/tree-sitter/tree-sitter-verilog/pull/83
Source1:        https://raw.githubusercontent.com/tree-sitter/tree-sitter-verilog/refs/tags/v%{version}/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Verilog grammar for tree-sitter.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
cp -p %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install
cp -p LICENSE %{buildroot}%{crate_instdir}/

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
