%global source0_hash 7ab85b9b05e3978cc9a9cf8fea7f01b494e1a09ed3037e16ba39edc7a29eb61a

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate fst

Name:           rust-fst
Version:        0.4.7
Release:        %autorelease
Summary:        Use finite state transducers to compactly represents sets or maps of...

License:        Unlicense OR MIT
URL:            https://crates.io/crates/fst
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Use finite state transducers to compactly represents sets or maps of many
strings (> 1 billion is possible).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/COPYING
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+levenshtein-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+levenshtein-devel %{_description}

This package contains library source intended for building other packages which
use the "levenshtein" feature of the "%{crate}" crate.

%files       -n %{name}+levenshtein-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+utf8-ranges-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+utf8-ranges-devel %{_description}

This package contains library source intended for building other packages which
use the "utf8-ranges" feature of the "%{crate}" crate.

%files       -n %{name}+utf8-ranges-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
