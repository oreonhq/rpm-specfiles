%global source0_hash b8a38e961a0e805c9c0f5b53e6fe9245273345701d93b1588bb22b2c56abdb46

# Generated by go2rpm 1.17.1
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/mautrix/go-util
%global goipath         go.mau.fi/util
%global forgeurl        https://github.com/mautrix/go-util
Version:                0.9.0

%gometa -L -f

%global common_description %{expand:
Various Go utilities used by mautrix-go and bridges.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md base58/README.md

Name:           golang-mau-util
Release:        %autorelease
Summary:        Various Go utilities used by mautrix-go and bridges

License:        MPL-2.0
URL:            %{gourl}
Source:         %{gosource}
Patch:          0001-Disable-tests-fetching-online-resources.patch

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%if %{without bootstrap}
%files
%license LICENSE
%doc CHANGELOG.md README.md base58/README.md
%endif

%gopkgfiles

%changelog
%autochangelog
