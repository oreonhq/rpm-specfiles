%global source0_hash 5066dde7ebf5ca84504c9535cf35bdb184f5262f53d67b9b4708c27a3e506e30

# Generated by go2rpm
%bcond check 0

# https://github.com/gucumber/gucumber
%global goipath         github.com/gucumber/gucumber
%global commit          7d5c79e832a25825dbed83e78f8484d4ffe8ec16

%gometa

# Remove in F33:
%global godevelheader %{expand:
Obsoletes:      golang-github-lsegal-gucumber-devel < 0-0.14
}

%global goaltipaths     github.com/lsegal/gucumber

%global common_description %{expand:
An implementation of Cucumber BDD-style testing for Go.}

%global golicenses      LICENSE.txt
%global godocs          README.md

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Version:        0
Release:        0.31%{?dist}
Summary:        Cucumber BDD-style testing for Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

BuildRequires:  golang(github.com/shiena/ansicolor)
BuildRequires:  golang(github.com/stretchr/testify/assert)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

# Remove in F33
# Remove erroneous glide.lock folder
%pretrans devel -p <lua>
path = "%{gopath}/src/%{goipath}/glide.lock"
st = posix.stat(path)
if st and st.type == "directory" then
  os.remove(path)
end

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE.txt
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
