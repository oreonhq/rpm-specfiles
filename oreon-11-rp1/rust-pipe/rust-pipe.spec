%global source0_hash 1c7b8f27da217eb966df4c58d4159ea939431950ca03cf782c22bd7c5c1d8d75

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate pipe

Name:           rust-pipe
Version:        0.4.0
Release:        %autorelease
Summary:        Synchronous Read/Write memory pipe

License:        MIT
URL:            https://crates.io/crates/pipe
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Synchronous Read/Write memory pipe}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/COPYING
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+bidirectional-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+bidirectional-devel %{_description}

This package contains library source intended for building other packages which
use the "bidirectional" feature of the "%{crate}" crate.

%files       -n %{name}+bidirectional-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+readwrite-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+readwrite-devel %{_description}

This package contains library source intended for building other packages which
use the "readwrite" feature of the "%{crate}" crate.

%files       -n %{name}+readwrite-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unstable-doc-cfg-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-doc-cfg-devel %{_description}

This package contains library source intended for building other packages which
use the "unstable-doc-cfg" feature of the "%{crate}" crate.

%files       -n %{name}+unstable-doc-cfg-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
