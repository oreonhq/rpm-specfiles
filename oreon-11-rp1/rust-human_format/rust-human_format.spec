%global source0_hash eaec953f16e5bcf6b8a3cb3aa959b17e5577dbd2693e94554c462c08be22624b

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate human_format

Name:           rust-human_format
Version:        1.2.1
Release:        %autorelease
Summary:        Rust Port of human-format from node, formatting numbers for us, while...

License:        MIT
URL:            https://crates.io/crates/human_format
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Rust Port of human-format from node, formatting numbers for us, while the machines are still at bay.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/changelog.md
%{crate_instdir}/

%package     -n %{name}+panic_parse-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+panic_parse-devel %{_description}

This package contains library source intended for building other packages which
use the "panic_parse" feature of the "%{crate}" crate.

%files       -n %{name}+panic_parse-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
