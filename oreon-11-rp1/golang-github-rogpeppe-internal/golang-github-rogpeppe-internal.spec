%global source0_hash b51d588d05f3e82d3045545ef46bd758a0610b9cfde1ae243489a593908b6060

# Generated by go2rpm 1.10.0
%bcond check 1

# https://github.com/rogpeppe/go-internal
%global goipath         github.com/rogpeppe/go-internal
Version:                1.12.0

%gometa -L

%global common_description %{expand:
Selected Go-internal packages factored out from the standard library.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-rogpeppe-internal
Release:        %autorelease
Summary:        Selected Go-internal packages factored out from the standard library

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
for test in "TestScripts" "TestSimple" "TestScan" "TestTestwork" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/testscript
%{_bindir}/txtar-addmod
%{_bindir}/txtar-c
%{_bindir}/txtar-goproxy
%{_bindir}/txtar-x

%gopkgfiles

%changelog
%autochangelog
