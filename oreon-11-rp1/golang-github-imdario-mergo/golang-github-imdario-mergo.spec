%global source0_hash 0cfd6c6d112688aea782726ef90379065e12632d27d886c3b1d9bd4481304652

# Generated by go2rpm 1.8.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/imdario/mergo
%global goipath         github.com/imdario/mergo
Version:                0.3.13

%gometa

%global common_description %{expand:
A helper to merge structs and maps in Golang. Useful for configuration default
values, avoiding messy if-statements.}

%global golicenses      LICENSE
%global godocs          CODE_OF_CONDUCT.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Tool to merge Go structs and maps

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

BuildRequires:  golang(gopkg.in/yaml.v3)

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
