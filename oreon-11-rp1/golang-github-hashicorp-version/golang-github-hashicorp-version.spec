%global source0_hash b8cc8cc8d04030fe8c91e3df5ec299f48298629456ca578c852a662200bdb43c

# Generated by go2rpm 1.17.1
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/hashicorp/go-version
%global goipath         github.com/hashicorp/go-version
Version:                1.7.0

%gometa -L -f

%global common_description %{expand:
Go-version is a library for parsing versions and version constraints, and
verifying versions against a set of constraints. go-version can sort a
collection of versions properly, handles prerelease/beta versions, can increment
versions, etc.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           golang-github-hashicorp-version
Release:        %autorelease
Summary:        Go library for parsing and verifying versions and version constraints

License:        MPL-2.0
URL:            %{gourl}
Source:         %{gosource}
# Fix string formatting with Go 1.24.
Patch:          %{gourl}/pull/151.patch

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
