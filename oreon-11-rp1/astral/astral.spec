%global source0_hash b4024301c4abec366f2131062363cbe3503c97a7e8c03e6be2342111e4335982

# Generated by go2rpm 1.6.0
%bcond check 0

# https://github.com/sj14/astral
%global goipath         github.com/sj14/astral
Version:                0.2.2

%gometa

%global common_description %{expand:
Go calculations for the position of the sun and moon.}

%global golicenses      LICENSE
%global godocs          AUTHORS README.md

Name:           astral
Release:        %autorelease
Summary:        Go calculations for the position of the sun and moon

License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc AUTHORS README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
