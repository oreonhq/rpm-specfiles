%global source0_hash bb7d90bef01670c55ac953c7bcfed1c80e20262c248ed62b618af1ab022c5120

# Generated by go2rpm 1
%bcond check 0

# https://github.com/rickb777/date
%global goipath         github.com/rickb777/date
Version:                1.20.5

%gometa

%global common_description %{expand:
A Go package for working with dates.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        A Go package for working with dates

# Upstream license specification: BSD-3-Clause
License:        BSD-3-Clause
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/rickb777/plural)
BuildRequires:  golang(golang.org/x/text/language)
BuildRequires:  golang(golang.org/x/text/message)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/onsi/gomega)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in datetool; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
