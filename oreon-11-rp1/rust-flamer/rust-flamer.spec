%global source0_hash 36b732da54fd4ea34452f2431cf464ac7be94ca4b339c9cd3d3d12eb06fe7aab

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate flamer

Name:           rust-flamer
Version:        0.4.0
Release:        %autorelease
Summary:        a procedural macro to insert `flame::start_guard(_)` calls

License:        Apache-2.0
URL:            https://crates.io/crates/flamer
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
a procedural macro to insert `flame::start_guard(_)` calls}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+test-nightly-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+test-nightly-devel %{_description}

This package contains library source intended for building other packages which
use the "test-nightly" feature of the "%{crate}" crate.

%files       -n %{name}+test-nightly-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
