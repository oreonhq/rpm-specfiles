%global source0_hash 054ccb5b10f9f2cbf51eb355ca1d05c2d279ce1804688d0db74b4733a5aeafd8

# Generated by rust2rpm 28
# * skip checks in mock / awkward deps
%bcond check 0
%global debug_package %{nil}

%global crate console

Name:           rust-console0.15
Version:        0.15.11
Release:        %autorelease
Summary:        A terminal and console abstraction for Rust

License:        MIT
URL:            https://crates.io/crates/console
Source:         %{crates_source}
# Automatically generated patch to strip foreign dependencies
Patch:          console-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A terminal and console abstraction for Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ansi-parsing-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ansi-parsing-devel %{_description}

This package contains library source intended for building other packages which
use the "ansi-parsing" feature of the "%{crate}" crate.

%files       -n %{name}+ansi-parsing-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+windows-console-colors-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+windows-console-colors-devel %{_description}

This package contains library source intended for building other packages which
use the "windows-console-colors" feature of the "%{crate}" crate.

%files       -n %{name}+windows-console-colors-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
