%global source0_hash 586b2b42a4165ac52b35808be1238f90d109789bfcb1ddecd3f512eddbac0a9f

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/fatih/color
%global goipath         github.com/fatih/color
Version:                1.18.0

%gometa -L -f

%global common_description %{expand:
Color lets you use colorized outputs in terms of ANSI Escape Codes in Go. It has
support for Windows too! The API can be used in several ways, pick one that
suits you.}

%global golicenses      LICENSE.md
%global godocs          README.md

Name:           golang-github-fatih-color
Release:        %autorelease
Summary:        Color package for Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
