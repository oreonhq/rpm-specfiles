%global source0_hash e8ee224abc82b72be648384d8fb843e45f912a3832a608830f8dd07858fd0476

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/creack/goselect
%global goipath         github.com/creack/goselect
Version:                0.1.3

%gometa

%global common_description %{expand:
Select(2) implementation in Go.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Select(2) implementation in Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
