%global source0_hash 0398022d5f700414f6b899e10b8348231abf9173fa93144cbc1a43b9793c1fbc

# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate unic-char-range

Name:           rust-unic-char-range
Version:        0.9.0
Release:        %autorelease
Summary:        UNIC — Unicode Character Tools — Character Range and Iteration

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/unic-char-range
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
UNIC — Unicode Character Tools — Character Range and Iteration}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+exact-size-is-empty-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+exact-size-is-empty-devel %{_description}

This package contains library source intended for building other packages which
use the "exact-size-is-empty" feature of the "%{crate}" crate.

%files       -n %{name}+exact-size-is-empty-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fused-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fused-devel %{_description}

This package contains library source intended for building other packages which
use the "fused" feature of the "%{crate}" crate.

%files       -n %{name}+fused-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+trusted-len-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+trusted-len-devel %{_description}

This package contains library source intended for building other packages which
use the "trusted-len" feature of the "%{crate}" crate.

%files       -n %{name}+trusted-len-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unstable-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-devel %{_description}

This package contains library source intended for building other packages which
use the "unstable" feature of the "%{crate}" crate.

%files       -n %{name}+unstable-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
