%global source0_hash 0348a1c054491f4bfe6ab86a7b6ab1e44e45d899005de92f58b3df180b36ddaf

# Generated by rust2rpm 27
# * unit tests require a running display server
%bcond check 0
%global debug_package %{nil}

%global crate arboard

Name:           rust-arboard
Version:        3.6.1
Release:        %autorelease
Summary:        Image and text handling for the OS clipboard

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/arboard
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          arboard-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * drop Windows- and macOS-specifc features
Patch:          arboard-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Image and text handling for the OS clipboard.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE.txt
%license %{crate_instdir}/LICENSE-MIT.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+image-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+image-devel %{_description}

This package contains library source intended for building other packages which
use the "image" feature of the "%{crate}" crate.

%files       -n %{name}+image-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+image-data-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+image-data-devel %{_description}

This package contains library source intended for building other packages which
use the "image-data" feature of the "%{crate}" crate.

%files       -n %{name}+image-data-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+wayland-data-control-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+wayland-data-control-devel %{_description}

This package contains library source intended for building other packages which
use the "wayland-data-control" feature of the "%{crate}" crate.

%files       -n %{name}+wayland-data-control-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+wl-clipboard-rs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+wl-clipboard-rs-devel %{_description}

This package contains library source intended for building other packages which
use the "wl-clipboard-rs" feature of the "%{crate}" crate.

%files       -n %{name}+wl-clipboard-rs-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
