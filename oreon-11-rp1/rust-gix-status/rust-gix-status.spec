%global source0_hash ed0d94c685a831c679ca5454c22f350e8c233f50dcf377ca00d858bcba9696d2

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate gix-status

Name:           rust-gix-status
Version:        0.24.0
Release:        %autorelease
Summary:        A crate of the gitoxide project dealing with 'git status'-like functi...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/gix-status
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A crate of the gitoxide project dealing with 'git status'-like functionality}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT

%{crate_instdir}/

%package     -n %{name}+document-features-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+document-features-devel %{_description}

This package contains library source intended for building other packages which
use the "document-features" feature of the "%{crate}" crate.

%files       -n %{name}+document-features-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gix-diff-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gix-diff-devel %{_description}

This package contains library source intended for building other packages which
use the "gix-diff" feature of the "%{crate}" crate.

%files       -n %{name}+gix-diff-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+gix-dir-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+gix-dir-devel %{_description}

This package contains library source intended for building other packages which
use the "gix-dir" feature of the "%{crate}" crate.

%files       -n %{name}+gix-dir-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+worktree-rewrites-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+worktree-rewrites-devel %{_description}

This package contains library source intended for building other packages which
use the "worktree-rewrites" feature of the "%{crate}" crate.

%files       -n %{name}+worktree-rewrites-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
