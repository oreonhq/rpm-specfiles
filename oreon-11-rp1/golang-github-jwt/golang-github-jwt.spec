%global source0_hash 4cd18a4bb5d949244f137fed0884f93695ff268cb9efaba71ddcf4c50b17efe0

# Generated by go2rpm 1.5.0
%bcond check 0

# https://github.com/golang-jwt/jwt
%global goipath         github.com/golang-jwt/jwt
Version:                3.2.2

%gometa

%global common_description %{expand:
A go implementation of JSON Web Tokens. Supports the parsing and
verification, as well as the generation and signing of JWTs.}

%global golicenses      LICENSE
%global godocs          MIGRATION_GUIDE.md README.md VERSION_HISTORY.md

Name:           %{goname}
Release:        %autorelease
Summary:        A go implementation of JSON Web Tokens

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

install -m 0755 -vd                     %{buildroot}%{_defaultdocdir}/%{goname}
install -m 0644 -vp cmd/jwt/README.md   %{buildroot}%{_defaultdocdir}/%{goname}/README-jwt.md

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc MIGRATION_GUIDE.md README.md VERSION_HISTORY.md README-jwt.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
