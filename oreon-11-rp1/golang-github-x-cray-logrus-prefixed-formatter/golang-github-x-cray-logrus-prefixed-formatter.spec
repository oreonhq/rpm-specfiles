%global source0_hash a966d81bbaef60a98073b589c2054f0171ccc09539589b71c30c936009fbb164

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/x-cray/logrus-prefixed-formatter
%global goipath         github.com/x-cray/logrus-prefixed-formatter
Version:                0.5.2

%gometa -f

%global common_description %{expand:
Logrus Prefixed Log Formatter.}

%global golicenses      LICENSE
%global godocs          examples README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Logrus Prefixed Log Formatter

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%if 0%{?fc37}
# The dependency generation isn't picking these up
BuildRequires:  golang(github.com/onsi/ginkgo)
BuildRequires:  golang(github.com/onsi/gomega)
%endif

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
