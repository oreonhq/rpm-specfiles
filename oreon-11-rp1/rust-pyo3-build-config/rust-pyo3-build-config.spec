%global source0_hash c5e2a7d2f0d013342f295c048ad19237add5154a55b1c5a254c0ec93d4109078

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate pyo3-build-config

Name:           rust-pyo3-build-config
Version:        0.29.0
Release:        %autorelease
Summary:        Build configuration for the PyO3 ecosystem

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/pyo3-build-config
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Build configuration for the PyO3 ecosystem}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT

%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+extension-module-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+extension-module-devel %{_description}

This package contains library source intended for building other packages which
use the "extension-module" feature of the "%{crate}" crate.

%files       -n %{name}+extension-module-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+generate-import-lib-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+generate-import-lib-devel %{_description}

This package contains library source intended for building other packages which
use the "generate-import-lib" feature of the "%{crate}" crate.

%files       -n %{name}+generate-import-lib-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+resolve-config-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+resolve-config-devel %{_description}

This package contains library source intended for building other packages which
use the "resolve-config" feature of the "%{crate}" crate.

%files       -n %{name}+resolve-config-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
