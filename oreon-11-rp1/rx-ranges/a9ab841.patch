From 5f18097d496cafddf624c4554d1c8f6d32e6ccfb Mon Sep 17 00:00:00 2001
From: Hazurl <ricoujules69@gmail.com>
Date: Mon, 25 Nov 2019 14:33:50 +0100
Subject: [PATCH 1/3] Remove default-constructibility requirement from
 ChainRange

---
 include/rx/ranges.hpp | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)

diff --git a/include/rx/ranges.hpp b/include/rx/ranges.hpp
index 18fbdbb..f144ba2 100644
--- a/include/rx/ranges.hpp
+++ b/include/rx/ranges.hpp
@@ -8,6 +8,7 @@
 #include <set>
 #include <type_traits>
 #include <vector>
+#include <cstdlib>
 
 // This override is provided to avoid name clashes in foreign codebases where `rx` already has a
 // different meaning.
@@ -2048,11 +2049,13 @@ struct ChainRange {
     }
 
 private:
+    [[noreturn]]
     constexpr output_type _get(std::index_sequence<>) const {
         RX_ASSERT(false); // chain was at end!
-        return output_type{};
+        std::abort();
     }
 
+
     template <size_t i, size_t... rest>
     constexpr output_type _get(std::index_sequence<i, rest...>) const {
         if (i == current_input) {

From bb750c9d667ec447fd40b83a574366274d82a42f Mon Sep 17 00:00:00 2001
From: Hazurl <ricoujules69@gmail.com>
Date: Mon, 25 Nov 2019 14:34:08 +0100
Subject: [PATCH 2/3] Add test for ChainRange

---
 test/test_ranges.cpp | 31 +++++++++++++++++++++++++++++++
 1 file changed, 31 insertions(+)

diff --git a/test/test_ranges.cpp b/test/test_ranges.cpp
index 1d8aca5..7339569 100644
--- a/test/test_ranges.cpp
+++ b/test/test_ranges.cpp
@@ -566,6 +566,37 @@ TEST_CASE("ranges chain") {
     CHECK(homogenous_actual == homogenous_expected);
 }
 
+struct NoDefaultConstruction {
+    NoDefaultConstruction() = delete;
+    NoDefaultConstruction(int i_) : i{ i_ } {}
+
+    int i;
+
+    friend bool operator==(NoDefaultConstruction const& lhs, NoDefaultConstruction const& rhs) { return lhs.i == rhs.i; }
+};
+
+TEST_CASE("ranges chain no-default-constructible type") {
+    std::vector<NoDefaultConstruction> vec1{
+        NoDefaultConstruction(1),
+        NoDefaultConstruction(2)
+    };
+
+    std::vector<NoDefaultConstruction> vec2{
+        NoDefaultConstruction(3),
+        NoDefaultConstruction(4)
+    };
+
+    std::vector<NoDefaultConstruction> vec_expected{
+        NoDefaultConstruction(1),
+        NoDefaultConstruction(2),
+        NoDefaultConstruction(3),
+        NoDefaultConstruction(4)
+    };
+
+    auto vec_actual = chain(vec1, vec2) | to_vector();
+    CHECK(vec_actual == vec_expected);
+}
+
 TEST_CASE("ranges chain advance_by") {
     auto input = chain(seq() | take(3), seq(10) | take(3), seq(20) | take(3));
     auto result1 = input | to_vector();

From a5b2dd15e4683e8f584eaf2112eeb477fbc2da4a Mon Sep 17 00:00:00 2001
From: Hazurl <ricoujules69@gmail.com>
Date: Sun, 1 Dec 2019 16:49:05 +0100
Subject: [PATCH 3/3] Fill Changelog

---
 CHANGELOG.md | 9 +++++++++
 1 file changed, 9 insertions(+)

diff --git a/CHANGELOG.md b/CHANGELOG.md
index ee1e6bd..ed67537 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,3 +1,12 @@
+## Version 2.0.1
+
+Many optimizations, bug fixes, and new features. With this version, the popular "calendar" showcase
+for range libraries is supported (see [./test/calendar.cpp](calendar.cpp) for more).
+
+### Other
+
+- ChainRange does not require the output type to be default-constructible.
+
 ## Version 2.0.0
 
 Many optimizations, bug fixes, and new features. With this version, the popular "calendar" showcase
