%global source0_hash 869675ad2d7541aea90c6d88c81f46a7f4ea9af8cd0395d38f11a95126998a0d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate pure-rust-locales

Name:           rust-pure-rust-locales
Version:        0.8.2
Release:        %autorelease
Summary:        Pure Rust locales imported directly from the GNU C Library. `LC_COLLA...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/pure-rust-locales
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Pure Rust locales imported directly from the GNU C Library. `LC_COLLATE` and `LC_CTYPE` are not yet supported.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.Apache-2.0
%license %{crate_instdir}/LICENSE.MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+defmt-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+defmt-devel %{_description}

This package contains library source intended for building other packages which
use the "defmt" feature of the "%{crate}" crate.

%files       -n %{name}+defmt-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
