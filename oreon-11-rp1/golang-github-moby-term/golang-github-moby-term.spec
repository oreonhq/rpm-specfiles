%global source0_hash 2733b6246d59bf6362335dbcbfba2d258e1ade9dcf5c6515e0b4dc784955ed10

# Generated by go2rpm 1.8.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/moby/term
%global goipath         github.com/moby/term
%global commit          1aeaba8785877a66f57739be9fccb6f5cfab429e
%gometa

%global common_description %{expand:
Go utilities for dealing with terminals

Package term provides structures and helper functions to work with terminal
(state, sizes).}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        %autorelease -p
Summary:        None

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
