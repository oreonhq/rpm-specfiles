%global source0_hash 49981ce3f04ecb9b4920a469f0103d986ca9699f7f0ba4ed09745a3d5e175db6

# Generated by go2rpm 1.14.0
# Test fail due to racing test in other archs
%ifarch x86_64
%bcond check 1
%else
%bcond check 0
%endif
%bcond bootstrap 0

# Racing tests with 10000 runs need more than 10min
%global gotestflags -short %{gotestflags}

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/cyphar/filepath-securejoin
%global goipath         github.com/cyphar/filepath-securejoin
Version:                0.3.6

%gometa -L -f

%global common_description %{expand:
An implementation of SecureJoin, a
candidate for inclusion in the Go standard library. The purpose of this function
is to be a "secure" alternative to filepath.Join, and in particular it provides
certain guarantees that are not provided by filepath.Join.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-cyphar-filepath-securejoin
Release:        %autorelease
Summary:        Proposed filepath.SecureJoin implementation

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
