%global source0_hash e3534fc53b3abf515c9a9795f40997e8720d9ac27681de70b3e74e3540acb269

# Generated by go2rpm
%bcond check 0

# https://github.com/BurntSushi/xgb
%global goipath         github.com/BurntSushi/xgb
%global commit          5f9e7b3c49cdbee70f16f2e417cb8b473e20bc78

%gometa

%global common_description %{expand:
XGB is the X Go Binding, which is a low-level API to communicate with the
core X protocol and many of the X extensions. It is closely modeled after
XCB and xpyb.}

%global golicenses      LICENSE xgbgen/COPYING
%global godocs          examples AUTHORS CONTRIBUTORS README

Name:           %{goname}
Version:        0
Release:        0.29%{?dist}
Summary:        Low-level api to communicate with the X server

License:        WTFPL
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in xgbgen; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck -d xproto
%endif

%files
%license %{golicenses}
%doc %{godocs}
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
