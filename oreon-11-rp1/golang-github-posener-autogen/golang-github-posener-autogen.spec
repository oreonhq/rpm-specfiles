%global source0_hash 2cbe9937748c13f69b50ccf82c0f8ad7f654b788a37c62f199b0fd63ef5db22f

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/posener/autogen
%global goipath         github.com/posener/autogen
Version:                0.0.2

%gometa

%global common_description %{expand:
Automatically generate files.}

%global golicenses      LICENSE
%global godocs          example

Name:           %{goname}
Release:        %autorelease
Summary:        Automatically generate files

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/posener/script)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
