%global source0_hash 018f9fa060edd15a2a7425cf5c45156fbaf01009f34f9aeea5e456df104a729a

# golang-github-docker.spec
# Generated by go2rpm
%bcond check 0
%global debug_package %{nil}

# https://github.com/docker/go
%global goipath         github.com/docker/go
Version:                1.5.1
%global commit          d30aec9fd63c35133f8f79c3412ad91a3b08be06

%global goname          golang-github-docker-go
%global godevelname     golang-github-docker-go-devel

%gometa

%global common_description %{expand:
Go packages with small patches autogenerated (used for canonical/json).}

%global golicenses      LICENSE
%global godocs          README.md

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Release:        %autorelease
Summary:        Go packages with small patches autogenerated

# Upstream license specification: BSD-3-Clause
# Automatically converted from old format: BSD - review is highly recommended.
License:        LicenseRef-Callaway-BSD
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.lock
Source2:        glide.yaml
Patch0:         Fix-formatting.patch

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%patch -P0 -p1
cp %{S:1} %{S:2} .

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
