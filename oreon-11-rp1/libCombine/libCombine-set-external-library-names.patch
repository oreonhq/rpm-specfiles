--- a/CMakeLists.orig.txt	2022-04-20 12:22:24.000000000 +0200
+++ b/CMakeLists.txt	2022-05-11 14:57:51.517960318 +0200
@@ -344,7 +344,7 @@
 add_definitions( -DUSE_ZLIB )
 
 if (ZLIB_LIBRARY)
-  set(LIBCOMBINE_LIBS ${LIBCOMBINE_LIBS} ZLIB::ZLIB)
+  set(LIBCOMBINE_LIBS ${LIBCOMBINE_LIBS} z)
 endif()
 
 if (LIBSBML_LIBRARY_NAME)
@@ -354,7 +354,7 @@
 endif()
 
 if (ZIPPER_LIBRARY)
-  set(LIBCOMBINE_LIBS ${LIBCOMBINE_LIBS} ZIPPER::ZIPPER)
+  set(LIBCOMBINE_LIBS ${LIBCOMBINE_LIBS} Zipper)
 endif()
 
 set(USING_INTEL FALSE)
