%global source0_hash 445e8f1abdb7a5f0f9ad37c50268799568c6031537773fbe775c657cde462a95

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate afl

Name:           rust-afl
Version:        0.16.0
Release:        %autorelease
Summary:        Fuzzing Rust code with american-fuzzy-lop

License:        Apache-2.0
URL:            https://crates.io/crates/afl
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Fuzzing Rust code with american-fuzzy-lop}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+no_cfg_fuzzing-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+no_cfg_fuzzing-devel %{_description}

This package contains library source intended for building other packages which
use the "no_cfg_fuzzing" feature of the "%{crate}" crate.

%files       -n %{name}+no_cfg_fuzzing-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
