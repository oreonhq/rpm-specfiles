%global source0_hash e2953bfe4f93bbd20cc71198842756f77d161884c99ebbabc41d80231ded88d1

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate defmt

Name:           rust-defmt
Version:        1.1.1
Release:        %autorelease
Summary:        A highly efficient logging framework that targets resource-constraine...

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/defmt
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A highly efficient logging framework that targets resource-constrained devices, like microcontrollers}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+avoid-default-panic-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+avoid-default-panic-devel %{_description}

This package contains library source intended for building other packages which
use the "avoid-default-panic" feature of the "%{crate}" crate.

%files       -n %{name}+avoid-default-panic-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+encoding-raw-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+encoding-raw-devel %{_description}

This package contains library source intended for building other packages which
use the "encoding-raw" feature of the "%{crate}" crate.

%files       -n %{name}+encoding-raw-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+encoding-rzcobs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+encoding-rzcobs-devel %{_description}

This package contains library source intended for building other packages which
use the "encoding-rzcobs" feature of the "%{crate}" crate.

%files       -n %{name}+encoding-rzcobs-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ip_in_core-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ip_in_core-devel %{_description}

This package contains library source intended for building other packages which
use the "ip_in_core" feature of the "%{crate}" crate.

%files       -n %{name}+ip_in_core-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unstable-test-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-test-devel %{_description}

This package contains library source intended for building other packages which
use the "unstable-test" feature of the "%{crate}" crate.

%files       -n %{name}+unstable-test-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
