%global source0_hash a84b605a2453f2096645b2bc81182e9df2a9a5f6c6f8927ad9141521c5f1d9f4

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate ossl-sys

Name:           rust-ossl-sys
Version:        1.5.2
Release:        %autorelease
Summary:        Low-level OpenSSL version 3+ bindings

License:        Apache-2.0
URL:            https://crates.io/crates/ossl-sys
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Low-level OpenSSL version 3+ bindings}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-2.0.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+dynamic-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+dynamic-devel %{_description}

This package contains library source intended for building other packages which
use the "dynamic" feature of the "%{crate}" crate.

%files       -n %{name}+dynamic-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fips-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fips-devel %{_description}

This package contains library source intended for building other packages which
use the "fips" feature of the "%{crate}" crate.

%files       -n %{name}+fips-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
