%global source0_hash aa4da3c766cd7a0db8242e326e9e4e081edd567072893ed320008189715366a4

# Generated by rust2rpm 24
# * tests can only be run in-tree
%bcond check 1
%global debug_package %{nil}

%global crate failure_derive

Name:           rust-failure_derive
Version:        0.1.8
Release:        %autorelease
Summary:        Derives for the failure crate

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/failure_derive
Source0:        %{crates_source}
# https://github.com/rust-lang-deprecated/failure/issues/239
Source1:        https://github.com/rust-lang-deprecated/failure/raw/135e2a3/LICENSE-APACHE
Source2:        https://github.com/rust-lang-deprecated/failure/raw/135e2a3/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Derives for the failure crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
# The "failure" and "failure_derive" crates were officially marked as
# "deprecated" by upstream with the v0.1.8 release in 2020:
# https://github.com/rust-lang-deprecated/failure/pull/347
Provides:       deprecated()

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
