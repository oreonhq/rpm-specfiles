%global source0_hash 1425d9ba60e9d5df600fa53a7b2e34824801770bcb579961bc1852372b7bfcaf

# Generated by go2rpm 1.9.0
# odd unanticipated failures
%ifarch x86_64
%bcond check 0
%endif
%global debug_package %{nil}

# https://github.com/mdlayher/genetlink
%global goipath         github.com/mdlayher/genetlink
Version:                1.3.2

%gometa -f

%global common_description %{expand:
Package genetlink implements generic netlink interactions and data types. MIT
Licensed.}

%global golicenses      LICENSE.md
%global godocs          CHANGELOG.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Package genetlink implements generic netlink interactions and data types. MIT Licensed

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
