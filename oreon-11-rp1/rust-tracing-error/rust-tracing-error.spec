%global source0_hash 8b1581020d7a273442f5b45074a6a57d5757ad0a47dac0e9f0bd57b81936f3db

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate tracing-error

Name:           rust-tracing-error
Version:        0.2.1
Release:        %autorelease
Summary:        Utilities for enriching errors with tracing

License:        MIT
URL:            https://crates.io/crates/tracing-error
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * add missing "tracing/attributes" dev-dependency to fix doctests
Patch:          tracing-error-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Utilities for enriching errors with `tracing`.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+traced-error-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+traced-error-devel %{_description}

This package contains library source intended for building other packages which
use the "traced-error" feature of the "%{crate}" crate.

%files       -n %{name}+traced-error-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
