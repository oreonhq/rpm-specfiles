%global source0_hash c86c44877c47f69cd23611e22029ab26b613f620195b76b3ec20f589367a7962

# Generated by go2rpm
%global debug_package %{nil}
# Needs network and needs access to /var/run
%bcond check 1

# https://github.com/containernetworking/plugins
%global goipath         github.com/containernetworking/plugins
Version:                1.1.1

%gometa

%global common_description %{expand:
Some CNI network plugins, maintained by the containernetworking team.}

%global golicenses      LICENSE
%global godocs          RELEASING.md CONTRIBUTING.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Standard networking plugins, maintained by the CNI team

# Upstream license specification: Apache-2.0
# Automatically converted from old format: ASL 2.0 - review is highly recommended.
License:        Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
