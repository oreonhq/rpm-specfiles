%global source0_hash 02f506689067855a2afcbea692a63f60bcb50a3994722650339c82027d1cbe3e

# Generated by go2rpm 1.3
%bcond check 0

%global debug_package %{nil}

# https://github.com/felixge/httpsnoop
%global goipath         github.com/felixge/httpsnoop
Version:                1.0.1

%gometa

%global common_description %{expand:
Package Httpsnoop provides an easy way to capture http related metrics (i.e.
response time, bytes written, and http status code) from your application's
http.Handlers.}

%global golicenses      LICENSE.txt
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Ccapture http related metrics from your application's http.Handlers

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
