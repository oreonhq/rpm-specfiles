%global source0_hash 232da392e29e46d101fcae6c1e2667d8add738201b3265b051be465dc532880a

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/mreiferson/go-httpclient
%global goipath         github.com/mreiferson/go-httpclient
%global commit          5e475fde3a4dda1261a1a38a38f31b7f7cae8e5d

%gometa

%global common_description %{expand:
Go-httpclient provides an HTTP Transport that implements the RoundTripper
interface and can be used as a built in replacement for the standard library's,
providing:

 - connection timeouts
 - request timeouts

This is a thin wrapper around http.Transport that sets dial timeouts and uses
Go's internal timer scheduler to call the Go 1.1+ CancelRequest() API.}

%global golicenses      LICENSE
%global godocs          README.md

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Version:        0
Release:        %autorelease
Summary:        Go HTTP client with timeouts

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .

%install
%gopkginstall

%if %{with check}
%check
# .: needs network
%gocheck -d .
%endif

%gopkgfiles

%changelog
%autochangelog
