%global source0_hash d8f347189c62a579b8cd5f80714efa178f52e461dc2e6d701d264f5ff22e566c

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate borsh-derive

Name:           rust-borsh-derive
Version:        1.8.0
Release:        %autorelease
Summary:        Binary Object Representation Serializer for Hashing

License:        Apache-2.0
URL:            https://crates.io/crates/borsh-derive
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Binary Object Representation Serializer for Hashing}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+force_exhaustive_checks-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+force_exhaustive_checks-devel %{_description}

This package contains library source intended for building other packages which
use the "force_exhaustive_checks" feature of the "%{crate}" crate.

%files       -n %{name}+force_exhaustive_checks-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+schema-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+schema-devel %{_description}

This package contains library source intended for building other packages which
use the "schema" feature of the "%{crate}" crate.

%files       -n %{name}+schema-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
