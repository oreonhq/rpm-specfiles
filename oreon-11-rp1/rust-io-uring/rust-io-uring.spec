%global source0_hash 9080b15e63775b9a2ac7dca720f7050a8b955e092ea0f6020a4a80f69998cdc0

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate io-uring

Name:           rust-io-uring
Version:        0.7.13
Release:        %autorelease
Summary:        The low-level `io_uring` userspace interface for Rust

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/io-uring
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
The low-level `io_uring` userspace interface for Rust}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+direct-syscall-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+direct-syscall-devel %{_description}

This package contains library source intended for building other packages which
use the "direct-syscall" feature of the "%{crate}" crate.

%files       -n %{name}+direct-syscall-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+io_safety-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+io_safety-devel %{_description}

This package contains library source intended for building other packages which
use the "io_safety" feature of the "%{crate}" crate.

%files       -n %{name}+io_safety-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+overwrite-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+overwrite-devel %{_description}

This package contains library source intended for building other packages which
use the "overwrite" feature of the "%{crate}" crate.

%files       -n %{name}+overwrite-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+sc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+sc-devel %{_description}

This package contains library source intended for building other packages which
use the "sc" feature of the "%{crate}" crate.

%files       -n %{name}+sc-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
