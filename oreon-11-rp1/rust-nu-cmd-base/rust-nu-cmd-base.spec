%global source0_hash 1722248f82c3dda957dd19e579f44a060719fad18b38449572133dbbf38993c4

# Generated by rust2rpm 26
%bcond check 0
%global debug_package %{nil}

%global crate nu-cmd-base

Name:           rust-nu-cmd-base
Version:        0.99.1
Release:        %autorelease
Summary:        Foundation tools to build Nushell commands

License:        MIT
URL:            https://crates.io/crates/nu-cmd-base
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
The foundation tools to build Nushell commands.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * test fixtures are not shipped
%cargo_test -- -- --skip arg_glob::test::exact_vs_bad_glob::case_ --skip arg_glob::test::exact_vs_glob::case_ --skip arg_glob::test::glob_subdirs::case_
%endif

%changelog
%autochangelog
