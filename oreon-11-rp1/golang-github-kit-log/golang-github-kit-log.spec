%global source0_hash b37718967f9cbdb3eea4aa2fa9420b7d329b6bab7f9b85f970db197257226152

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/go-kit/log
%global goipath         github.com/go-kit/log
Version:                0.2.1

%gometa -L -f

%global common_description %{expand:
A minimal and extensible structured logger.}

%global golicenses      LICENSE term-LICENSE
%global godocs          README.md

Name:           golang-github-kit-log
Release:        %autorelease
Summary:        A minimal and extensible structured logger

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

mv term/LICENSE term-LICENSE

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
