%global source0_hash fdd65261866c0192ab3c18a847237aef1803d1917b9da6e5d66feb0724cee606

# Generated by go2rpm 1.8.1
%ifarch s390x
# Disable tests on s390x for now
# https://github.com/mdlayher/netlink/issues/201
%bcond check 1
%else
%bcond check 0
%endif
%global debug_package %{nil}

# https://github.com/mdlayher/netlink
%global goipath         github.com/mdlayher/netlink
Version:                1.7.2

%gometa -f

%global common_description %{expand:
Package netlink provides low-level access to Linux netlink sockets.}

%global golicenses      LICENSE.md
%global godocs          CHANGELOG.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Low-level access to Linux netlink sockets

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%if %{with check}
BuildRequires:  iproute
%endif

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
