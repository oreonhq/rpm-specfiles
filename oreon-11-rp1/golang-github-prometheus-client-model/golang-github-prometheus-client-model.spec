%global source0_hash b9b690bc35d80061f255faa7df7621eae39fe157179ccd78ff6409c3b004f05e

# Generated by go2rpm 1.12.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/prometheus/client_model
%global goipath         github.com/prometheus/client_model
Version:                0.6.1

%gometa -L

%global common_description %{expand:
Data model artifacts for Prometheus.}

%global golicenses      LICENSE NOTICE
%global godocs          CONTRIBUTING.md MAINTAINERS.md README.md

Name:           golang-github-prometheus-client-model
Release:        %autorelease
Summary:        Data model artifacts for Prometheus

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
