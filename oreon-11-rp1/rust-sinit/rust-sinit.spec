%global source0_hash 81b29f588822880371213aec1da87a4b0c042599a0fd813ae8ccf7f9c4fb6a85

# Generated by rust2rpm 22
%bcond check 0

%global crate sinit

Name:           rust-sinit
Version:        0.1.2
Release:        %autorelease
Summary:        Simple init system for use in containers

License:        BSD-2-Clause
URL:            https://crates.io/crates/sinit
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Simple init system for use in containers.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Effective license calculation:
# BSD-2-Clause and (MIT OR Apache-2.0) and (MIT or ASL 2.0)
License:        BSD-2-Clause and (MIT or Apache-2.0)

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%doc README.md
%{_bindir}/sinit

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
