%global source0_hash 42f2f834407ff4e2df5292b364075556d9ced155e5ef450bd1ed81dd2d4105ef

# Generated by go2rpm
%bcond check 0

# https://github.com/eknkc/amber
%global goipath         github.com/eknkc/amber
%global commit          cdade1c073850f4ffc70a829e31235ea6892853b

%gometa

%global common_description %{expand:
Amber is an elegant templating engine for Go Programming Language It is inspired
from HAML and Jade}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        0.31%{?dist}
Summary:        Elegant templating engine for Go Programming Language

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in amberc; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
