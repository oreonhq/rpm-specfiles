%global source0_hash cc35e59094e2c254c884b87065524dc30db313f23de97d9515143f5d80b26916

# Generated by go2rpm 1.11.1
%bcond check 0

# https://github.com/goccy/go-yaml
%global goipath         github.com/goccy/go-yaml
Version:                1.18.0

%gometa -L -f

%global common_description %{expand:
Go package similar to github.com/go-yaml/yaml with some additional
features.}

%global golicenses      LICENSE
%global godocs          README.md CHANGELOG.md

Name:           golang-github-goccy-yaml
Release:        %autorelease
Summary:        YAML support for the Go language

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md CHANGELOG.md
%{_bindir}/ycat

%gopkgfiles

%changelog
%autochangelog
