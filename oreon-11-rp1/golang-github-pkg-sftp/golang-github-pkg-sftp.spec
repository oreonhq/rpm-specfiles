%global source0_hash e48cee5bbf57777264c30d3aea0063d978c8321a7a261cba05f04a6c339dd9c8

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0
%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/pkg/sftp
%global goipath         github.com/pkg/sftp
Version:                1.13.7

%gometa -L -f

%global common_description %{expand:
SFTP support for the go.crypto/ssh package.}

%global golicenses      LICENSE
%global godocs          examples CONTRIBUTORS README.md request-readme.md

Name:           golang-github-pkg-sftp
Release:        %autorelease
Summary:        SFTP support for the go.crypto/ssh package

License:        BSD-2-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
