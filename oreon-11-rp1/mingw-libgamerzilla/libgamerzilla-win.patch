diff --git a/gamerzilla.c b/gamerzilla.c
index ea2d80f..44f1166 100644
--- a/gamerzilla.c
+++ b/gamerzilla.c
@@ -64,7 +64,7 @@ static struct dirent *readdir(DIR *dirp)
 #ifdef _WIN32
 #define GAMERZILLA_USETCP
 
-#define mkdir(x,y) _mkdir(x)
+#define mkdir(x,y) mkdir(x)
 #define writesocket(a,b,c) send(a,(const char *)b,c,0)
 #define readsocket(a,b,c) recv(a,(char *)b,c,0)
 #endif
