%global source0_hash 876a53fff98e03a936a674b29568b0e605f06b29372c2489ff4de23f1949743d

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate num-derive

Name:           rust-num-derive0.3
Version:        0.3.3
Release:        %autorelease
Summary:        Numeric syntax extensions

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/num-derive
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Numeric syntax extensions}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+full-syntax-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+full-syntax-devel %{_description}

This package contains library source intended for building other packages which
use the "full-syntax" feature of the "%{crate}" crate.

%files       -n %{name}+full-syntax-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
