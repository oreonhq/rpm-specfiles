diff --git a/src/archi.cc b/src/archi.cc
index 18f134b..7156bda 100644
--- a/src/archi.cc
+++ b/src/archi.cc
@@ -21,13 +21,6 @@
 
 #include <zlib.h>
 
-#include <bit>
-
-static uint32_t byteswap(uint32_t v)
-{
-    return (v >> 24) | (v << 24) | ((v >> 8) & 0xff00) | ((v << 8) & 0xff0000);
-}
-
 archive::archive(FILE *stream) : f(stream)
 {
     size_t read = 0;
@@ -75,7 +68,7 @@ archive::~archive()
     fclose(f);
 }
 
-archive::file archive::open(std::string_view name)
+archive::file archive::open(const std::string & name)
 {
     for (auto & ff : files)
     {
diff --git a/src/archi.h b/src/archi.h
index 83a48d0..f1f52c2 100644
--- a/src/archi.h
+++ b/src/archi.h
@@ -103,7 +103,7 @@ class archive {
 
         ~archive();
 
-        file open(std::string_view name);
+        file open(const std::string & name);
 
         const auto & filelist() const { return files; }
 
diff --git a/src/configuration.cc b/src/configuration.cc
index 268968c..1954054 100644
--- a/src/configuration.cc
+++ b/src/configuration.cc
@@ -140,8 +140,8 @@ configuration::configuration(const std::string & glob, const std::string & local
 
     fname = local;
 
-    i_start_lives = std::clamp(i_start_lives, 1, 3);
-    i_game_speed = std::clamp(i_game_speed, 0, MAX_GAME_SPEED);
+    i_start_lives = clamp(i_start_lives, 1, 3);
+    i_game_speed = clamp(i_game_speed, 0, MAX_GAME_SPEED);
 }
 
 configuration::~configuration(void)
diff --git a/src/decl.h b/src/decl.h
index c58a5fb..866eb5f 100644
--- a/src/decl.h
+++ b/src/decl.h
@@ -21,6 +21,7 @@
 
 #include <string>
 #include <functional>
+#include <vector>
 
 /* screen width and height, in pixels. */
 extern int currentScreenWidth;
@@ -217,4 +218,12 @@ typedef int mbstate_t;
 size_t mbrtowc (wchar_t * out, const char *s, int n, mbstate_t * st);
 #endif
 
+template<class T>
+T clamp(T v, T lo, T hi )
+{
+    if (v > hi) return hi;
+    if (v < lo) return lo;
+    return v;
+}
+
 #endif
diff --git a/src/leveledit.cc b/src/leveledit.cc
index b6718b7..73e115e 100644
--- a/src/leveledit.cc
+++ b/src/leveledit.cc
@@ -129,7 +129,9 @@ const struct _ed_key _ed_keys[] = {
    {EDACT_GOTOEND,       SDLK_END},
    {EDACT_ROT180,        SDLK_y,     'y'},
    {EDACT_INSROW,        SDLK_INSERT},
+   {EDACT_INSROW,        SDLK_f,     'f'},
    {EDACT_DELROW,        SDLK_DELETE},
+   {EDACT_DELROW,        SDLK_g,     'g'},
    {EDACT_CUTROW,        SDLK_MINUS, '-'},
    {EDACT_PASTEROW,      SDLK_PLUS,  '+'},
    {EDACT_PUTSPACE,      SDLK_SPACE, ' '},
diff --git a/src/screen.cc b/src/screen.cc
index eb18543..2d0dfb8 100644
--- a/src/screen.cc
+++ b/src/screen.cc
@@ -30,6 +30,8 @@
 #include "keyb.h"
 #include "configuration.h"
 
+#include <algorithm>
+
 static SDL_Surface *display = nullptr;
 static SDL_Window *sdlWindow = nullptr;
 static SDL_Renderer *sdlRenderer = nullptr;
@@ -1797,7 +1799,7 @@ static void putthings(long vert, long a, long angle) {
     /* calc the x pos where the thing has to be drawn */
     int x = sintab[a % TOWER_ANGLES] + (SCREENWID/2);
 
-    int depth = std::clamp(400-2*(sintab[(a+TOWER_ANGLES/4) % TOWER_ANGLES]+TOWER_RADIUS+SPR_STEPWID/2+1), 0, 205);
+    int depth = clamp(400-2*(sintab[(a+TOWER_ANGLES/4) % TOWER_ANGLES]+TOWER_RADIUS+SPR_STEPWID/2+1), 0, 205);
 
     int slice = 0;
     int ypos = SCREENHEI / 2 - SPR_SLICEHEI + vert;
@@ -1822,7 +1824,7 @@ static void putthings(long vert, long a, long angle) {
       /* ok calc the angle the robots needs to be drawn at */
       int rob_a = (rob_angle(rob) - 4 + angle) & (TOWER_ANGLES - 1);
 
-      int depth = std::clamp(400-2*(sintab[(rob_a+TOWER_ANGLES/4) % TOWER_ANGLES]+TOWER_RADIUS+SPR_STEPWID/2+1), 0, 205);
+      int depth = clamp(400-2*(sintab[(rob_a+TOWER_ANGLES/4) % TOWER_ANGLES]+TOWER_RADIUS+SPR_STEPWID/2+1), 0, 205);
 
       /* check if the robot is "inside" the current column */
       if (rob_a == a)
@@ -1897,7 +1899,7 @@ static void putthings_editor(long vert, long a, long angle, int state) {
     /* calc the x pos where the thing has to be drawn */
     int x = sintab[a % TOWER_ANGLES] + (SCREENWID/2);
 
-    int depth = std::clamp(400-2*(sintab[(a+TOWER_ANGLES/4) % TOWER_ANGLES]+TOWER_RADIUS+SPR_STEPWID/2+1), 0, 205);
+    int depth = clamp(400-2*(sintab[(a+TOWER_ANGLES/4) % TOWER_ANGLES]+TOWER_RADIUS+SPR_STEPWID/2+1), 0, 205);
 
     int slice = 0;
     int ypos = SCREENHEI / 2 - SPR_SLICEHEI + vert;
