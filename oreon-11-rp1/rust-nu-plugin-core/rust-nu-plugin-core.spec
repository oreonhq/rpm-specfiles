%global source0_hash 3b6de4cbecdc9568acb9e81a6e769813f3decb97802e26fc5c66dc360f18e332

# Generated by rust2rpm 26
%bcond check 0
%global debug_package %{nil}

%global crate nu-plugin-core

Name:           rust-nu-plugin-core
Version:        0.99.1
Release:        %autorelease
Summary:        Shared internal functionality to support Nushell plugins

License:        MIT
URL:            https://crates.io/crates/nu-plugin-core
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          nu-plugin-core-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Shared internal functionality to support Nushell plugins.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+interprocess-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+interprocess-devel %{_description}

This package contains library source intended for building other packages which
use the "interprocess" feature of the "%{crate}" crate.

%files       -n %{name}+interprocess-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+local-socket-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+local-socket-devel %{_description}

This package contains library source intended for building other packages which
use the "local-socket" feature of the "%{crate}" crate.

%files       -n %{name}+local-socket-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# compilation OOMs on i686
%ifnarch %{ix86}
%cargo_test
%endif
%endif

%changelog
%autochangelog
