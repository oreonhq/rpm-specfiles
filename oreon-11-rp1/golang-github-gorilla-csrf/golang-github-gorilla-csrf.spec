%global source0_hash 8cbc7df34ba5266f78c81b5d1a86b822d4107533dd4c3fd9fe16cdee4d916db5

# Generated by go2rpm 1
%bcond check 0

%global debug_package %{nil}

# https://github.com/gorilla/csrf
%global goipath         github.com/gorilla/csrf
Version:                1.7.0

%gometa

%global common_description %{expand:
Gorilla/csrf provides Cross Site Request Forgery (CSRF) prevention middleware
for Go web applications & services.}

%global golicenses      LICENSE
%global godocs          AUTHORS README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Cross Site Request Forgery (CSRF) prevention middleware

# Upstream license specification: BSD-3-Clause
License:        BSD
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/gorilla/securecookie)
BuildRequires:  golang(github.com/pkg/errors)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
