%global source0_hash 237ce90685b801ee09de8c46f774fcb9b53c89cc202582d2e6eaf48564d075bd

# Generated by go2rpm 1.6.0
%bcond check 1

# https://github.com/multiformats/go-multibase
%global goipath         github.com/multiformats/go-multibase
Version:                0.0.3
%global commit          f067816be342404d18f4f8e51eef28b5bc9ad92b

%gometa

%global common_description %{expand:
Implementation of multibase parser in go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Implementation of multibase parser in go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/mr-tron/base58/base58)
BuildRequires:  golang(github.com/multiformats/go-base32)
BuildRequires:  golang(github.com/multiformats/go-base36)

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%build
for cmd in multibase-conv; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
