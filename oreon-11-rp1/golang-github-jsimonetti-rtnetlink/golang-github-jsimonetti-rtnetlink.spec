%global source0_hash 6753cf38b97588fae472fb1ff04f48430136323825c584b296c3a7535642532a

# Generated by go2rpm 1.10.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/jsimonetti/rtnetlink
%global goipath         github.com/jsimonetti/rtnetlink
Version:                1.4.2

%gometa -L -f

%global common_description %{expand:
Package rtnetlink provides low-level access to the Linux rtnetlink API. MIT
Licensed.}

%global golicenses      LICENSE.md
%global godocs          README.md

Name:           golang-github-jsimonetti-rtnetlink
Release:        %autorelease
Summary:        Package rtnetlink provides low-level access to the Linux rtnetlink API. MIT Licensed

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
