%global source0_hash 0892e0d71b97b3730e9d6f2a85a7df2cc9ced0a04c88ea5365d10fe2de91f470

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/gobuffalo/logger
%global goipath         github.com/gobuffalo/logger
Version:                1.0.7

%gometa

%global common_description %{expand:
The logger.Logger interface is used throughout Buffalo apps, and other systems,
to log a whole manner of things.}

%global golicenses      LICENSE
%global godocs          README.md SHOULDERS.md

Name:           %{goname}
Release:        %autorelease
Summary:        Common logging interface for the Buffalo ecosystem

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
