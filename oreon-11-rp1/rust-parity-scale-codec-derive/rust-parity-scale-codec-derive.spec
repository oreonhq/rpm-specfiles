%global source0_hash 1557010476e0595c9b568d16dcfb81b93cdeb157612726f5170d31aa707bed27

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate parity-scale-codec-derive

Name:           rust-parity-scale-codec-derive
Version:        2.3.1
Release:        %autorelease
Summary:        Serialization and deserialization derive macro for Parity SCALE Codec

License:        Apache-2.0
URL:            https://crates.io/crates/parity-scale-codec-derive
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Serialization and deserialization derive macro for Parity SCALE Codec}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+max-encoded-len-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+max-encoded-len-devel %{_description}

This package contains library source intended for building other packages which
use the "max-encoded-len" feature of the "%{crate}" crate.

%files       -n %{name}+max-encoded-len-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
