diff -up libecpint-1.0.7/src/CMakeLists.txt.cerf3 libecpint-1.0.7/src/CMakeLists.txt
--- libecpint-1.0.7/src/CMakeLists.txt.cerf3	2021-12-05 07:40:21.000000000 -0700
+++ libecpint-1.0.7/src/CMakeLists.txt	2025-08-12 06:53:36.736539274 -0600
@@ -65,10 +65,17 @@ if (LIBECPINT_USE_CERF)
         endif()
     endif()
     target_compile_definitions(ecpint PRIVATE USING_CERF=1)
-    target_link_libraries(ecpint
-     PRIVATE
-      cerf::cerf
-    )
+    if(cerf_VERSION VERSION_GREATER_EQUAL "3")
+        target_link_libraries(ecpint
+         PRIVATE
+          cerf::cerfcpp
+        )
+    else()
+        target_link_libraries(ecpint
+         PRIVATE
+          cerf::cerf
+        )
+    endif()
 else()
     message(STATUS "${Cyan}Using Faddeeva${ColourReset}: internal")
     target_link_libraries(ecpint
