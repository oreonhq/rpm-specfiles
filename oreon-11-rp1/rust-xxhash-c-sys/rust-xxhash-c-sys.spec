%global source0_hash 4d4a8210e2c9209f553817636cc6054d3b22424cc879c31b616e4cb4027be705

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate xxhash-c-sys

Name:           rust-xxhash-c-sys
Version:        0.8.7
Release:        %autorelease
Summary:        Bindings to xxhash

License:        BSL-1.0
URL:            https://crates.io/crates/xxhash-c-sys
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * build and link against shared xxhash library
Patch:          xxhash-c-sys-fix-metadata.diff
# * build and link against system xxhash library
Patch2:         0001-build-and-link-against-shared-xxhash-library.patch

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  pkgconfig(libxxhash) >= 0.8.1

%global _description %{expand:
Bindings to xxhash.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       pkgconfig(libxxhash) >= 0.8.1

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+no_std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+no_std-devel %{_description}

This package contains library source intended for building other packages which
use the "no_std" feature of the "%{crate}" crate.

%files       -n %{name}+no_std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# remove bundled copy of xxhash
rm -r src/c

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip a test that hardcodes a check against the system xxhash version
%cargo_test -- -- --exact --skip assert_version
%endif

%changelog
%autochangelog
