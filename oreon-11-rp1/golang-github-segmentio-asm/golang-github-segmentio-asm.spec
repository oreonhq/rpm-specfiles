%global source0_hash e4b74487709aef00ff9df241e577d66ae97056ad20d602e5a6fc03b35039156d

# Generated by go2rpm 1.9.0
%bcond check 0
%global debug_package %{nil}

# https://github.com/segmentio/asm
%global goipath         github.com/segmentio/asm
Version:                1.2.0

%gometa -f

%global common_description %{expand:
Go library providing algorithms optimized to leverage the characteristics of
modern CPUs.}

%global golicenses      LICENSE
%global godocs          CODE_OF_CONDUCT.md CONTRIBUTING.md README.md

%global gosupfiles      build/qsort/sort_asm.go

Name:           %{goname}
Release:        %autorelease
Summary:        Algorithms optimized to leverage the characteristics of modern CPUs

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
for test in "TestSort8" \
            "TestSort16" \
            "TestSort24" \
            "TestSort32" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
# https://github.com/segmentio/asm/issues/73
%gocheck -t build
%endif

%gopkgfiles

%changelog
%autochangelog
