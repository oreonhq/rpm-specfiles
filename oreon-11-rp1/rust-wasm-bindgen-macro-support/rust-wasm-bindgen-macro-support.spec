%global source0_hash f3997c7839262f4ef12cf90b818d6340c18e80f263f1a94bf157d0ec4420380e

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate wasm-bindgen-macro-support

Name:           rust-wasm-bindgen-macro-support
Version:        0.2.126
Release:        %autorelease
Summary:        Implementation APIs for the `#[wasm_bindgen]` attribute

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/wasm-bindgen-macro-support
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Implementation APIs for the `#[wasm_bindgen]` attribute}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT

%{crate_instdir}/

%package     -n %{name}+extra-traits-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+extra-traits-devel %{_description}

This package contains library source intended for building other packages which
use the "extra-traits" feature of the "%{crate}" crate.

%files       -n %{name}+extra-traits-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+strict-macro-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+strict-macro-devel %{_description}

This package contains library source intended for building other packages which
use the "strict-macro" feature of the "%{crate}" crate.

%files       -n %{name}+strict-macro-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
