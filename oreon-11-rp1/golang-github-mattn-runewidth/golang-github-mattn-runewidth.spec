%global source0_hash e98c1e4a82b8a6d026ee63385955cc178c91c77f106c7cf3d743736301106a8f

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/mattn/go-runewidth
%global goipath         github.com/mattn/go-runewidth
Version:                0.0.16

%gometa -L

%global common_description %{expand:
Package go-runewidth provides functions to get fixed width of a character or
string.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-mattn-runewidth
Release:        %autorelease
Summary:        Functions for getting fixed width of a character or string

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
