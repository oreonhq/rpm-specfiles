%global source0_hash 4cb68c231e2575f7503a7c19213875f9d4ec2e84e963a56ce3de4b6bee351ef7

# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate vcsgraph

Name:           rust-vcsgraph
Version:        0.2.0
Release:        %autorelease
Summary:        Library to perform various computation of a version control graph

# https://foss.heptapod.net/mercurial/vcsgraph/-/issues/16
License:        GPL-2.0-or-later
URL:            https://crates.io/crates/vcsgraph
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * prevent development-only binaries from being built and installed
# * drop structopt dependency that is only needed for these binaries
Patch:          vcsgraph-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A library to perform various computation of a version control graph.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
