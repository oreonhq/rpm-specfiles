%global source0_hash 11590ff4fcd1dc844513c03cf332e7e061165a3ab5ce28a029334e90e4a33b53

# Generated by go2rpm
%bcond check 0

%global debug_package %{nil}

# https://github.com/kballard/go-shellquote
%global goipath         github.com/kballard/go-shellquote
%global commit          95032a82bc518f77982ea72343cc1ade730072f0

%gometa

# Remove in F33:
%global godevelheader %{expand:
Obsoletes:      golang-github-kballard-go-shellquote-devel < 0-0.12
}

%global common_description %{expand:
Shellquote provides utilities for joining/splitting strings using sh's
word-splitting rules.}

%global golicenses      LICENSE
%global godocs          README

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Version:        0
Release:        %autorelease
Summary:        Go utilities for performing shell-like word splitting/joining

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
cp %{S:1} %{S:2} .

%install
%gopkginstall

# Remove in F33
# Remove erroneous glide.lock folder
%pretrans devel -p <lua>
path = "%{gopath}/src/%{goipath}/glide.lock"
st = posix.stat(path)
if st and st.type == "directory" then
  os.remove(path)
end

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
