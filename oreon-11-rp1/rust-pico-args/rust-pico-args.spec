%global source0_hash 5be167a7af36ee22fe3115051bc51f6e6c7054c9348e28deb4f49bd6f705a315

# Generated by rust2rpm 28
%bcond check 0
%global debug_package %{nil}

%global crate pico-args

Name:           rust-pico-args
Version:        0.5.0
Release:        %autorelease
Summary:        An ultra simple CLI arguments parser.

License:        MIT
URL:            https://crates.io/crates/pico-args
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An ultra simple CLI arguments parser.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+combined-flags-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+combined-flags-devel %{_description}

This package contains library source intended for building other packages which
use the "combined-flags" feature of the "%{crate}" crate.

%files       -n %{name}+combined-flags-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+eq-separator-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+eq-separator-devel %{_description}

This package contains library source intended for building other packages which
use the "eq-separator" feature of the "%{crate}" crate.

%files       -n %{name}+eq-separator-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+short-space-opt-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+short-space-opt-devel %{_description}

This package contains library source intended for building other packages which
use the "short-space-opt" feature of the "%{crate}" crate.

%files       -n %{name}+short-space-opt-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
