%global source0_hash d3dd19c9e21b534bebc5a44a8699542f6e537dfdd7d67979fb6551a3053ed695

# Generated by go2rpm 1.3
%bcond check 0

%global debug_package %{nil}

# https://github.com/schollz/logger
%global goipath         github.com/schollz/logger
Version:                1.2.0

%gometa

%global common_description %{expand:
Simplistic, opinionated logging for Golang.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Simplistic, opinionated logging for Golang

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
