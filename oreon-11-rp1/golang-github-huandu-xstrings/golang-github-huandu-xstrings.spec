%global source0_hash 2b242f966abe8aa75dbd6cf2c6625692feea117bc937b9f3fe76b99cdaf7365a

# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/huandu/xstrings
%global goipath         github.com/huandu/xstrings
Version:                1.5.0

%gometa -L -f

%global common_description %{expand:
Go package xstrings is a collection of string functions, which are widely used
in other languages but absent in Go package strings.

All functions are well tested and carefully tuned for performance.}

%global golicenses      LICENSE
%global godocs          CONTRIBUTING.md README.md

Name:           golang-github-huandu-xstrings
Release:        %autorelease
Summary:        Collection of useful string functions in Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
