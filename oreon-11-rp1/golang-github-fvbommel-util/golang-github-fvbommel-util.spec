%global source0_hash 51ef3ab185d632aa97d614ed31b7f2d01ccd3a58599db96525c57afa10f1b15f

# Generated by go2rpm 1.3
%bcond check 0

# https://github.com/fvbommel/util
%global goipath         github.com/fvbommel/util
Version:                0.0.3

%gometa

# Remove in F36:
%global godevelheader %{expand:
Obsoletes:      golang-vbom-util-devel < 0-0.8
}

%global goaltipaths     vbom.ml/util

%global common_description %{expand:
Go utility packages.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        19%{?dist}
Summary:        Go utility packages

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%if %{with check}
# Tests
BuildRequires:  golang(github.com/bruth/assert)
%endif

%description
%{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
rm -rf sortorder

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
