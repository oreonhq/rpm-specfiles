%global source0_hash 37d975b280d884ca0d55a800bc6e47314b6e86268e56254f9d15d19ca9404eb8

# Generated by go2rpm 1.8.1
%bcond check 0
%global debug_package %{nil}

# https://github.com/go-logr/stdr
%global goipath         github.com/go-logr/stdr
Version:                1.2.2

%gometa -f

%global common_description %{expand:
Logr implementation against the stdlib log package.}

%global golicenses      LICENSE
%global godocs          example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Logr implementation against the stdlib log package

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}
# log used to write out k1:v1,k2:v2 and now writes k1=v1 k2=v2
# https://github.com/go-logr/stdr/pull/29
Patch:          stdr-fix-expected-output.diff

%description %{common_description}

%gopkg

%prep
test "%{source0_hash}" = "none" || { f="%{SOURCE0}"; test -f "$f" || { echo "oreon: missing Source0 $f" >&2; exit 1; }; h=$(sha256sum "$f" | awk '{print $1}'); test "$h" = "%{source0_hash}" || { echo "oreon: Source0 hash mismatch" >&2; exit 1; }; }

%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
