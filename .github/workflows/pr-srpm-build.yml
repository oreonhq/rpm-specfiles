name: SRPM Build

on:
  pull_request_target:
    paths:
      - "**/*.spec"

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.find.outputs.packages }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Find changed package directories
        id: find
        run: |
          pkgs=$(git diff --name-only --diff-filter=d \
              "${{ github.event.pull_request.base.sha }}" \
              "${{ github.event.pull_request.head.sha }}" \
            | grep '\.spec$' \
            | xargs -r -I{} dirname {} \
            | sort -u \
            | jq -Rc '.' | jq -sc '.')
          echo "packages=$pkgs" >> "$GITHUB_OUTPUT"

  srpm:
    needs: detect
    if: needs.detect.outputs.packages != '[]'
    runs-on: ${{ matrix.arch.runner }}
    container:
      image: fedora:44
    strategy:
      fail-fast: false
      matrix:
        pkg: ${{ fromJSON(needs.detect.outputs.packages) }}
        arch:
          - { runner: ubuntu-latest, name: amd64 }
          - { runner: ubuntu-24.04-arm, name: aarch64 }
    name: srpm / ${{ matrix.arch.name }} / ${{ matrix.pkg }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Verify package and architecture support
        id: check
        run: |
          spec=$(find "${{ matrix.pkg }}" -name '*.spec' | head -1)
          if [ -z "$spec" ]; then
            echo "No spec file found for ${{ matrix.pkg }}. Skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          excl_arch=$(grep -i "^ExclusiveArch:" "$spec" | awk '{print $2}')
          
          if [ "$excl_arch" = "x86_64" ]; then excl_arch="amd64"; fi

          if [ ! -z "$excl_arch" ] && [ "$excl_arch" != "${{ matrix.arch.name }}" ]; then
            echo "Package exclusively supports $excl_arch. Skipping run on ${{ matrix.arch.name }}."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"

      - name: Install tools
        if: steps.check.outputs.skip != 'true'
        run: dnf install -y --quiet rpm-build rpmdevtools

      - name: Setup rpmbuild tree
        if: steps.check.outputs.skip != 'true'
        run: rpmdev-setuptree

      - name: Stage spec and local sources
        if: steps.check.outputs.skip != 'true'
        run: |
          spec=$(find "${{ matrix.pkg }}" -name '*.spec' | head -1)
          cp "$spec" ~/rpmbuild/SPECS/
          find "${{ matrix.pkg }}" -maxdepth 1 -type f ! -name '*.spec' | xargs -r cp -t ~/rpmbuild/SOURCES/

      - name: Fetch remote sources
        if: steps.check.outputs.skip != 'true'
        run: |
          spec=$(find "${{ matrix.pkg }}" -name '*.spec' | head -1)
          pkgdir=$(cd "$(dirname "$spec")" && pwd)
          dist=$(bash .github/scripts/oreon-dist.sh "${{ matrix.pkg }}")
          args=(--define "_sourcedir $pkgdir")
          [ -n "$dist" ] && args+=(--define "dist $dist")
          spectool -g -C ~/rpmbuild/SOURCES/ "${args[@]}" "$spec"

      - name: Build SRPM
        if: steps.check.outputs.skip != 'true'
        run: |
          spec=$(basename "$(find "${{ matrix.pkg }}" -name '*.spec' | head -1)")
          dist=$(bash .github/scripts/oreon-dist.sh "${{ matrix.pkg }}")
          args=()
          [ -n "$dist" ] && args+=(--define "dist $dist")
          rpmbuild -bs "${args[@]}" ~/rpmbuild/SPECS/"$spec"

      - name: Set artifact name
        id: meta
        if: success() && steps.check.outputs.skip != 'true'
        run: echo "name=srpm-${{ matrix.arch.name }}-$(basename '${{ matrix.pkg }}')" >> "$GITHUB_OUTPUT"

      - name: Upload SRPM
        uses: actions/upload-artifact@v4
        if: success() && steps.check.outputs.skip != 'true'
        with:
          name: ${{ steps.meta.outputs.name }}
          path: /root/rpmbuild/SRPMS/*.src.rpm

  report:
    needs: [detect, srpm]
    if: always() && needs.detect.outputs.packages != '[]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const { data: { jobs: allJobs } } = await github.rest.actions.listJobsForWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId,
              per_page: 100,
            });

            const srpmJobs = allJobs.filter(j => j.name.startsWith('srpm /'));
            const icon = c => ({ success: '✅', failure: '❌', cancelled: '⏸️' }[c] ?? '⏳');

            const rows = srpmJobs.map(j => {
              const [, arch, ...rest] = j.name.split(' / ');
              const pkg = rest.join(' / ').split('/').pop();
              const url = j.html_url;
              return `| [\`${pkg}\`](${url}) | ${arch} | ${icon(j.conclusion)} ${j.conclusion ?? 'in progress'} |`;
            }).join('\n');

            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const marker = '';
            const body = [
              marker,
              `### SRPM Build — [run #${context.runNumber}](${runUrl})`,
              '',
              '| Package | Architecture | Result |',
              '|---------|-------------|--------|',
              rows,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
            });

            const existing = comments.find(c => c.body.startsWith(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body,
              });
            }