Index: marisa-trie-0.3.0/CMakeLists.txt
===================================================================
--- marisa-trie-0.3.0.orig/CMakeLists.txt
+++ marisa-trie-0.3.0/CMakeLists.txt
@@ -311,3 +311,18 @@ install(
 
 configure_file(marisa.pc.in ${CMAKE_CURRENT_BINARY_DIR}/marisa.pc @ONLY)
 install(FILES ${CMAKE_CURRENT_BINARY_DIR}/marisa.pc DESTINATION ${LIB_INSTALL_DIR}/pkgconfig)
+
+install(
+  FILES
+    "${CMAKE_CURRENT_BINARY_DIR}/libmarisa.so"
+    "${CMAKE_CURRENT_BINARY_DIR}/libmarisa.so.0"
+    "${CMAKE_CURRENT_BINARY_DIR}/libmarisa.so.0.3.0"
+    DESTINATION
+      ${LIB_INSTALL_DIR}
+)
+
+if(ENABLE_TOOLS)
+  foreach(_tool ${MARISA_TOOLS})
+    install(FILES ${CMAKE_CURRENT_BINARY_DIR}/${_tool} DESTINATION ${BIN_INSTALL_DIR})
+  endforeach()
+endif()
